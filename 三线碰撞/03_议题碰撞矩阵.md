# 03 议题碰撞矩阵

> 三线在 12 个议题域上的对称碰撞。每格判定六档关系之一：`一致` / `互补` / `精度升级` / `可归并` / `冲突` / `单线独有`。
> 冲突项编号 `Cxx` 指向 `04_冲突拍板单.md`；可融合项指向 `05_可直接融合清单.md`。
> 出处锚约定：线 A = `HANDOFF` / `REVISION` / `DISCUSSION_01`；线 B = `CBGD`（`CHAR_BUILD_GUIDE_DESIGN.md`）及配套 + `design/` 下 6 份审查报告；线 C = 文件名。
> 教程依据一律引 `01_教程事实底册.md` 的对应节。
>
> ⚠️ **本文件已经过独立证伪复核并回修**（见 `07_证伪复核报告.md`）。**3 条原判冲突被消解**（C4 / C8 / C13 的一半），因为线 B 的对应内容在 `design/` 审查报告里而不在主文档；**1 条教程证据被方向性修正**（D07）。修正处均带 ⚠️ 标注。

---

## 〇、先看这张表

三线不是三个版本，是**三个粒度**。下表的"厚度"是各线在该域的决策原子数（主域计）。

| 域 | 议题域 | 线 A | 线 B | 线 C | 主导线 | 总关系判定 | 冲突 |
|---|---|---|---|---|---|---|---|
| D01 | skill 边界与拆分 | 11 | 55 | 10 | **B** | 可归并 + 互补 | C1 |
| D02 | 文件架构与 refs 切分 | 15 | 50 | 36 | B / C 并列 | **冲突**（切分维度不同） | **C2** **C3** |
| D03 | 阶段流程模型 | 24 | 12 | 20 | 三线均衡 | 可归并 + 冲突 | ~~C4~~ → 确认项 · C5 |
| D04 | 诊断体系 | 14 | 8 | **57** | **C** | 精度升级 + 互补 | C6 |
| D05 | 追问动作库 | 13 | 18 | **30** | **C** | 精度升级 + 可归并 | C7 |
| D06 | AI 推演权限 | **18** | 11+ | 2 | A / B 一致，**但教程反对** | 可归并 + **已拍板** | ✅ ~~C8~~ → `09` D-01 |
| D07 | 术语暴露 | 10 | 4 | 8 | 三线对立 | **冲突** | **C9** |
| D08 | 文档排版与记录 | 23 | **35** | 1 | **B** | 精度升级 + **冲突** | **C10** C11 |
| D09 | MVC | 10 | **37** | **0** | **B** | 精度升级 + 可归并 | C12 |
| D10 | 意象 | 8 | 4+ | 1 | 三线均薄 | 一致 + 冲突（位置） | 并入 C11 |
| D11 | 快速路径与卡住 | 8 | **16+** | 4 | **B** | 互补 + 可归并 | C13（已重写） |
| D12 | 命名与 slug | 4 | 12 | 2 | **B** | **冲突**（L0） | **C14** |

> "线 B 厚度带 `+`"的三格，是证伪复核发现主文档之外的 `design/` 审查报告里还有内容（`KNOT_BODY_VS_REFS.md` 的心理学框架落位、`E_SECTION_DESIGN.md` 的降级链条、`CBGD §十三 G3` 的意象写法）。原盘点把 6 份审查报告当"按需回查"，导致对线 B 做了 3 条不成立的全称否定。

**三条线的性格**（这是理解整个碰撞的钥匙）：

- **线 A = 协作契约线。** 它唯一系统回答了"AI 和人在这场对话里各自能做什么、谁裁决、什么要确认什么只需告知"。代价：**D01 拆分完全空白**（单 skill 是默认前提不是决策）、**D02 文件清单自己就打架**、**D05 有 13 条却没有一个追问动作的完整定义**。
- **线 B = 工程化线。** 它唯一系统回答了"这个 skill 怎么落成文件、决策怎么记、人物文档长什么样、什么时候可以停"。代价：**D04 诊断极薄**（8 条且无判定树）、**D05 只有措辞样本没有动作库**。
- **线 C = 可执行精度线。** 它唯一系统回答了"AI 每一轮怎么判断用户这句话有没有问题、判断出来用哪个动作、这个动作做到什么算够"。代价：**D09 MVC 完全空白**、**D08 文档机制几乎空白**、**D06 推演权限几乎空白**。

**三线互补性极强，真冲突比预期少。** 经证伪复核后剩 **11 个真冲突 + 3 个确认项**：L3 哲学级只有 1 个（**C9 术语暴露**），L2 行为契约级 1 个（**C10 过滤同步**），其余是 L1 结构 / L0 命名。

**证伪复核消解掉的 3 条**（原本会让你白拍板）：

| 原冲突 | 为什么不是冲突 |
|---|---|
| ~~C8 心理学框架~~ | 线 B `KNOT_BODY_VS_REFS.md` **也有** 4 核心框架 + 扩展池，落位方案与线 A `REVISION §1.4` 逐条重合。⚠️ **但这个"不是冲突"的结论后来被推翻**——两线同源于同一个未读教程时的构想，一致性不构成证据。经教程侧专题查证（`08`）升级回真冲突，**现已拍板撤回**（`09` D-01） |
| ~~C13 二选一~~ | 线 B `E_SECTION_DESIGN.md 决策 6` **就是**"镜子→脚手架→推演→溯源→降级路径"，切换判据与线 A `§2.22` 几乎逐字，且被 `KNOT_BODY_VS_REFS.md` 列为 body 必备。不是二选一，是**线 B 内部两套并存需要分工** |
| ~~C4 排版还是推演~~ | `CBGD §12.8 决策 L4` **依据第 3 条已自答**："否决 b2 的'核心人格层在前'……混淆了构建路径（多入口）和文档框架（固定结构）。文档框架只有一个，按依赖链排最稳定。"L4 只否决文档框架，且明确承认构建路径是多入口 |

---

## 〇之二、必须先说的一件事：线 A 的时序不是单向链

盘点时发现的重大事实，它改变了三个预判：

**`DESIGN_HANDOFF.md` 是一份被反向回填过的活文档。** 正文里有 13 处 `2026-08-07` 标注（`HANDOFF:186/188/220/231/256/322/340/464/575/598/628/671/675`），把 REVISION 和 DISCUSSION_01 的结论写回了 handoff，并在 §2.23/§6.6 上进一步裁定了 DISCUSSION_01 挂起的议题。**后果是 REVISION 有若干条被 handoff 反向推翻**：

| REVISION 主张 | 被 HANDOFF 反向推翻为 | 锚 |
|---|---|---|
| §2.2 世界设定与用户角色**同时问** | §2.9 第二步"**先后不是同时**——先世界设定，再用户角色" | `HANDOFF §2.9` |
| §2.4 意象 = **必须** | §2.6 步骤分级"意象｜**必须尝试**"（2-3 个方向不贴可跳过） | `HANDOFF §2.6`、`§2.23` |
| §2.3 MVC 含**意象** | §2.21"MVC = **调色盘 + 二次解释**" | `HANDOFF §2.21` |
| §2.3 MVC 时**展示蓝图** | ⚠️ 经复核修正：`DISCUSSION_01 §决策 1` 否决的是"**快照**"和"自然衔接的**花招**"，**不是蓝图**。同节 `§待确认` 明写"MVC 时是否需要展示'蓝图'"**仍未决**，`HANDOFF §2.17` 蓝图示例仍完整保留 | `DISCUSSION_01 §决策 1` |
| §五 6.1 否决后重推**不预设机制** | §2.19d + §2.22 改为**预设**统一四步，且"禁止跳过溯源" | `HANDOFF §2.22` |

**这直接修正了两个原本被当作硬冲突的预判**（详见 C4、C8）——只按标称时序压平会把结论搞反。这很可能就是上一次融合"撤回两条已证伪断言"的成因。

---

## 〇之三、融合前必须先各线自洽

盘点共记录 **83 条各线内部张力**：线 A 21 条、线 B 前半 26 条 + 后半 15 条、线 C 21 条。

**其中 6 条严重到会污染跨线碰撞结论**，必须在融合前先由本线自裁：

| 编号 | 线 | 张力 | 为什么会污染碰撞 |
|---|---|---|---|
| A/T-01 | A | references 是 8 个（`HANDOFF §4`）还是 6 个（`REVISION §四`） | 拿哪个去和线 B 的 11 个、线 C 的 9 个比，结论完全不同 |
| A/T-08 | A | §2.4"阶段/下一步概念被取消" vs §2.16/§2.19b"SKILL.md 阶段三"、§2.8 线性环路图、§2.6 步骤分级表 | 直接决定 D03 是不是冲突 |
| A/T-19 | A | §2.17 蓝图把**多面性排在核心人格层之前**（恰是本线明文拒绝的顺序），且意象排最后——与本线的呈现顺序（意象第一）、构建顺序（层在面前）**都不一致** | 线 A 自己有三套互斥的顺序，无法确定拿哪套参与 C4/C11 |
| B/T16 | B | §2.2"不替用户生成"（硬约束）vs §4.2/§4.3.3 卡住阶梯"从已有素材推一个具体候选"、G7"示范允许" | 直接决定 D06 是不是 L2 冲突 |
| B/T20 | B | MVC 告知原话出现"多面性/混色/核心人格层"三个术语，与本线"不把术语抛给用户"冲突 | 直接决定 C9 术语冲突的实际边界 |
| C/T-19 | C | 4 个基础诊断的**外部问法与质量门无处安放**——按"诊断不是话术"不该在 SKILL.md，9 个 probe 里也没有它们的文件 | 线 C 的架构有洞，无法直接和线 B 的 11 refs 做结构比较 |

**处理原则**：本矩阵在涉及这 6 条时，采用"**取本线现行态中论证最完整的一支**"，并在对应格子里标注该假设。拍板时如果用户对本线自裁给出不同结论，相关格子需重判。

---

## D01 skill 边界与拆分 —— 可归并 + 互补

### 三线各说什么

| 线 | 主张 | 锚 |
|---|---|---|
| **A** | **未做决策。单 skill 是默认前提。** 三份文档全程没有"做几个 skill""谁路由给谁"的讨论。只有 §2.15 边界（不生成开场白、不写正文、不做 NSFW、不替代作者决策） | `HANDOFF §2.15`；空白见原子 A-008 |
| **B** | **4 个 skill**：`char-build-guide`（主入口，阻塞项）/ `dialogue-persona` / `worldbook-config` / `user-info-calibration`。经历 5→6→3→4 的完整演进，每步都有否决理由 | `CBGD §六`、`verified.md`、`DESIGN_DECISIONS.md`、`SKILL_DECOMPOSITION_BRIEF.md` |
| **C** | **当前只做 1 个角色塑造 skill** + 记录 4 个"发现的其他 skill"（角色卡设计 / 世界书格式转换 / 角色验证体检 / 台词人设），并给出依赖关系 | `PIPELINE_STATE.md §发现的其他 skill` |

### 教程裁定

**教程明确支持拆分**，且给出了最硬的一条依据（`01_教程事实底册 §三`）：执行器四禁"不要输出完整角色卡格式，不要写开场白，不要写世界书，不要写台词人设成品"，且整理方向"必须服务于：调色盘、混色、三面性、核心人格层、二次解释"——下游清单里没有开场白、世界书、用户信息、长期性。

### 关系判定

**可归并。** 线 B 的 4 skill 与线 C 的 1+4 是**同一张地图的两个时间切片**：

| 线 C 的未来 skill | 线 B 的对应物 | 判定 |
|---|---|---|
| 世界书格式转换 skill | `worldbook-config` | 可归并 |
| 台词人设 skill | `dialogue-persona` | 可归并 |
| 角色卡设计 skill（机制/长期性/开场白/用户信息） | 部分 = `user-info-calibration`；机制/长期性/开场白在线 B 是"**当前无 skill 覆盖，B 段诚实标注缺口**" | **互补**——线 C 给出了线 B 自认缺口的填法 |
| **角色验证 / 体检 skill** | **线 B 无对应** | **单线独有 → C1** |

⚠️ 复核修正：线 C 原文说的是"阶段 1 的 **6 个**基础诊断逻辑可以反过来用作检测工具"（`PIPELINE_STATE.md:71`），不是 4 个——4 个是 `DIAGNOSTICS_DECISION.md` 里收敛后的另一个概念。

线 A 在本域**没有可参与碰撞的内容**，它的 §2.15 边界（不做开场白/不做 NSFW）与线 B/C 的拆分结论方向一致，不冲突。

### 冲突

- **C1（L1）**：线 C 提出"角色验证/体检 skill——4 个基础诊断反过来用作已有角色卡的检测工具"，线 B 完全没有考虑过这个复用方向。要不要纳入？

---

## D02 文件架构与 references 切分 —— 冲突（切分维度正交）

### 三线各说什么

| 线 | SKILL.md 体量 | references | 切分维度 |
|---|---|---|---|
| **A** | ~250-300 行（六段：红线/角色定义/入口引导/决策树/交互原则/禁止事项） | **6 个**（`REVISION §四`）或 **8 个**（`HANDOFF §4`）——本线内打架 | 按**方法论构件** |
| **B** | **~100 行** body（80-120） | **11 个** | 按**方法论构件 + 流程工具** |
| **C** | ~150-180 行（大脑：4 诊断 + 信号路由 + 流程管控） | **9 个 `probe-*.md`** | 按**追问动作** |

线 B 的 11 个：`palette-guide` / `multi-facet-guide` / `color-mixing-guide` / `core-layer-guide` / `secondary-explanation` / `route-logic` / `case-library` / `question-bank` / `common-mistakes` / `user-confirm-signals` / `quick-path-guide`（锚 `CBGD §5.1.3`）

线 C 的 9 个：`probe-tag-compress` / `probe-why` / `probe-world` / `probe-faces` / `probe-moments` / `probe-core` / `probe-nsfw` / `probe-misread` / `probe-imagery`（锚 `ARCHITECTURE_DECISION.md §最终架构`）

### 关系判定：正交，不是互斥

把两套清单叠在一起看，重叠区共 **5 处**（3 处 1:1 同构、1 处线 C 拆成两个动作、1 处命名差异）：

| 线 B 文件 | 线 C 文件 | 关系 |
|---|---|---|
| `multi-facet-guide` | `probe-faces` | 同一构件，线 C 多了对话轮数（8-15 轮）与"重模式"标注 |
| `color-mixing-guide` | `probe-moments` | 同上 |
| `core-layer-guide` | `probe-core` | 同上 |
| `palette-guide` | `probe-tag-compress` + `probe-why` | **线 C 把调色盘拆成了两个动作** |
| `secondary-explanation` | `probe-misread` | 线 C 叫动作名，线 B 叫构件名 |
| — | `probe-world`、`probe-nsfw` | **线 B 无对应** |
| `route-logic`、`case-library`、`question-bank`、`common-mistakes`、`user-confirm-signals`、`quick-path-guide` | — | **线 C 无对应** |

**线 B 的是"知识块"，线 C 的是"动作块"。** 二者可以共存（body + 构件手册 + 动作手册），但文件数会到 15-18 个，且"调色盘"这一块会同时存在于两套里。这必须拍板。

线 B 有一条线 C 没有的**可操作分界测试**（`CBGD §5.1.1`）："如果把这条从 body 拿掉，AI 是在**每一轮都可能犯同一个错**，还是**只在某个阶段可能犯某个错**？前者必须在 body，后者可以放 references。" 这条测试本身三线通用 → 进 `05`。

线 C 有一条线 B 没有的**统一内部结构**：每个 probe 三段式（AI 内部诊断 / AI 对用户的外部问法 2-4 个 / 质量门），且强制"内部诊断和外部问法必须分开"（`ARCHITECTURE_DECISION.md §references 的职责`）。这条是线 C 最有价值的资产之一 → 进 `05`。

### 教程裁定

**教程未直接涉及**（教程不是 skill）。唯一间接可类比的是 `0.md:415-426` 的 C 类世界观四层拆分（总纲/区域速览常驻，势力详情/NPC 详情按需加载），但那是世界书条目的加载策略，引用时须标注类比性质。**本域无法用教程裁定，必须由用户拍板。**

### 冲突

- **C2（L1）**：references 按**构件**切（线 A/B）还是按**追问动作**切（线 C）还是二维共存？
- **C3（L1）**：SKILL.md 体量 ~100 / ~150-180 / ~250-300，差 3 倍。这不是单纯的行数问题——它取决于 4 个基础诊断的判定树进不进 body（线 C 要进，线 B 的分界测试也判它该进，但线 B 自己的 body 清单里没有它）。

---

## D03 阶段流程模型 —— 可归并 + 冲突

### 三线各说什么

| 线 | 流程模型 | 锚 |
|---|---|---|
| **A** | §2.8 **迭代环**；§2.4 明说"**阶段/下一步概念被取消**"（构建是特征驱动的迭代环，可能同时推进多个特征点）；续接靠"最近更新的特征点旁做标记" | `HANDOFF §2.4`、`§2.8` |
| **B** | §四 **三个职责阶段**：接住用户→追问循环→MVC 暂停→深化/收束。"阶段之间是路标，不是流程强制" | `CBGD §四` |
| **C** | **六阶段流转**：入口分流→基础身份(轻量)→性格质地(主阶段)→AI 可执行性→意象→整理输出。深度分支在性格质地阶段内**由信号触发**，"**深度不设关卡**" | `questioning-moves.md §第三部分` |

### 构建顺序 / 排版顺序

| 线 | 顺序主张 | 锚 |
|---|---|---|
| **A 构建顺序** | 素材收集→调色盘→**核心人格层→多面性**→混色→二次解释→意象。但 `DISCUSSION_01 §3` 已把"层→面"**收窄为 AI 内部推演优先级，不是用户交互硬约束**（螺旋模型） | `HANDOFF §2.6`、`DISCUSSION_01 §决策 3` |
| **A 呈现顺序** | **意象**→素材区→调色盘→核心人格层→多面性→混色→二次解释 | `HANDOFF §2.5` |
| **A 蓝图顺序** | 又是第三套：**多面性在核心人格层之前**、意象最后（本线内张力 A/T-19） | `HANDOFF §2.17` |
| **B** | 构建区 = 教程依赖链：基础信息→调色盘→**多面性→混色→核心人格层**→二次解释→意象，**明确否决"核心人格层在前"** | `CBGD §12.8 决策 L4` |
| **C** | 无构件级排序，只有阶段级流转 | — |

### 教程裁定（这一域教程说得最清楚，见 `01_教程事实底册 §2.2`–`§2.4`）

1. `07_意象篇.md:196` 的七步序列**是排版顺序不是追问顺序**。它回答的问题是"这个放在**人设的什么位置**"（`:192`），理由是"**AI 最后读到的东西记忆残留更强**"（`06:170`）——输出排版理由，不是创作步骤理由。
2. **教程从未规定核心人格层与三面性（多面性）的构建先后。** 三处相关表述分别在说并列同级（`05:44`）、深浅比较（`05:182`）、生成关系（`05:272`），没有一处在说先做哪个。
3. **唯一被明写的构建依赖是"调色盘必须最先"**（`03:41`"先有调色盘，再有三面性。没有调色盘的三面性就是空壳"）。核心人格层的输入依赖是调色盘 + 已写行为 + 背景，**没有任何一处说依赖三面性**。

### 关系判定

**核心人格层 vs 多面性顺序：伪冲突，且线 B 自己已经答了。** ⚠️ 复核修正（原判为 C4 冲突，现降为确认项）：

`CBGD §12.8 决策 L4` 的**依据第 3 条**原文：

> "否决 b2 的'核心人格层在前'（少数派）：**混淆了构建路径（多入口）和文档框架（固定结构）**。姜糖的构建路径（先核心驱动力）不代表文档排列要跟随构建路径——**文档框架只有一个，按依赖链排最稳定**。"

**L4 明确只否决"文档框架排列"，并明确承认"构建路径是多入口"。** 这与线 A `DISCUSSION_01 §决策 3` 的螺旋模型（层→面是 AI 内部推演优先级，不是用户交互硬约束）**完全兼容**。

拆开看就消解了：

- **文档排版顺序**：多面性在核心人格层前 → 线 B 的 L4 与教程 `07:196` 一致，线 A 的 §2.6 说的不是排版。**三线可统一。**
- **推演优先级**：线 A 的理由是"多面性每张面有'功能'部件，而这个'保护什么'恰恰就是核心人格层的防御机制；先挖层，面的功能一次写准"。教程无法裁定（两个方向都说过，见 `01` §2.4），但 `7.md:488-510` 层写完可以让面降级/删掉，是旁证。**L4 没有否决它，线 A 也已降格为内部优先级。** → 可归并。

**阶段概念存废：可归并。** 三线说的是三个不同层面：

| 层面 | 谁说的 | 结论 |
|---|---|---|
| AI 内部的流程管控 | 线 C 六阶段、线 B 三阶段 | 保留，用户无感知 |
| 文档里要不要记"我现在在第 3 阶段"字段 | 线 A §2.4 取消 | 取消 |
| 文档里怎么记"上次挖到哪" | 线 A"最近更新标记" / 线 B L5 `🔄` + L8 断点记录 | **线 B 是线 A 的精度升级**——`🔄 正在挖` 就是线 A 要的"具体位置标记" |

线 A 的 §2.4 与线 B/C 的阶段模型**不冲突**，前提是线 A 先自裁 A/T-08。

### 冲突

- ~~**C4**~~ **→ 降为确认项**（L4 依据已自答，见上）。只需你确认这个读法：文档框架按依赖链排（面在层前），构建路径多入口、推演优先级采纳线 A 的螺旋模型。
- **C5（L1）**：阶段模型用线 B 的三阶段（职责路标）还是线 C 的六阶段（含入口分流/基础身份/AI 可执行性）？线 C 的更细且带信号路由，线 B 的更抽象。

---

## D04 诊断体系 —— 精度升级 + 互补（融合价值最大的一域）

### 三线各说什么

| 线 | 内容 | 厚度 |
|---|---|---|
| **A** | 绝大多数是 **AI 自查**：推演前内部理解、推演后交叉检查、组装后一致性验证（性格打架吗/衍生矛盾吗/多面性混入二次解释吗/意象从核心矛盾长出吗）。针对用户输入的诊断只有两条粗判据。AI 常犯错误只有五个名字（标签放大/频道切换/情绪点名/戏剧化升级/泛化填充），**无判定树、无正反例** | 14 |
| **B** | §3.2 深度信号识别表 6 行（信号→识别→追问方向）；**质量信号 A–G 7 个 + 假信号黑名单**（回答变长、用了术语、说"我知道了"、情绪激动都明确不算） | 8 |
| **C** | **4 个基础诊断的完整判定树**：三级判定（触发/观察/通过）、灰色地带处理、正例反例、教程锚点、跨诊断优先级（**矛盾 > 结论 > 泛化 > 扁平**）、三条核心纪律、可复现性目标（"同一段输入，AI 10 次判断应得到 10 次一样的结果"） | **57** |

### 关系判定：三种检查在三个不同时机，完全不冲突

这是整个碰撞里最干净的一处互补：

```mermaid
graph LR
    U["用户每轮输入"] --> C4["线 C 4 诊断<br/>判这句话有没有问题"]
    C4 --> P["追问动作"]
    P --> MVC["线 B 质量信号 A-G<br/>判角色活没活 能不能停"]
    MVC --> ASM["线 A 一致性验证<br/>判组装后有没有互相打架"]
```

- **线 C 的 4 诊断** = 输入侧诊断（每轮运行）
- **线 B 的质量信号 A–G** = 停止侧判据（MVC 时运行）
- **线 A 的一致性验证** = 产物侧校验（组装/收束时运行）

三者不是同一件事的三个版本，是三个环节。**直接叠加即可，无需取舍。** → 进 `05`

线 C 对线 A/B 是**精度升级**：线 A 的"AI 常犯错误五个名字"和线 B 的"深度信号识别 6 行"，在线 C 里都有对应的完整判定树。

### 教程裁定

教程给出的可执行判据（`01_教程事实底册 §四 D04`）全部落在线 C 的四诊断上：遮名测试→泛化、放大器+标签打架反测→矛盾、解释性词表→结论。

**但有两条要注意**：

1. **"扁平检测"在教程里没有直接判据。** ⚠️ 复核修正：不要称它为"线 C 的原创扩展"——线 C 自己的口径是 `verified.md`"V1 跨域佐证**通过**：4 个独立场景"。准确表述是：**它通过了线 C 的验证流程，但教程里找不到直接判据**（间接旁证有 `03:263-269` 三问否决清单和点缀色"负责反差"的定义 `02:33-37`）。
2. **大总结版丢了三条可机械匹配的判据**（去重判据 `2.md:414`、合并判据 `2.md:446`、解释性词表 `2.md:444`）。线 C 的结论检测用的正是这条词表，说明线 C 引的是单篇而不是大总结——**这是线 C 的加分项**，也提示融合时要引单篇。

### 冲突

- **C6（L1）**：线 C 的"扁平检测"教程无直接依据，是原创扩展。保留 / 降级为观察项 / 移除？

---

## D05 追问动作库 —— 精度升级 + 可归并

### 三线各说什么

| 线 | 内容 | 关键缺陷 |
|---|---|---|
| **A** | **问法六类**（按"问什么维度"切）。`DISCUSSION_01 §2` 废除了"按可答性排序"，改为"**按用户卡壳类型匹配**"。另有 §2.16 **问题目标层**（补"问的目标是什么、问到什么程度算够"，源头是教程 80 问十组目标） | **13 条却没有一个动作的完整定义**；六类只有名字+一句匹配条件+零散例句；存放位置三方不一（本线张力 A/T-05）；无逐动作质量门 |
| **B** | **追问深度轴**：标签→画面→矛盾→为什么→内核（body 必备第 2 项）；判停标准 = 遮名测试；每个深度层 1-2 个**标志性问法**作为措辞内化样本（明确"不是穷举范例库"） | 只有措辞样本，没有动作库 |
| **C** | **13 个追问动作**，其中 **11 个**有 **AI 内部诊断 / 外部问法 2-4 个示例 / 质量门** 三要素完整（⚠️ 复核修正：2 个用户捞回动作——NSFW 动机追问、世界观锚定追问——**无质量门**）。另有 2 个降级为流程规则、1 个淘汰，全部记了理由 | 编号三套并存（本线张力 C/T-08）；2 个动作质量门待补 |

### 关系判定

**线 C 是线 A/B 的精度升级**，且三者的切法可以叠：

| 线 A 六类 | 线 B 深度轴 | 线 C 13 动作 | 归并方式 |
|---|---|---|---|
| 按"问什么维度"切 | 按"用户现在在哪一层"切 | 按"诊断到什么问题就用哪个"切 | 深度轴当**默认方向盘**（每轮判"现在在哪层、该往哪推"）；13 动作当**动作库**（诊断触发后取用）；六类的"按卡壳类型匹配"当**卡住时的选择器** |

三者是"方向 / 动作 / 卡住兜底"，不互斥 → 进 `05`。

**线 A §2.16 问题目标层 ≈ 线 C 的质量门**，但两者互补：线 A 的是**系统级**（引 80 问十组"这一组解决的是……"，给出"每个问题在探什么"），线 C 的是**动作级**（逐动作"回答到什么程度算够"）。教程的 80 问十组目标声明（`01_教程事实底册 §四 D05`）是两者共同的源头 → 可归并。

**线 B 的判停标准（遮名测试）= 线 C 泛化检测的质量门** → 可归并。

### 冲突

- **C7（L1）**：线 A 的"问法六类"与线 C 的"13 追问动作"存放位置和主从关系。线 C 的 13 动作各自独立成 probe 文件；线 A 的六类连放哪个文件本线内都有三个答案。要不要保留六类这一层？

---

## D06 AI 推演权限 —— 可归并（伪冲突）+ 单线独有

### 这一域的预判被修正了

原本预告"线 A 多框架推演 vs 线 B §2.2 只追问不填空"是 L2 行为契约级硬冲突。**盘点后判定为伪冲突**，理由如下。

### 三线各说什么

| 线 | 主张 | 锚 |
|---|---|---|
| **A** | **三层模式**：镜子（只反射用户说的）→ 脚手架（给试探方向让用户否定）→ 推演（多路径候选）。切换判据基于**产出质量非固定轮数**。**动机追问协议四层**：先问 → 作者答上了就验证不替换 → 作者无解才多路径推演 → 人裁决。红线"AI 永远提案，用户永远裁决"、"推演禁止出形容词，只许出行为场景"、每条推演标注三要素（行为场景/心理机制/**从哪段经历推出来的**） | `HANDOFF §2.22`、`§2.10`、`§2.11`、`§2.14` |
| **B** | §2.2"**只追问，不填空**……这条是硬约束"；§1.4 主动性边界表；G7 **示范允许**但三条硬约束；行为测试**原始版禁止修改版允许**（判据是场景的来源方）；§七 已记下翻译层共识"**推演候选供用户裁决 ≠ AI 替用户生成**" | `CBGD §2.2`、`§1.4`、`§十三 G7`、`§4.3.3`、`§七` |
| **C** | 几乎空白（2 条）。只有"输出必须分三层：已确认 / AI 推论 / 待用户决定"和意象"AI 主动提炼并提议" | `questioning-moves.md §运行规则` |

### 为什么是伪冲突

线 B 的 §1.4 表格自己就写了：AI **应该**"从用户已有行为中找出模式，推给用户确认"，**不应该**"替用户编一个她没有描述过的行为或特质"。这与线 A 的"AI 永远提案用户永远裁决"+"推演必须标注从哪段经历推出来的（闭环可回溯）"**完全一致**。

线 A 自己也规定："**推演的主引擎是从用户素材里抓出重复出现的模式——框架是后备**，当素材内部的模式不明显时才调用。"（`HANDOFF §2.11`）

冲突只在**措辞强度**：§2.2 写成"AI 替用户生成的任何内容都是 AI 数据库里最常见的东西"，字面读会连自己的 G6 四级阶梯（第三级"多选行为引导"、第四级"假设场景"）和 G7（示范允许）一起禁掉。**这是线 B 的内部张力 B/T16，不是跨线冲突。**

教程侧同样支持这个边界（`01_教程事实底册 §四 D06`）：教程内部有真实张力（`02:57` 禁止 AI 代写衍生 vs mentor 反复代写、`04:108-118` 混色画面全部代写），唯一自洽的解读是"**AI 可凝练命名并提交确认，不得引入用户没说过的内容**"——正好是三线的交集。

→ **可归并**，融合时统一措辞为线 B §1.4 的表格版本，删掉 §2.2 的绝对化句子。进 `05`。

### ⚠️ 心理学框架：原判"线 A 独有"不成立，线 B 也有且方案重合

复核修正（`07` 修正 1）。原判断为 L2 冲突 C8，实际是**两线独立收敛到同一方案**：

| 线 | 主张 | 锚 |
|---|---|---|
| **A** | 4 核心框架（依恋理论 / 防御机制 / 社会学习 / 认知图式）+ 8 扩展池 + 筛选标准（生成性 / 行为落地 / 结构性差异 / 领域覆盖 / 卡片映射）+ 排除理由。**落位**：SKILL.md 不列清单只写"从 ≥2 个不同的角度推演"，完整清单进 `references/palette.md` | `HANDOFF §2.11`、`REVISION §1.4` |
| **B** | "**4个核心框架（依恋理论/防御机制/社会学习/认知图式）+扩展池**。AI 在进入推演阶段时去查……**body 里需要内化的是'推演时必须用≥2个框架'这条规则，具体框架清单放 references**" | `KNOT_BODY_VS_REFS.md` |
| **B（兜底原则）** | "推演主引擎是'从用户素材里抓重复出现的模式'，**框架是后备而非首选**" | `B_SEGMENT_AGENT_INSIGHTS.md` |
| **C** | 未覆盖 | — |

线 A `HANDOFF §2.11` 的兜底原则是"推演的主引擎是从用户素材里抓出重复出现的模式——框架是后备"，与线 B 那句**同义**。两线的框架清单、落位规则、兜底原则**逐条重合**。

**这在教程里完全没有** —— 它是为解决"频率偏差"（AI 天然偏向语料高频关联）引入的教程外生成轴，应当被显式标记为增补。

### 冲突

- ~~**C8**~~ ✅ **已拍板：按教程来** → `09_拍板记录.md` D-01。

  ⚠️ **改判轨迹**：初判 L2 冲突 → 证伪复核降为确认项（两线重合）→ **教程侧专题查证升级回真冲突** → 用户拍板关闭。
  关键在于：**"两条线都提到"不构成证据**——它们同源于同一个未读教程时的构想。教程侧查证（`08_专题_行为背后的原因.md`，5 个 agent 覆盖全教程）发现：心理学框架**零命中 + 明文禁令**（`7.md:299`「不要拿一套新理论把原角色重写成另一个人」）；构想设想的产出物形态（"什么经历/心理导致他这样"）被**逐字列为错误示范**（`7.md:472`、`7.md:92`、`11.md:212-220`）。
  **结论**：撤回框架，改为落地教程自己的四条机制（行为题 / 运行规则 / 高可能推论区块 + 四围栏 / 说不出就跳过这一层）。保留问题诊断、反推方向、频率偏差分析。

---

## D07 术语暴露 —— 冲突（唯一的 L3 哲学级）

### 三线各说什么

| 线 | 主张 | 锚 |
|---|---|---|
| **A** | **双名并行**：面向用户输出用产物名 + 首次带括号方法名；内部文档直接用方法名；人物文档标题产物名为主。`REVISION §1.3` **确认保留并扩展**，新增理由：**产物名是隐喻失效时的 fallback**——"'调色盘'这个隐喻在某些模型上可能失效"，且要求 AI"如果用户反馈角色表现不对，先检查是不是模型没理解隐喻"。**方法名对用户可见** | `HANDOFF §2.17`、`REVISION §1.3`、`§四` |
| **B** | §1.3 用户不需要学习教程；**I 段不出现术语**（三方一致），I 段用执行语言不用教学语言；术语放 E 段作 AI 内部标签。**但 MVC 告知原话出现"多面性/混色/核心人格层"三个术语**（本线张力 B/T20） | `CBGD §1.3`、`§七 原 #2`、`§4.3` |
| **C** | **零术语最彻底**：决策 3"skill 不教方法论"、决策 8"遮名测试等诊断逻辑是 AI 内部用的"、每个 probe 强制**内部诊断 / 外部问法两层分离**、**四个诊断词永远不对用户说** | `PIPELINE_STATE.md §关键决策 3/8`、`ARCHITECTURE_DECISION.md`、`DIAGNOSTICS_DECISION.md §追问的核心纪律` |

### ⚠️ 教程裁定：原判"教程未涉及"已撤回，证据方向相反

复核修正（`07` 修正 4，这是本次最严重的一条）。原表述"教程未涉及此议题，三线都是设计外推，谁都不能声称教程支持"**写反了**。

教程正文确实没有**讨论**过这个议题，但唯一一份写给 AI 执行的规范用**示范**给出了答案——`工作流/角色理解24-80-200问AI循环问答工作流.md` 里术语不但对用户出现，还由 AI 主动定义：

```
110| Q24_05：她的底色是什么？
111| 解释：底色是无论场景怎么变都隐隐存在的质地。
119| Q24_08：哪两种颜色最容易撞在一起？
120| 解释：这是混色的入口。
```

产出模板以术语作小节标题（`:256`"## 三、调色盘线索"、`:266`"## 五、核心人格层线索"、`:276`、`:295`），"下一步建议"的选项名是"先写调色盘。先写核心人格层。先写混色画面。"（`:314-319`）。

**因此**：**"全程零术语"在教程里没有原型；"方法名可对用户出现"有直接原型。**

**但这不构成裁定**：执行器是**逐题问答**形态，与三线设计的**追问式对话**形态不同。这条证据能做的是撤销"谁都不能引"这个说法，并把举证责任交还给主张全程零术语的一方。

`AGENTS.md` 的最高约束是"skill 不对用户解释方法论**原理**（除非用户主动问）"——**"不解释原理" ≠ "不能出现词"**。所以 AGENTS.md 也不能直接裁定这一条。

有一类术语**必须**对用户可见：蓝灯/绿灯/D0/扫描深度是酒馆界面上的真实标签（`0.md:620-675`），隐藏术语原则不适用。但这属于 `worldbook-config` 的范围。

### 关系判定

**真冲突，L3。** 而且值得注意的是：**线 B 自己的 T20 漏洞恰好暴露了线 A 想解决的问题**——MVC 告知时要告诉用户"还可以往哪走"，不说术语就说不清楚，线 B 只好泄漏了三个术语。线 A 的双名并行正是为此设计的。

### 冲突

- **C9（L3）**：术语对用户零暴露（线 B/C）vs 双名并行（线 A）。这一条会连带决定：产物名要不要定、MVC 告知怎么说、I 段/probe 外部问法的措辞规范。

---

## D08 人物文档排版与记录机制 —— 精度升级 + 冲突

### 三线各说什么

| 线 | 内容 | 厚度 |
|---|---|---|
| **A** | 素材区 + 构建区**单文件**；§2.4 **过程记录整个取消**（五步推理链）；§2.12 **事件驱动记录**（特征闭环即记录，每轮不自动写）；§2.3c 素材区记录标准（**推演价值**判据）；**§2.15b 确认 vs 同步二分**；最近更新标记续接；§2.5 呈现顺序（意象第一） | 23 |
| **B** | **L1–L12 完整定稿** + §12.1–12.7 承载机制。四层布局（索引头/素材区/构建区/归档区）、素材区四类分组+检索频率排序、构建区依赖链排序+注释、三档状态（🔄/📌/✅）、**轮后静默拆解记录**、埋没点登记制回收、索引头 20 行内、归档区推翻不删除+影响范围、**自然语言投影确认**、最终卡用户手动触发导出、追问日志折叠区否决 | **35** |
| **C** | 几乎空白（1 条）。只有"输出分三类" | 1 |

### 关系判定：线 B 是线 A 的超集，且线 B 自己承认来源

线 B 明确记录了对线 A 的吸收：§12.3 依据 `handoff §2.12`、§12.5 依据 `handoff §2.3c`、§11.5 依据 `handoff §2.4`。这三条是**精度升级**（线 B 更细）。

**但有两条线 A 独有的内容，线 B 不但没吸收，还实质上否决了：**

**① 确认 vs 同步二分（`HANDOFF §2.15b`）—— 与线 B 的 L10 直接冲突**

线 A：纯过滤**不需要确认但必须同步**。理由是"用户说'她喜欢听音乐'，AI 默默不记——用户下次看文档发现漏了，会怀疑'AI 是不是出 bug 了'。信任直接崩掉"；而且同步一句常常钓出隐藏素材（"不不不，她喜欢听音乐是因为那是她爸唯一留给她的东西"）。

线 B L10：**"素材静默，构件投影"**——素材记录是轮后静默的 AI 内部动作，不展示、不打断、不让用户等；只有构建区构件才用自然语言投影给用户看。判据是"素材不是结论不需要确认"。

**两者对"过滤掉一条素材要不要告诉用户"给出了相反答案。** 这是真冲突。

**② 呈现顺序意象第一（`HANDOFF §2.5`）—— 与线 B L4/L2 + 教程冲突**

线 A：意象呈现第一（锚定读文档者对人物整体的感觉），构建最后。
线 B + 教程：意象在最后（`07:192/214/274`，理由是 AI 读到的最后一样东西记忆残留最强）。

**但这两者的读者不同**：线 A 说的是**给人读的人物文档**，教程说的是**给扮演 AI 读的角色卡**。线 B 的 L11 已经把两者分开了（最终卡由用户手动触发导出）。→ 解法清晰：人物文档和最终卡分开定序。

### 冲突

- **C10（L2）**：过滤掉一条素材要不要同步告知用户？线 A"必须同步"vs 线 B"静默"。
- **C11（L1）**：意象在人物文档里排第一（线 A）还是最后（线 B + 教程）？（D10 的位置冲突并入此条）

---

## D09 MVC —— 精度升级 + 可归并

### 三线各说什么

| 线 | 主张 | 锚 |
|---|---|---|
| **A** | MVC = **调色盘 + 二次解释**（意象已被移出）。告知方式：**直说三件事**（到哪了 / 能干嘛"可以先看看效果" / 下一步"也可以继续深化——多面性、混色、核心人格层。要继续吗？"）。明确否决"展示蓝图"和"自然衔接的花招"——"**不需要中间快照，不需要自然衔接的花招。直说。**" | `HANDOFF §2.21`、`DISCUSSION_01 §决策 1` |
| **B** | **三层判读**：用户主权 > 质量信号（≥3 个，含至少 1 个 D/E/F/G）> 结构兜底。**明确废弃了结构定义**（原 §4.2"用户能说出底色/主色调/点缀"），理由之一是"非调色盘路径（姜糖画面入口、三明月）结构性永远进不了 MVC"。行为测试修改版作可选增强层。G4 快速路径的 MVC 锚在情绪阈值后意象前 | `CBGD §4.3`、`§4.3.1`、`§4.3.2`、`§十三 G4` |
| **C** | **完全未覆盖。** 全部 10 份文档没有 MVC / 最小可运行角色 / 交付下限的概念 | 空白 |

### 关系判定

**精度升级。** 线 B 的第三层"结构兜底"实际上就包含了线 A 的结构定义——线 A 被线 B 吸收，不是被否决。

**但告知方式是两回事**：线 A 的"直说三件事"（明确否决**快照**和自然衔接的花招）是**线 B 没有的资产**；而线 B 的 MVC 告知内容既泄漏了三个术语（B/T20），又与自己的"是否激活进阶不暴露给用户选"矛盾（B/T13）。→ **可归并**：采纳线 B 的三层判读 + 线 A 的"直说"告知方式，告知语的术语问题挂在 C9 上。

⚠️ **蓝图是否展示在线 A 内部仍未决**（复核修正）——`DISCUSSION_01 §待确认` 明写"MVC 时是否需要展示'蓝图'作为状态告知的一部分？还是直接一句话告知即可"。这是线 A 的本线未决项，不是三线冲突。

### 教程裁定

教程无 MVC 概念，但**必须/条件的分界是明写的**（七步里第三/四/五步带条件标注，第一/二/六/七步不带，`07:245-278`）。这支持把"基础信息+调色盘+二次解释+意象"当作结构兜底——**注意教程的无条件项包含意象**，而线 A 明确把意象移出了 MVC。

### 冲突

- **C12（L1）**：MVC 的结构兜底要不要含意象？线 A 移出（理由：审查 A 指出秋明月就没有意象），教程字面上意象是无条件项。

---

## D10 意象 —— 一致 + 冲突（位置）

三线在"**总是尝试**"上**一致**：线 A §2.23"必须尝试，允许跳过"、线 C 决策 11"意象总是尝试，用户觉得对就留"、线 B 快速路径 G3 把意象列为五步末位（无条件）。教程字面上意象是无条件项。→ 进 `05`。

跳过条件：线 A"2-3 个方向都不贴可跳过"+"不贴的意象比没有意象更糟"；线 C"用户觉得不对就不要"；线 B 无。→ **可归并**（取线 A 的判据，它最具体）。

位置冲突并入 **C11**。

⚠️ **原判"三线都没覆盖怎么写意象"不成立**（复核修正，`07` 修正 5）。线 B `CBGD §十三 决策 G3` 五步流程表的意象行已给出：

> 追问动作："她存在的方式像什么"（不是外貌不是性格）/ 从角色本身长出来（截断外部套用："不要从'她代表什么'出发，问'她像什么'"）/ 承载主要矛盾 / **写短** / **不解释** / 放最后
> 完成标准：有具体象征物；承载核心矛盾；**篇幅短**；**无解释性语言**

与教程 `07:206-214`（要短、不解释自己、能承载关系动态、从核心矛盾和存在状态长出来、不要外面搜比喻）**逐条对应**。→ 直接采纳线 B 的 G3 意象行，**G-2 从缺口清单移除**。

⚠️ 提醒：`07:25` 宣布"你问自己三个问题"但全章只给出第一问，**"意象三问"没有教程依据**，融合时不要凭空补。

---

## D11 快速路径与卡住处理 —— 互补 + 可归并

### 三线各说什么

| 线 | 内容 |
|---|---|
| **A** | 8 条，**全部是卡住处理**：三层模式（镜子→脚手架→推演）、否决溯源四步、情景引导。**没有任何降级路径 / 轻量模式设计**——教程 11 这条明显的快速路径只出现在被降级的辅助材料里 |
| **B** | **两套并存**。① `E_SECTION_DESIGN.md 决策 6`：**降级链条 = 镜子→脚手架→推演→溯源→降级路径**，切换条件"最近 3 轮无新行为场景或关系细节"，被 `KNOT_BODY_VS_REFS.md` 列为 **body 必备**、"切换依据是用户的产出质量"。② `§十三 G1–G9`：快速路径 = 能力问题不是意愿问题、切换条件（连续卡住：≥2 切面 + ≥2 脚手架被否）、**五步流程直接来自教程 11**、MVC 锚点 + 可自选入口 + 双向切换、标签优先、**四级引导阶梯**（钻探链→回链+行为锚定→多选行为引导→假设场景）、示范三条硬约束、肯定+追问成对、情绪串防御性纠偏 |
| **C** | ⚠️ 复核修正：**线 C 在本域没有可参与碰撞的主张。** 原判"唯一系统性的卡住规则只存在于选读的长版文件"不成立——线 C 全库"长版/短版/选读"零命中，也没有那套规则；"卡住/卡壳/说不出"全库仅 3 处且都不是规则 |

### ⚠️ 关系判定（已重判）

原判"线 A 三层模式 vs 线 B 四级阶梯是同一件事的两种描述、需二选一"**不成立**（`07` 修正 2）。实际情况是：

**线 A 的三层模式 = 线 B 的 `E_SECTION 决策 6`。** 两者不仅同名（镜子/脚手架/推演），切换判据措辞也几乎逐字，且线 B 把它列为 body 必备。这是**同一条机制在两条线各存一份** → **归并，不是取舍**。

**线 B 内部的两套是不同层面的东西，需要分工**：

| 层面 | 谁 | 管什么 |
|---|---|---|
| 模式链（镜子→脚手架→推演→溯源→降级） | `E_SECTION 决策 6` = 线 A `§2.22` | **AI 扮演什么角色**（只反射 / 给试探方向 / 出候选） |
| 引导阶梯（四级，从最轻开始） | `§十三 G6` | **引导强度给到几分**（钻探链 / 回链 / 多选 / 假设场景） |

原对齐表（镜子≈一级+二级、脚手架≈三级、推演≈四级）是**错位的**——线 B 自己的"脚手架"另有所指。

线 B 的 G1–G9 在**降级路径**上完全填补了线 A 的空白 → 互补。

### 教程裁定

- 教程 11 是快速路径的**教程原型**，核心机制是"把要求分析换成要求描述"（`11.md:189-191`）。线 B 的 G3 五步 = 教程 11 六步去掉"理解 AI"（那一步是 B 类原理教学，去掉是对的）。
- **教程只支持单向递进**（`11.md:491` 升级出口指向调色盘），**没有写创作中途的反向退回**。线 B G4 的"双向切换"是设计外推，教程未禁止但也未支持。
- 核心人格层卡住时**教程的指引是"跳过"**（`05:57-62` 把"作者自己还没想明白核心矛盾的角色"列进不适合写的角色），不是追问到底。**三线都没有把这条落成规则。**
- 执行器的卡住规则是"抽象标签**追问一次**具体行为，再答不出记'暂未确定'"（`工作流:57-58`）——这是全套教程唯一一处对追问深度给数量约束的地方，**三线都没引用过**。

### 冲突

- **C13（L1，已重写）**：不是"二选一"，而是**线 B 内部两套并存需要分工**（模式链管角色、G6 阶梯管强度），加上两个待确认：双向切换是否保留（教程只支持单向递进），以及教程的追问量事实（`工作流:57` 是**必须追问一次的下限**，不是上限；`24问:23-28` 给 4 问）要怎么落地。

---

## D12 命名与 slug —— 冲突（L0）

| 线 | skill 名 | 状态 |
|---|---|---|
| **A** | `character-palette-collab` | **从未被论证过**，三份文档直接使用，无候选比较 |
| **B** | `char-build-guide` | 定稿。⚠️ 复核修正：**它本身没有变迁**——自 `verified.md` 首次出现起一路沿用。早期的 `skill-0N-` 序号前缀残留确实存在（`verified.md` 里 `skill-04` 还撞号两次），但**从未被登记为决策**，两张汇总表直接改用无前缀 slug，没有一句说明前缀为何消失 |
| **C** | `char-creator` / `char-builder/` / "角色塑造 skill" / `char-palette` | **四不一致，本线内未决**，无任何改名理由记录。（其中 `char-palette` 只是仓库目录名，文内从未作为 slug 提出） |

维度命名三线**一致**：都用"多面性"（线 A `HANDOFF §2.7`、线 B `DESIGN_DECISIONS.md 决策 5` + `ARCHITECTURE.md`、线 C 沿用"三面性（多面性）"）。教程依据在分散单篇 `2.md:21-31`（大总结丢了这一节）。→ 进 `05`。

**产物名**（情感混色 / 角色边界 等）挂在 C9 的拍板结果上：线 A 的双名并行需要产物名且本线未决（`DISCUSSION_01 议题 6`），线 B/C 不需要。

**description**：线 B 有 **3 个** 🔴 分歧（char-creator 措辞 / OOC 信号 / 排除项粒度），v1/v2 两条草稿都是 🟡 且原文明写"依赖 A2 决策 2/3/4 → 决定用 v1 还是 v2"。线 A/C 无。

### 冲突

- **C14（L0）**：最终 slug。线 B 的 `char-build-guide` 论证最完整，但线 C 的目录树用的是 `char-builder/`，线 A 用 `character-palette-collab`。

---

## 附：三线共同缺口（谁都没覆盖）

融合后仍然是洞，需要单独补：

| # | 缺口 | 教程有没有现成材料 |
|---|---|---|
| G-1 | **4 个基础诊断的外部问法与质量门的落位**——线 C 自认的架构洞（C/T-19），线 B 的 11 refs 里也没有 | 有，见 `01_教程事实底册 §四 D04/D05` |
| ~~G-2~~ | ~~怎么写意象~~ | ⚠️ **已移除**——线 B `§十三 G3` 意象行已给（写法约束 + 完成标准），与教程 `07:206-214` 逐条对应 |
| G-3 | **测试与迭代**——线 B 在 B 段诚实标注为缺口，线 A/C 未提 | 无 |
| G-4 | **已有角色卡的检测/体检**——只有线 C 提到作为未来 skill | 部分（判据可反向使用） |
| G-5 | **追问深度的数量约定**——⚠️ 已重写：教程执行器 `工作流:57` 是"你**必须**追问一次"的**下限**（原分析读成了上限）；`24问:23-28` 给 4 问、`80问教程:36/44-49` 给 4 问、`:1022-1028` 给 5 问。**教程没有给出上限**。三线都没引用过这批追问组 | 有，但只有下限 |
| G-6 | **核心人格层"作者答不出"时应当跳过这一层**——`05:57-62` 把"作者自己还没想明白核心矛盾的角色"列进不适合写的角色，三线都没落成规则 | 有（注意这是事前筛选，不是追问卡住后的处理） |
| G-7 | **2 个用户捞回动作的质量门**（NSFW 动机追问、世界观锚定追问）——线 C 的 13 动作里这 2 个没有质量门 | 部分 |
