# 过程日志（断点续跑用）

> 每完成一个 Phase 追加一条。中断后接手者先读本文件 + `00_README.md`。

---

## 基线信息

- 分支：`cursor/three-track-decision-collision-627d`
- 起点 commit：`935255f`（main tip，"更新 AGENTS.md 进度导航；落盘 char-build-guide 设计决策 L1-L12"）
- 本目录只新增文件，不改任何既有文件。

## 已确认的范围裁定

| 项 | 裁定 | 来源 |
|---|---|---|
| 线 A 的范围 | `DESIGN_HANDOFF.md`(694) + `DESIGN_HANDOFF_REVISION.md`(371) + `DESIGN_DISCUSSION_01.md`(375)，三件套 | 用户确认 |
| `.claude/skills/char-creator/` | **失败品，不看不引用** | 用户指示 |
| `char-creator-skill-path.md` / `char-creator-full-context.md` | 辅助材料，仅在时序压平出歧义时回查 | 主流程判断 |
| 上次失败残留 | 在 `origin/cursor/merge-two-decision-sets-9d6f` 与 `origin/cursor/merge-three-decision-tracks-1e3a` 两条分支上，不在工作树；**禁止读取** | 用户指示 |

---

## Phase 0 — 基线 ✅

- 建 `三线碰撞/` 目录
- 写 `00_README.md`：任务定义、三线定义、禁读清单、决策原子 schema、12 议题域、六档关系、L0-L3 冲突分级、拍板单格式、A/B 类教程记录规范

**关键发现（已影响计划）**：线 A 不是单份文档而是三件套自我修订链。只读 handoff 主文件必然产出已被自我推翻的断言。

## Phase 1 — 教程事实底册 ✅

6 个 agent 分组通读全部教程（11 篇 + 大总结 7 章 + 工作流 4 份），产出 **586 条**带 `文件:行号` 锚的事实，按 A 类（追问动作，可入 skill）/ B 类（教学手段，不入默认行为）标注。

- 逐条底册在 `_work/01_教程底册_组{1a,1b,2,3a,3b,4}.md`
- 汇总层（裁定用）在 `01_教程事实底册.md`

**分组教训**：单个 agent 一次读 ~100KB 是上限。组 1（106KB）超时、组 3（123KB）资源耗尽，拆小后全部成功。

## Phase 2 — 三线原子化盘点 ✅

4 个 agent 对称盘点（线 B 因主文档 1270 行拆成两段），产出 **636 条**决策原子 + **83 条**各线内部张力。

| 线 | 原子数 | 内部张力 | 文件 |
|---|---|---|---|
| A | 158 | 21 | `_work/02A_线A_决策原子清单.md` |
| B 前半 | 197 | 26 | `_work/02B1_线B_决策原子清单_前半.md` |
| B 后半 | 110 | 15 | `_work/02B2_线B_决策原子清单_后半.md` |
| C | 171 | 21 | `_work/02C_线C_决策原子清单.md` |

**重大发现**：线 A 的时序**不是**单向链。`DESIGN_HANDOFF.md` 被反向回填过（13 处 `2026-08-07` 标注），REVISION 有若干条被 handoff 反向推翻。只按标称时序压平会把结论搞反。

**⚠️ 本阶段的方法缺陷（已在 Phase 5 暴露）**：线 B 的盘点只覆盖了 `CHAR_BUILD_GUIDE_DESIGN.md` 主文档，把 `design/` 下 6 份审查报告当作"按需回查"。这导致对线 B 做了 3 条不成立的全称否定，进而误列了 3 条冲突。**教训：盘点某条线时，该线的全部文档都要覆盖，不能预设哪些是"历史输入"。**

## Phase 3 — 议题碰撞矩阵 ✅

主流程自己做，不外包。12 个议题域 × 三线，六档关系判定。产出 `03_议题碰撞矩阵.md`。

## Phase 4 — 冲突拍板单 + 可融合清单 ✅

产出 `04_冲突拍板单.md`（初版 14 条冲突）和 `05_可直接融合清单.md`（48 条可直接融合项）。

## Phase 5 — 独立证伪复核 ✅

2 个 agent 只做核查不做设计判断，逐条回原文核实 `01`/`03`/`04`/`05` 的每一条带锚断言。

- 教程侧：核查 201 处，179 ✅ / 4 ⚠️ / 6 ❌ / 12 🔴
- 三线侧：核查 173 处，117 ✅ / 22 ⚠️ / 26 ❌ / 8 🔴（线 A 侧 64 条 **0 个 ❌**）

**这一轮拦下了 5 条会直接导致拍板返工的错误**（详见 `07_证伪复核报告.md`）：

1. C8 心理学框架被误列为冲突——线 B `KNOT_BODY_VS_REFS.md` 也有，落位方案与线 A 逐条重合
2. C13 被误列为"二选一"——线 B `E_SECTION_DESIGN.md 决策 6` 就是那条三层模式链
3. C4 被误列为开放冲突——`CBGD §12.8 L4` 的依据原文已自答
4. **C9 的教程证据方向摆反**——原判"教程未涉及"，实际教程执行器满屏术语且 AI 主动定义
5. G-2 被误列为缺口——线 B `§十三 G3` 已给意象写法约束

修正结果：**14 条冲突 → 11 条真冲突 + 3 条确认项**；7 条共同缺口 → 6 条（另新增 G-7）。全部修正已回写。

## Phase 6 — 融合形态草案 ✅

产出 `06_融合后形态草案.md`，每个待定处标注挂在哪个冲突上。**明确标注为草案，拍板前不具权威性。**

## Phase 7 — 交付 ✅

提交 + PR（[#5](https://github.com/dwf89044485/sillytavern-ops/pull/5)）。

## Phase 8 — C08 专题查证 ✅

用户指出：**C8 不能因为"两条线都提到"就当作合理**——这套心理学框架方法最初是在没读教程的情况下自行延展出来的构想，未经实战验证。用户的原始思路是："用户擅长想象行为，但说不出行为背后的原因，这一层需要专业知识，所以由 AI 用框架帮用户反推；有了深层原因，AI 演角色会更贴合。"

派 5 个 agent 独立覆盖全部教程查证（`_work/Q1`～`Q5`），产出 `08_专题_行为背后的原因.md`。

**核心发现**：

| 构想的组成 | 教程 |
|---|---|
| 用户说不出原因是真实现象 | ✅ 同意（专门写了核心人格层 + 教程 11 整条替代路径） |
| 从已有行为反推 | ✅ 同意，"反推"是教程原词 |
| 靠心理学框架反推 | ❌ **零命中 + 明文禁令**（`7.md:299`） |
| 产出"什么经历/心理导致他这样" | ❌ **逐字列为错误示范**（`7.md:472`/`:92`/`11.md:212-220`） |
| 说不出就必须由 AI 解决 | ❌ 教程列为"不适合写这一层"的**排除条件**（`05:62`） |
| 有深层原因 AI 演得更真实 | ⚠️ 收益被换成"跨情境不演错"；核心人格层在活人感归因清单里排**第 7** |

最关键的一条反证：`01:213` 柚子说的正是这个构想的假设（"这样AI就知道为什么她变成这样了"），**晴空当场否掉**：「写多了解释，AI 反而会偷懒——它不再自己理解角色，只是照搬你的结论。」

## Phase 9 — 第一条拍板 ✅

**用户拍板：C08 按教程来。** 建 `09_拍板记录.md`，按项目自己的决策记录规范（线 B `§〇` 五问 + 三条铁律）落决策 **D-01**。已回改 `03`/`04`/`05`/`06`/`08`/`00`。

**D-01 确立的一条判定原则，后续拍板沿用**：**"多条线都提到"本身不构成证据**，必须先确认这些线是否独立收敛。

---

## 给接手者的四条提醒

1. **不要读那两条残留分支**（见 §基线信息），也不要读 `.claude/skills/char-creator/`
2. **先读 `09_拍板记录.md`** —— 那里是已经定死的结论，不要再当开放问题讨论
3. **拍板从 `04` 的依赖图第 0 批开始**（2 个确认项），再拍第 1 批 C9——它是剩下的根节点
4. **拍 C9 之前务必先看它的教程证据**，那一条经复核后方向反转了（教程唯一写给 AI 执行的规范满屏术语且由 AI 主动定义）
