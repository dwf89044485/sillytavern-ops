# FIXLOG_02 · `SKILL交接包/02_SKILL.md施工规格.md` 修复执行记录

> **台账基线**：`SKILL交接包/12_修复台账.md`（132 条 / 77 TODO）
> **本轮范围**：台账里"改哪"指向 `02_SKILL.md施工规格.md` 的全部 TODO 条目（P1/P2/P3；P0 的 T-005 / T-007 已是 DONE，不重做）
> **只改了一个文件**：`SKILL交接包/02_SKILL.md施工规格.md`（585 → 585 行，从 224 行扩到 585 行）。台账、`01`/`03`/`04`/`05`/`06`/`10`/`素材` 一律没动。
> **验收命令一律从 `/workspace` 原样跑**，跨文件的命令若命中别人正在改的文件，结果在"跑出来是什么"栏里逐个文件列出。

## 统计

| 状态 | 条数 |
|---|---:|
| **DONE** | 40 |
| **SKIPPED-待拍板** | 1（T-118） |
| **MOOT** | 1（T-021，`02` 侧本来就不需要动） |
| **BLOCKED** | 0 |
| **合计** | **42** |

> 另有 3 条是**整体 DONE、内部一小句挂起**：T-116（中途切换条件 ⏳ D-e）· T-127（写作原则 ⑤ ⏳ D-e）· T-128（description 第 2 条 ⏳ C14）。三处都在 `02` 里写成了显式"⏳ 待拍板"登记块，不是留白。

## 范围认定

台账里"改哪"提到 `02` 的 TODO 共 42 条：**P1 6 条**（T-010/017/021/022/026/033）· **P2 31 条**（T-100~T-155 段 + T-122）· **P3 5 条**（T-306/308/317/329/334）。

两条筛查修正，记在这里免得下一个人重筛：

- **T-151 是误命中**，不在范围内。它的"改哪"里出现的 `02` 来自 `D-02 占用` 这个决策编号，落点其实是 `03`/`06`。
- **T-122 差点漏掉**。它的"改哪"写的是「`03_references施工规格.md` …，或 `02` §11 流程地图」，用 ``` `02_SKILL` ``` 检索不到，要用 ``` `02` ``` 才命中。已补做。

---

## 逐条记录

| T 编号 | 状态 | 改了什么（具体到哪一节、加了什么删了什么） | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|
| T-010 | DONE | §4 硬铁律**新增 item 6**「追问默认不暗示方向」（默认镜子模式；"给试探方向让她否定"只在脚手架模式下允许，判据指 `04` H-31）与 **item 7**「不给平铺候选清单」（可计数判据：单轮并列待选方向 ≥3 → 违反；唯一例外"摆素材反推"）。§9 禁止写表**新增 N4**（≥3 条并列待选一次抛给用户，指 `04` H-32） | `grep -A3 -B3 "试探方向" SKILL交接包/04_硬约束与红线.md \| grep -c "脚手架"` · `grep -c "H-31\\|H-32" SKILL交接包/04_硬约束与红线.md` · `grep -n "最近几轮" SKILL交接包/04_硬约束与红线.md` | `8`（应 ≥1）· `6`（应 ≥2）· 0 命中（应 0 命中） | ✅ |
| T-017 | DONE | §8 参考原句 block 的**标题整段重写**：原来笼统标"参考原句"，现在逐句分级——第 1/2/3/5 句标"教程逐字，可直接用"，**第 4 句「遮住名字……」显式标为线 B `§5.1.2` 的问句化转写**，并写明教程 `01_基础信息篇:139` 原文是陈述句、只针对外貌描写。item 1 的形态要求同步加「标"逐字"的必须与素材逐字一致，标"转写"的必须注明来源」 | `grep -A10 "参考原句" SKILL交接包/02_SKILL.md施工规格.md \| grep -c "转写\\|线 B"` · `grep -n "逐字原句.*不是转写" SKILL交接包/06_验收.md` | `2`（应 ≥1）· 0 命中（应 0 命中） | ✅ |
| T-021 | MOOT-`02` 侧无动作 | 台账给 `02` 的动作是「H-11 的姿态原则那半**移到** `02` §1」，但台账自己的"问题"栏就写了"它**已在** `02` §1 以规格形式存在"——§1 item 1 已声明"我是追问引擎…… 默认动作是提问"。真正要做的是**从 `04` 移除**，那是 `04` 负责人的活。`02` 未改一字 | `grep -c "^### S-" SKILL交接包/04_硬约束与红线.md` · `grep -A3 "H-04" SKILL交接包/04_硬约束与红线.md \| grep -c "摆素材"` · `grep -c "H-19a\\|H-19b\\|H-19c" SKILL交接包/04_硬约束与红线.md` | `0`（应降到 0 或 1）· `2`（应 ≥1）· `6`（台账写"应为 3"，见文末台账问题 ③） | ✅（`04` 侧已由他人改完） |
| T-022 | DONE | §4 **来源行删掉**"取线 B `§1.4` 表格版"，改成「边界表是**三线归并版**（线 B `§1.4` 第 4 行 + `§2.5` 凝练命名 + 线 A `§2.22` 试探方向 + D-01），不是照抄线 B `§1.4`」。§9 item 1 的形态要求**加一句反向提醒**「⚠️ 这四行是三线归并版，不是线 B `§1.4` 原表——照 `04` H-12 写，不要回去抄 `§1.4`」 | ``grep -c "线 B \`§1.4\` 表格版为准" SKILL交接包/04_硬约束与红线.md`` · `grep -A1 "H-12" SKILL交接包/04_硬约束与红线.md \| grep -c "三线归并版"` · ``grep -c "取线 B \`§1.4\` 表格版" SKILL交接包/02_SKILL.md施工规格.md`` | `0`（应 0）· `1`（应 ≥1）· `0`（应 0） | ✅ |
| T-026 | DONE | **新增 §10a「矛盾检测触发之后的分流规则」**，并显式标注"不受 C3/C6 阻塞"。内容：item 1「发现矛盾时**先追问画面，再判类型**」+ item 2 的**四列决策表**（矛盾类型 → 标记为 → 当前动作 → 处理时机）：跨场景矛盾 → 多面性线索 / 继续当前方向不打断 / 收束时统一激活；同瞬间矛盾 → 混色线索 / **当场**追问"那个瞬间她具体做了什么？" / 给不出就记线索。加了兜底句：若 §10 整段不落地，这张表挂 §11 或 `10` §六之二，**不许连表一起丢** | `grep -c "跨场景矛盾\\|同瞬间矛盾" SKILL交接包/02_SKILL.md施工规格.md` | `2`（应 ≥2） | ✅ |
| T-033 | DONE（`02` 侧） | §8 **新增「⏳ 缺口登记」块**：写明深度轴第五层「绝对不变的内核」在 `素材/追问原句库.md` 检索零命中、五句参考原句里也没有对应句，而 `06` S8-1 要求每层 1-2 句逐字——处理指令是"body 第五层留 ⏳ 占位，**不要用转写句顶替**"。item 1 的素材栏加了 `⚠️ 见 T-033` 指针 | `grep -c "绝对不变的内核" SKILL交接包/素材/追问原句库.md` · `grep -c "教程未提供" SKILL交接包/素材/追问原句库.md` · `grep -A12 "参考原句" SKILL交接包/02_SKILL.md施工规格.md \| grep -c "内核"` | `0` · `0` · `1`（应 ≥1） | ⚠️ 前两条查的是 `素材/追问原句库.md`（不是我的文件），仍 0；`02` 侧 ✅ |
| T-100 | DONE | §7 入口意识**在五种入口之前插入两个必走前置步骤**：item 1「第 1 步（必走）：世界设定」带可直接说的问句（"想写什么题材？现代都市？古风？仙侠？末世？校园？"，世界基调按需追加）；item 2「第 2 步（必走）：用户角色定位」带问句（"你在这个世界里是什么身份？你和角色的关系是什么？还是说你是旁观者？"）；item 3 写清后果「**缺任一，角色就没有坐标系**」。原 item 1-4 顺延为 4-7 | `grep -c "世界设定" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "用户角色定位" …` · 行号必须小于"五种入口" | `2`（应 ≥1）· `1`（应 ≥1）· 世界设定 L274 / 用户角色定位 L275 **<** 五种入口 L277 | ✅ |
| T-103 | DONE | §5 姿态校准**新增 item 8**「不主动问'她是什么性格'」，配可机械检查的验收（首轮问句检索"什么性格 / 性格是什么"应零命中）；参考对照 block **加第三组对照句**`✅ 她昨天做的哪件事最像她？` / `❌ 她是什么性格的人？` | `grep -c "什么性格" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "什么性格" SKILL交接包/04_硬约束与红线.md` | `2`（应 ≥1）· `4`（应 ≥1，`04` 侧由他人改） | ✅ |
| T-104 | DONE | §4 硬铁律**新增 item 5**「**用户裁决不可上诉**」：你推演、用户裁决，裁决不争辩、换个方向再试，用户说"就是这个感觉"就是对的、不需要理由。配了可判判据——"用户否决后的**下一轮**输出里有没有辩护性表述（'我这样推是因为……'）"，有就是违反 | `grep -c "不可上诉\\|裁决不争辩" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/04_硬约束与红线.md` | `02`:`1` · `04`:`3`（至少一处 ≥1） | ✅ |
| T-105 | DONE | §9 推演权限**新增 item 6「进入闸门（作者无解判据）」**：满足**任一**才允许从追问切到推演——① 用户直接说"不知道 / 没想过" ② 答案模糊到无法落成一句行为描述 ③ 用户给的解释与已有素材矛盾。形态要求写死"条件清单，三条并列，显式写'满足任一即可'"。同时在段首加了**写作次序**说明：闸门（item 6/7）写在推演动作（item 1-5）**前面**，"闸门写在动作后面等于没有闸门" | `grep -c "不知道 / 没想过\\|没想过" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "进入条件\\|进入闸门" …` | `2`（应 ≥1）· `3`（应 ≥1） | ✅ |
| T-106 | DONE | §9 **新增 item 7「输入闸门（素材够不够推）」**，拆三块并附"内容已定，照抄"清单：① **软判断最低线**（素材区 ≥1 条非标签化记录 **AND** 目标槽位有素材支撑，不设硬数字）② **推演前 AI 内部走五步**（列已知事实 → 确认已观察特征 → 指出用户没说出口的模式 → 不确定留空标出 → 一句话概括理解，**不输出给用户**）③ **硬判据**「三句话说不清楚 = 素材不够，回去追问，不硬推」 | `grep -c "三句话说不清楚" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "非标签化记录" …` | `2`（应 ≥1）· `2`（应 ≥1） | ✅ |
| T-107 | DONE | §9 **新增 item 8「推演后的差异性检查」**：产出 2-3 个候选后**遮住框架名和术语只看行为**，判断这几条是不同的事还是同一件事换说法；**是同一件事 = 不合格，重推**。§9 自检行同步登记 `06` 要加 `P11 有候选差异性检查` | `grep -c "差异性检查" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/06_验收.md` | `02`:`2` · `06`:`1`（两处均应 ≥1） | ✅ |
| T-108 | DONE（`02` 侧） | **新增 §12a「溯源这一环的操作定义」**（链上唯一没有操作定义的环）：item 1 的**三行判定表**（"不是 X，是 Y" → 用户在自修正 → **直接改不追问**；"我也不知道，没想过" → 素材不够 → 降级或跳过这一层；"都不对，感觉不对" → 理解偏差 → **走溯源**）；item 2 溯源动作逐字问句「**哪一句不对？一个词就行。**」+ 答不上来时把 AI 的理解与用户反应之间的差摆出来；item 3「**禁止跳过溯源直接暴露理解**」 | `grep -c "哪一句不对" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/10_新会话接手指南.md` · `grep -c "A-09\\|溯源操作" SKILL交接包/10_新会话接手指南.md` | `02`:`1` / `10`:`0`（至少一处 ≥1 → 过）· `0`（应 ≥1，**未过**） | ⚠️ 第 2 条查的是 `10_新会话接手指南.md`，不是我的文件 |
| T-109 | DONE | §9 **新增 item 9「批次呈现节奏」**：不要每推出一条就单独抛给用户，**攒够一个维度**再整批呈现；但整批呈现后每条候选仍要用户**逐条**判。验收写死"两半都在，只写前半 → 不通过"。§5 item 7 处加了指针，说明两处是同一条原则、body 里不要各写一遍论证 | `grep -c "攒够一个维度\\|整批呈现" SKILL交接包/02_SKILL.md施工规格.md` | `1`（应 ≥1） | ✅ |
| T-112 | DONE | §9 item 3 的标注要求**从单标注改成双标注并列**：原来只要求"从哪段经历推出来"，现在写成 **① 来源：从哪段经历推出来的** + **② 去向：这条将落进哪个槽位**（如"→ 核心人格层 · 防御机制"），形态要求注明"两个标注**并列**，不是二选一"，验收"缺一即不通过"。保留了 D-01 的"不要标'心理机制'" | `grep -c "槽位标注\\|→ 核心人格层 · 防御机制" SKILL交接包/02_SKILL.md施工规格.md` · `grep -A2 "从哪段经历" … \| grep -c "槽位"` | `2`（应 ≥1）· `1`（应 ≥1，确认并列非替换） | ✅ |
| T-115 | DONE | 按台账给的第二选项**并入 §5**（不另开"交互原则"段，省一段抬头）：**新增 item 7「一次一个交互单元」**——每轮只给一个（一个问题 / 一个方向试探 / 一组推演候选），**不叠加**；配可计数判据"一轮里问号 ≥2 且分属不同话题 → 违反" | `grep -c "一个交互单元" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "问句数 ≤1\\|问句数量" SKILL交接包/06_验收.md` | `1`（应 ≥1）· `1`（应 ≥1，`06` 侧由他人改） | ✅ |
| T-116 | DONE（切换条件除外） | 新建的 §13 里落了三条运行时路由：**item 2 + 13b 与 `dialogue-persona` 的四维路由表**（角色表达媒介 / 用户擅长方式 / 需要的深度层 / 角色复杂度，每维两侧都给取值）· **item 3 与 `worldbook-config`**「**把所有条目改蓝灯 → 问题消失 = 配置问题**，走 `worldbook-config`；问题还在 = 内容问题，留本 skill」· **item 4 与 `user-info-calibration`**「描述对象是 **AI 演的角色**，还是 **AI 读的用户**」。**中途切换条件未写**，13b 下留了 ⏳ D-e 登记块 | `grep -c "所有条目改蓝灯" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "演.*的角色.*读.*的用户\\|AI 演的角色" …` | `1`（应 ≥1）· `2`（应 ≥1） | ✅ |
| T-117 | DONE | §4 **新增 item 8**「被要求'帮我生成 / 帮我扩写'时：**既不推给别的 skill、也不照做，当场把请求转成追问**」，并配一句可直接用的**转化措辞** block：「我可以帮你把她写出来，但先得知道她是谁——你脑子里关于她最清楚的一个画面是什么？」验收写死"只写'不照做'没配转化措辞 → 不通过（AI 会只剩拒绝）"。§13 item 5 只放**指针**指回 §4，措辞全包只出现一次 | `grep -c "帮我生成" SKILL交接包/02_SKILL.md施工规格.md` · `grep -A3 "帮我生成" … \| grep -c "转成追问\\|拦截"` | `3`（应 ≥1）· `2`（应 ≥1） | ✅ |
| T-118 | **SKIPPED-待拍板** | **未落地**。台账 §七 **D-c** 明写这条要用户拍板（五种入口分类 vs 素材类型识别 vs 并存），改了会白改。只在 §7 加了一个 **⏳ 待拍板登记块**，把三个选项和"拍板前不要动 item 4/5 的形态"写下来，避免拍完之后没人记得改 | `grep -c "素材类型" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "信息密度最高" …` | `1` · `1`（都 ≥1，**但这是登记块里的措辞，不是落地**，见文末台账问题 ②） | ⚠️ 命令过了，实质没做 |
| T-119 | DONE | **新增 §12b「卡住的根因诊断」**，并标明它与模式链**正交**（模式链管"AI 换什么角色"，根因诊断管"用户卡在哪种认知阶段"，同时生效不是二选一）。四行表：物化阶段（有画面说不出 → 场景引导）· 展开阶段（一个画面扩不开 → 换切面，顺序指 §6）· **收敛阶段**（素材够了看不出共性 → **回看整理，不是继续追问**）· 穿透阶段（说不清动机 → 追问 1-2 次仍说不出就**正常降级，不算失败**）。形态要求里点名"收敛阶段 → 不是继续追问"这句负向约束不能省 | `grep -c "收敛阶段" SKILL交接包/02_SKILL.md施工规格.md` · `grep -A2 "收敛阶段" … \| grep -c "不是继续追问"` | `2`（应 ≥1）· `2`（应 ≥1） | ✅ |
| T-120 | DONE | §6 判停标准**从"遮名测试单条"改建为三层框架**：新增 item 1「**三层判停框架**」+ 三行表（**步骤级** 判整体方向对不对 → 方向错回步骤 1 / 用户喊停跳收束；**轮次级** 判这条方向够不够 → 够换方向 / 枯竭换切面 / 卡住升级模式；**里程碑级** 判此时该不该问用户 → 达到 MVC 暂停展示）；新增 item 2「AI 先走轮次级，再依次检查更高级别」。**遮名测试降为 item 3 并挂进轮次级**，"用法：判这一层够深了还是继续追"那半句原样保留 | `grep -c "步骤级" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "里程碑级" …` | `2`（应 ≥1）· `3`（应 ≥1） | ✅ |
| T-121 | DONE | §6 **新增 item 4「单条追问的判停 AND 条件」**（三条同时满足：① 用户给出画面/行为描述 **AND** ② 这个画面能回答"她在什么场景下做了什么" **AND** ③ 和已有素材不重复），形态要求写死"显式 AND 不是 OR"；**新增 item 5「方向枯竭后换切面的顺序」**`正面 → 反面 → 侧面 → 关系面`，并写"不在同一枯竭方向继续挖" | `grep -c "正面 → 反面 → 侧面 → 关系面" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "AND" …` | `1`（应 ≥1）· `8`（应 ≥2） | ✅ |
| T-122 | DONE | 按台账的第二落点（`02`）**新增 §12c「方向变更处理」**：四步（① 确认旧素材是否保留，带逐字问句"你之前说的跟踪、偷看手机——她还是会做这些事吗？" ② 清除旧方向的**构建区**内容 ③ 保留用户确认仍属角色的素材，**素材是事实不动** ④ 回到步骤 1 重新初始化）+ 重点负向约束「**方向变更不适用脚手架 / 推演降级——它不是卡壳**」。另加与 `03` 的分工说明，防两处各写一遍第 2/3 步 | `grep -c "方向变更" SKILL交接包/03_references施工规格.md SKILL交接包/02_SKILL.md施工规格.md` · `grep -A5 "方向变更" … \| grep -c "不是卡壳"` | `03`:`1` / `02`:`2`（至少一处 ≥1）· `2`（应 ≥1） | ✅ |
| T-123 | DONE | 通用规格**新增「不可变约束」小节**（与"分界测试"并列）：五条判据表（每步有可判断的完成标准 / 判停点显式写出 / 每步是**动作**不是哲学，句子以动词开头 / 步骤骨架必须存在 / 步骤必须编号），每条配"怎么判"一列；再加**完成标准的锚定通则**——锚定**文档产物（AI 从文本可观察）**、**不锚定用户心理**，`✅ 素材区有 ≥1 条非标签化记录` / `❌ 用户觉得够了`，并区分"用户明说够了"是判停信号而非完成标准 | `grep -c "不锚定用户心理\\|AI 从文本可观察" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "不可变约束" …` | `1`（应 ≥1）· `1`（应 ≥1） | ✅ |
| T-124 | DONE | §13 **item 6 + 13c「覆盖盲区七项」七行表**，每项配一句**可直接对用户说的标注措辞**：同人角色构建 / 多角色卡与多角色交互 / 角色卡测试与迭代 / 群聊场景 / NSFW 深度内容 / 跨平台格式迁移 / token 预算的系统性管理。禁止写里加了"盲区的**原因分析**不写"（写标注措辞，不写"为什么教程没覆盖"） | `grep -c "同人" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "群聊" …` · `grep -A15 "盲区" … \| grep -c "^\|"` | `1`（应 ≥1）· `1`（应 ≥1）· `17`（应 ≥7） | ✅ |
| T-125 | DONE | §13 **item 7 + 13d「优雅退出路径」**（标为"追问引擎唯一的逃生门"）：**适用范围的承认**（对"心里有画面但说不出"的用户最有效，对"连角色概念都没有"的效果有限）+ **退出触发**（连续 N 轮无有效产出 → 不再继续追问，并写明与 H-19"方向枯竭换切面"的区别：那是**局部**退出，这是**整体**退出）+ **三个出口**（收束已有内容 / 切 24 问急救模式或快速路径 / 诚实告知局限，带原话"可能需要先积累一点对她的感受，我们改天再来"），验收"必须三个都写，只写一个等于没有出口" | `grep -c "无有效产出\\|N 轮无产出" SKILL交接包/04_硬约束与红线.md` · `grep -c "24 问急救\\|急救模式" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/04_硬约束与红线.md` | `3`（应 ≥1，`04` 侧由他人改）· `02`:`1` / `04`:`1`（至少一处 ≥1） | ✅ |
| T-126 | DONE | §13 **item 1 + 13a「四条不触发判据」**，写成条件-动作四行表：只需要写用户信息 → `user-info-calibration`；只需要配置世界书 → `worldbook-config`；明确想用台词/小传/采访 → `dialogue-persona`；**已有完整角色卡只想微调或格式检查 → 走校验模式，不启动完整引导流程**。第 4 条下加了说明：`§7` 的"带现成卡"是**要继续建角色**的路径，只想做格式检查的用户不该被拖进完整引导。禁止写里加"任何排除句都必须带路由" | `grep -c "校验模式" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "不触发判据\\|什么时候不该用" …` | `2`（应 ≥1）· `3`（应 ≥1） | ✅ |
| T-127 | DONE（⑤ 除外） | 通用规格「读者与语气」**从 3 条扩到 6 条**，补的三条全是 B 段写作原则：**body 是路由器和防误触机制，不是教程摘要**（AI 读 body 是为了判"该不该触发 / 触发了不该做什么 / 这一轮走哪条分支"）· **边界写判断标准不写模糊描述**（❌"看情况" ✅"描述对象是 AI **演**的角色还是 AI **读**的用户"，并附自问"AI 拿这句话能不能当场判"）· **翻译层矛盾显式处理不回避**（教程说"手写优于 AI 扩写"而本 skill 是 AI 驱动，要标出矛盾并给处理方式）· **覆盖盲区诚实标注**（指向 §13）。**原则 ⑤（中途切换）未写**，见 T-116 的 D-e 登记 | `grep -c "致命级" SKILL交接包/01_产物清单.md SKILL交接包/04_硬约束与红线.md` · `grep -c "严重级\\|中等级" SKILL交接包/04_硬约束与红线.md` | `01`:`1` / `04`:`6`（至少一处 ≥1）· `7`（应 ≥1） | ✅（但命令查的是 `01`/`04`，见文末台账问题 ④） |
| T-128 | DONE（第 2 条挂 C14） | **新建 §0 metadata 段**（段落清单同步加第 0 行）。七条内容规格表：① 覆盖五类触发场景的**用户原句措辞**（"角色很平面""标签写了一堆但演出来拧巴""只有一个画面拼不出人"）② 避开与 `char-creator` 撞车的措辞（**⏳ C14，本条唯一挂起项**）③ 收窄"帮我看看"这类万能句（必须带限定语，裸句不收）④ 不让"角色"成为唯一锚点（必须搭配状态词）⑤ 与三个兄弟 skill 的 `NOT for:` 边界信号 ⑥ **中英双写** ⑦ 内部术语零命中（调色盘/多面性/混色/核心人格层/二次解释/意象/底色/主色调/点缀色/衍生）。另加**四个不该触发的灰区** + **四个常见误触发热点**。与 `01` 的分工写清：frontmatter 字段格式在 `01`，本节只管 description 内容 | `grep -c "metadata" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "中英双写" …` · `grep -c "metadata" SKILL交接包/06_验收.md` | `3`（应 ≥1）· `2`（应 ≥1）· `2`（应 ≥1，`06` 侧由他人改） | ✅ |
| T-130 | DONE | §1 **新增 item 3**「质量取向：**活人 > 正确**——角色像活人比逻辑自洽更重要，**不要为了自洽把角色规整掉**」，形态要求点名必须带"不要为了自洽"这个负向半句，只写正向不通过。§5 **新增 item 6「只整理不总结」**：收束时复述用户说过的**具体场景和行为**，用"你刚才说的这些里有……"；**禁止**"所以她是……型的人"这种归纳句，验收可机械检查（产物里检索"所以她是"应零命中） | `grep -c "所以她是" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/06_验收.md` · `grep -c "活人" SKILL交接包/02_SKILL.md施工规格.md` | `02`:`1` / `06`:`1`（两处均应 ≥1）· `1`（应 ≥1） | ✅ |
| T-131 | DONE | **新增 §10b「完整性检查的维度清单」**（标"不受 C3/C6 阻塞"，并写明它是 AI 判断"**还该问什么**"的唯一维度清单，缺了它 AI 只会越挖越深、广度没了）：item 1 **六组核心维度**（角色核心 / 性格质地 / 来源 / 内在驱动 / 关系 / **AI 可执行性**，验收点名"AI 可执行性那一组不能漏"）· item 2 **不点名纪律**（不对用户说"你漏了 X 维度"，用自然追问引到空白区）· item 3 **三句逐字补白问法**（缺来源 / 缺关系 / 缺内在驱动各一句）。禁止写：不要把它写成一个独立的追问阶段 | `grep -c "AI 可执行性" SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/03_references施工规格.md` · `grep -c "你漏了" SKILL交接包/02_SKILL.md施工规格.md` | `02`:`2` / `03`:`3`（至少一处 ≥1）· `1`（应 ≥1） | ✅ |
| T-132 | DONE | §8 **新增 item 2「三句深度探测问句」**（AI 主动去探信号用，不是等信号出现），逐字 block 三句：**探多面性**「她在家里和在外面，状态差别大吗？是"语气柔和了一点"的那种差别，还是"像换了一个人"？」· **探混色**「她拒绝别人帮助的那个瞬间，她心里只有一种感觉在跑，还是好几种挤在一起？」· **探核心人格层**「同样的处境……她为什么偏偏选了自己扛？」。禁止写：不要把这三句写回"深度就绪检查"阶段（线 C 废掉的正是那个阶段）。篇幅预算同步从 ~15 行改成"含探测问句 ~20 行" | `grep -c "语气柔和了一点" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "好几种挤在一起" …` · `grep -c "偏偏选了自己扛" …` | `1` · `1` · `1`（均应 ≥1） | ✅ |
| T-140 | DONE | 段落清单表**加第 13 行**「边界与路由 \| ✅ 可写（中途切换那一条 ⏳ D-e）\| 触发时判一次、每次用户跑题再判一次；判错就接错任务」+ **新建 §13「边界与路由」整节**（容器段，装 T-116/117/124/125/126 五条）。段首写清它与 `01` §五 的区别：`01` 那张表是防**写作者** scope creep 的，本段是 **AI 的运行时判断规则**，"两者内容会重叠，但读者和用途不同，不能用一个替掉另一个"；本段每条都要能在**用户第一句话**上当场判。行数预估 ~30 行同步进清单 | `grep -c "^\| 13 \|" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "边界与路由" …` | `1`（应为 1）· `2`（应 ≥2：清单行 + 节标题） | ✅ |
| T-141 | DONE | §7 **item 5 从"每种入口第一动作"这一句扩成一张五行表**并写死"五种各一句，一句都不能省 / 缺一即不通过"：画面 → 围绕画面补上下文（"那个画面里还有其他人在吗？……她的手在做什么？"）· 标签 → 具象化（"她是做了什么，让你觉得她（标签）？"）· 背景 → 追事件细节 + 追变化 · 空白 → 走 item 7 场景钩子 · 现成卡 → 走 item 6 解析映射。素材栏顺手修正为 `素材/追问原句库.md` **§A1（起手回合）** | `grep -c "S7-4" SKILL交接包/06_验收.md` · `grep -A6 "^### 7. 入口意识" SKILL交接包/06_验收.md \| grep -c "^\| S7"` | `1`（应 ≥1）· **`3`（应 ≥4，未过）** | ❌ 第 2 条**命令本身写错了**，见文末台账问题 ① |
| T-148 | DONE | 三处。① 通用规格**新增「body 与 `04` 的关系」小节**：body 只承载**每轮都可能犯**的那几条，其余红线用 `见 04 H-xx` **指针**引用；并写死「**`04` 是写作者的检查表，不是 body 的内容源**」（抄 23 条进 body 会同时违反"原则驱动"和行数预算）。② 段落清单抬头改成「**本编号是清单编号，不是最终顺序**……写作时按编号找规格，不要按编号排 body 的段落次序」。③ §4 段首加"**这一段是两块，不是一块**"——item 1 的边界表是"内容从哪来"的总分界（四行，管所有场景），item 2 以下的铁律是高频场景的具体化，两块并列呈现，不要互相塞、互相替 | `grep -c "不是 body 的内容源\\|检查表，不是" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "清单编号" …` · `grep -c "S4-1a\\|S4-1b" SKILL交接包/06_验收.md` | `1`（应 ≥1）· `1`（应 ≥1）· `2`（应为 2，`06` 侧由他人改） | ✅ |
| T-152 | DONE（`02` 侧） | §10 的「⚠️ 已知缺口」**从"整块缺失"改写成"缺的是数量不是有无"**：保留归宿卡 C2/C3 的事实，但补上「**内容部分已有**」+ 线 C `diagnostics/4-basic-diagnostics-precise-criteria.md` 四句现成外部问法的**逐字与行号**（泛化 `:77` / 矛盾 `:242` / 结论 `:344` / 扁平 `:529`）+ `§7.2` 追问语气映射 + `§7.3` 跨诊断质量门，并写死结论「**不要把这一块当成未解决缺口整块跳过**」 | `grep -rc "内容部分已有" SKILL交接包/01_产物清单.md SKILL交接包/02_SKILL.md施工规格.md SKILL交接包/03_references施工规格.md SKILL交接包/10_新会话接手指南.md` · `grep -rc "4-basic-diagnostics-precise-criteria" SKILL交接包/*.md` | `01`:`1` / `02`:`1` / `03`:`1` / **`10`:`0`**（四处均应 ≥1）· `0*.md` 合计 `5`（应 ≥4） | ⚠️ `10_新会话接手指南.md` 仍 0，不是我的文件 |
| T-155 | DONE | §3 追问深度轴**新增 item 4「信号识别表」**（三列六行，用户描述中的信号 → 识别为 → 追问方向，六行全部照抄线 B `§3.2`：纯形容词/标签、具体但不够独有、两个以上截然不同的行为模式、两个特质在同一画面里打架、不知道行为背后原因、多个画面看不出共性）+ **item 5「五个常犯错误名」**（标签放大 / 频道切换 / 情绪点名 / 戏剧化升级 / 泛化填充，一行词表 + 每个一句症状，验收"没有一条超过一句话"）。§10 **可复现性目标补逐字原句**「**同一段用户输入，AI 10 次判断应得到 10 次一样的结果。**」并把锚从 `DIAGNOSTICS_DECISION.md` 换成 `4-basic-diagnostics-precise-criteria.md:4` | `grep -c "标签放大\\|频道切换" SKILL交接包/02_SKILL.md施工规格.md` · `grep -c "10 次判断" …` · `grep -c "放大器" SKILL交接包/素材/_提取清单.md` | `1`（应 ≥1）· `1`（应 ≥1）· `2`（应 ≥1，素材侧由他人改） | ✅ |
| T-306 | DONE（`02` 侧） | §2 来源行**删掉不存在的锚**"线 A `DISCUSSION_01 §决策 1c`"（`DESIGN_DISCUSSION_01.md` 只有决策 1/2/3/4），改成「线 A `DISCUSSION_01 §讨论中确认的全局性区分`（`:225-239`）」 | `grep -rc "决策 1c" 0*.md`（从 `SKILL交接包/` 内执行） | `02`:`0` ✅ · **`04`:`1`**（应全为 0） | ⚠️ `04_硬约束与红线.md` 仍残留 1 处，不是我的文件 |
| T-308 | DONE | §10 三级判定的档位名**改成线 C 实名**：原"三级判定（触发/观察/通过）"→「三级判定（**明确触发 / 弱触发 / 不触发**，线 C 内部记作 `strong` / `weak` / `false`）」。"观察"退回它本来的位置——三条核心纪律里的"灰色地带默认走观察"，不再当档位名用 | `grep -c "触发/观察/通过" SKILL交接包/02_SKILL.md施工规格.md` | `0`（应为 0） | ✅ |
| T-317 | DONE | 段落清单的行数预估行**删掉**"线 C **自认从未核对过**"（全仓无此自陈），改成「线 C 的行数一律标为"**预估行数**"，全仓无实测记录」。"写之前先做体量核对、把 4 个判定树按实际格式写出来数行数"这个提醒按台账要求**原样保留** | `grep -c "自认从未核对过" SKILL交接包/02_SKILL.md施工规格.md` | `0`（应为 0） | ✅ |
| T-329 | DONE | §1 来源行**从笼统的"三线一致"改成逐 item 归属**：「三线一致；**item 2 逐字取线 B `§1.3:119`**；item 3 取线 B `I_SECTION_REVIEW`（三方共识）」 | `grep -c "§1.3:119" SKILL交接包/02_SKILL.md施工规格.md` | `1`（应 ≥1） | ✅ |
| T-334 | DONE（`02` 侧） | 取台账给的第二个选项（把编号换成内容描述，因为 `05` 至今没有融合项索引）。`02` 里两处 M 编号**就地补上定义**：§7 来源行 → 「融合项 **M-19** = 入口分流与五种入口的归并项」；§12 来源行 → 「融合项 **M-38** =「素材是事实，构建是解读」的执行程序」。现在不查 `05` 也能看懂 | `grep -on "M-[0-9]\+" 0*.md \| wc -l`（从 `SKILL交接包/` 内执行）+ 逐处核对 | 全包 5 处：`02`:2（M-19/M-38，**两处均已带内容描述**）· `03`:2（M-08）· `04`:1（M-28） | ✅（`02` 侧；`03`/`04` 三处不是我的文件） |

---

## ① 验收没通过的条目

只有 **1 条是真·没过**，另有 **4 条是跨文件命令卡在别人的文件上**。

### 真·没过：T-141

- **命令**：`grep -A6 "^### 7. 入口意识" SKILL交接包/06_验收.md | grep -c "^| S7"` 期望 ≥4，**实际 3**。
- **不是修复没做**：`06_验收.md` 里 `S7-1`…`S7-4` 四条都在（L147-150），`grep -c "S7-4"` = 1 已通过；`02` §7 item 5 的五行第一动作表也已写好。
- **是命令的窗口开小了**：抬头在 L143，`-A6` 只覆盖到 L149，正好把 L150 的 `S7-4` 切在窗口外。**改成 `-A7` 就是 4。**
- 我没有改台账（禁止改），此处登记，请主流程把台账里这条命令的 `-A6` 改成 `-A7`。

### 跨文件卡住（`02` 侧全部到位，另一半在别人手里）

| T | 卡在哪 | 现在什么值 | 需要谁做 |
|---|---|---|---|
| T-033 | `grep -c "绝对不变的内核" SKILL交接包/素材/追问原句库.md` · `grep -c "教程未提供" …` | `0` · `0` | `素材/追问原句库.md` 要新增「深度轴五层 × 原句索引」一节。`02` 侧的缺口登记已写 |
| T-108 | `grep -c "A-09\\|溯源操作" SKILL交接包/10_新会话接手指南.md` | `0` | `10_新会话接手指南.md` §六之二 要登记 A-09（台账 T-143 是同一次编辑） |
| T-152 | `grep -rc "内容部分已有" … SKILL交接包/10_新会话接手指南.md` | `10`:`0`（其余三处均 1） | `10_新会话接手指南.md` 同步这句事实陈述 |
| T-306 | `grep -rc "决策 1c" 0*.md` 应全为 0 | `02`:`0` ✅ · `04`:`1` | `04_硬约束与红线.md`:379 还有一处"决策 1c" |

> 这四条我都**没有越界去改别人的文件**。`04`/`06`/`03`/`01`/`05`/`素材` 本轮有其他 agent 在并行改动，本文件记录的跨文件数值是 2026-08-15 本次跑命令时的快照，之后会变。

## ② 跳过的条目和原因

### 整条跳过：1 条

- **T-118 · 入口意识：五种入口 vs 素材类型识别** —— 台账 §七 **D-c** 明确列为"需要用户拍板才能动"，并写明"改了会白改"（AUDIT1 判 M-19 已落地 ✅，AUDIT4 判碰撞时取了旧版本 🔴，两份审计判定相反）。**没有落地任何形态改动**，只在 §7 末尾加了一个 ⏳ 待拍板登记块，写清三个候选方案和"拍板前不要动 item 4/5 的形态"。

### 条内部分跳过：3 处

| T | 跳过的那一小块 | 卡在 | `02` 里怎么处理的 |
|---|---|---|---|
| T-116 | 与 `dialogue-persona` 的**中途切换条件** | **D-e**（`01`:117 写"排他替代"，线 B `B_SEGMENT §六` ⑤ 写"不是绝对互斥，不要假装硬隔离"） | 13b 表下留 ⏳ 登记块，"拍板前只写四维，不写切换条件；拍完补一行" |
| T-127 | B 段写作原则 **⑤**（同一条切换问题） | **D-e** | 同上，与 T-116 共用一个登记块 |
| T-128 | description 的**第 2 条**（避开与 `char-creator` 撞车的措辞） | **C14**（skill slug 未定） | §0 表里该行素材栏标 ⏳ **C14**，并注明"本条唯一挂起项，其余六条不等 C14"；依赖段重复一次 |

> 另有 3 条内容我**照台账要求写死了、但归宿仍挂着**，这不算跳过：§10a/§10b 挂 C3/C6（内容不受阻塞，已写）· §12a/12b/12c 挂 C13（C13 只决定写在哪一段，内容已写死）· §6 里程碑级挂 C12（三层框架本身不依赖 C12，已写）。每处都在文件里写了"C 拍完只需搬位置 / 替换那一格"。

## ③ 台账本身写错的地方

按危害从高到低。

**① T-141 的验收命令窗口差一行（会误判成没修）。**
`grep -A6 "^### 7. 入口意识" SKILL交接包/06_验收.md | grep -c "^| S7"` 期望 ≥4，但 `06` §7 抬头在 L143、四条 `S7-x` 在 L147-150，`-A6` 只到 L149。**必须改成 `-A7`**，否则这条永远差 1、永远看起来没做完。

**② T-118 的验收命令查不出"做没做"，只查得出"提没提"。**
`grep -c "素材类型"` / `grep -c "信息密度最高"` 两条现在都返回 1 —— 但那 1 是我写的**待拍板登记块**里引用 D-c 原文时带出来的措辞，**实际改动一个字都没落地**。这条如果只看命令会被判成 DONE。它是 §七 D-c 的下游，建议在台账里给它加一条形态判据（比如"§7 item 4 的标题里出现'素材类型'"），或者干脆在拍板前把它的验收标成"不适用"。

**③ T-021 第三条命令的期望值和 `grep -c` 的语义对不上。**
`grep -c "H-19a\|H-19b\|H-19c" SKILL交接包/04_硬约束与红线.md` 期望"应为 3"，实际返回 `6`。`grep -c` 数的是**匹配行数**不是**出现次数**，三个编号各自出现在定义行和引用行上，行数天然大于 3。想数编号个数要用 `grep -o … | sort -u | wc -l`（那样才是 3）。同样的写法在 T-010 的 `grep -c "H-31\|H-32"`（期望 ≥2，实际 6）上不出问题，因为那条是 ≥ 不是 =。

**④ T-127 的验收命令和它的 `02` 侧动作完全无关。**
T-127 的"改哪"包含 `02` 通用规格（写作原则），但三条验收命令查的全是 `01_产物清单.md` 和 `04_硬约束与红线.md` 的"致命级 / 严重级 / 中等级"（那是同条目里 `04` §七 严重度分级那一半）。**`02` 侧写没写六条写作原则，这条验收查不出来。** 建议补一条 `02` 侧命令，例如 `grep -c "body 是路由器" SKILL交接包/02_SKILL.md施工规格.md` 应 ≥1。

**⑤ T-151 会被"改哪含 02"这类筛法误命中。**
它的"改哪"里的 `02` 出自决策编号 `D-02`，落点其实是 `03`/`06`。用 ``` `02_SKILL` ``` 或 ``` `02` ``` 检索都会踩到。

**⑥ T-122 用 `02_SKILL` 检索筛不出来（会漏做）。**
它的"改哪"写的是「…… 或 `02` §11 流程地图」，只有反引号包的 ``` `02` ```。按"改哪指向 `02_SKILL.md施工规格.md`"筛会直接漏掉这条。本轮已补做。建议台账统一落点写法：一律写全名 `02_SKILL.md施工规格.md`。

**⑦ 行号已经漂移（不影响判断，但会让人找错地方）。**
台账里给 `02` 的行号锚（如 T-100 的"§7 入口意识（L141-150）"、T-108 的"§12（L224-226）"、T-026 的"§10（L206-214）"、T-306 的"`02`:67"、T-308 的"`02`:210"、T-317 的"`02`:40"、T-329 的"`02` §1 item 2（L53）"）都是对 224 行版本的。**本轮改完 `02` 是 585 行，上述行号全部失效**，但节号（§1/§7/§10/§12）仍然有效。建议台账后续只用节号 + item 号，不用行号。

**⑧ 两节内部 item 重编号，外部引用会指错。**
补漏时 §6 和 §7 有 item 插到前面，编号整体后移。我已在 `02` 顶部加了一张「⚠️ item 编号变更记录」表登记换算关系（§6 遮名测试 item 1 → **item 3**；§7 五种入口 item 1 → **item 4**、每种入口第一动作 item 2 → **item 5**、现成卡 item 3 → **item 6**、什么都说不出 item 4 → **item 7**），其余各节一律只在尾部追加、原编号不动。**受影响的外部引用**：`06_验收.md` S7-4 现在写的是"（`02` §7 item 2）"，按新编号应为 **item 5**；台账 T-112 写的"§9 推演权限 item 3"仍然有效（§9 是尾部追加）。
