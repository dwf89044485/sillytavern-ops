# FIXLOG · 导航层四文件

> 执行范围：`SKILL交接包/00_START_HERE.md` · `01_产物清单.md` · `05_已定决策速查.md` · `素材/_提取清单.md`
> 依据：`SKILL交接包/12_修复台账.md`（2026-08-15 校验基线）。台账 §七 待用户拍板项一律跳过。
> 未触碰 `02`/`03`/`04`/`06`/`10`/`12`（其他 agent 在改或由主流程统一更新）。
> 验收命令一律从 `/workspace` 执行；下表"跑出来是什么"是本次实跑输出。

---

## 一、逐条台账

| T 编号 | 改哪个文件 | 状态 | 改了什么 | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|---|
| T-011（`01` 半边） | `01` §五 末段 | DONE（核实为已修） | "只有五个"排他清单已改成正向表述「必须服务于……⚠️ 这是正向要求不是排他清单——意象、基础信息、人物文档落盘也是本 skill 的产物」。本次到场时 `01` 已是正确版本，未再动手 | `grep -c "只有五个" SKILL交接包/04_硬约束与红线.md SKILL交接包/01_产物清单.md`（两处均应 0）；`grep -c "必须服务于" SKILL交接包/04_硬约束与红线.md`（≥1） | `04:0` `01:0`；`04` 必须服务于 = 2 | ✅ |
| T-016（`00` 半边 + 我四份文件里的裸编号） | `00` §二；`01` / `05` / `_提取清单` 全文 | DONE | ① `00` §二 新增「写第一个字符之前必须已经知道的四件事」第 4 条：三套 `C+数字` 命名空间对照表（待拍冲突 `C1`–`C14` / 素材分节 `C1`–`C14` / `06` 验收项 `C1`–`C15`）+ 误读实例 + 「遇到裸 C 编号先确认属于哪一套」。② `01` 全部 20 处、`05` 全部 12 处、`_提取清单` 全部 7 处冲突编号加「冲突」限定词，并在 `01`/`05`/`_提取清单` 各加一句"本文件的 C 编号是待拍冲突编号"。`03`/`06` 的裸编号未动（属其他 agent） | `grep -c "三套命名空间\|三套" SKILL交接包/00_START_HERE.md`（≥1）；补充自查 `grep -n '[^A-Za-z\`突]C[0-9]\+' <我的四份>` | `00` = 1；四份文件里已无不带限定词的 C 编号（`00` 唯一命中在警告正文里） | ✅ |
| T-024（`05` 半边） | `05` 待拍板表 C12 行 + 确认项 L4 行 | DONE | ① C12 行由"MVC 结构兜底含不含意象"扩成四条子问题：三层判读（用户主权 > 质量信号 > 结构兜底）的落位 / 结构兜底含不含意象 / 线 A 结构定义是吸收还是否决 / "非调色盘路径结构性永远进不了 MVC"要不要写。② L4 行扩成四条：文档框架顺序 / 构建路径多入口 / **AI 内部推演优先级（层的防御机制决定面的功能）** / 依赖链注释措辞。③ 表头上方加元规则：往本表写议题时议题描述必须覆盖 `04_冲突拍板单.md` 的全部子问题。`03` item 8 / `06` D8 / `10` §六之二 三处不归我 | `grep -c "三层判读\|用户主权" SKILL交接包/05_已定决策速查.md`（≥1）；`grep -A0 "L4 读法" SKILL交接包/05_已定决策速查.md \| grep -c "四条\|推演优先级"`（≥1）；❓ 与 `三线碰撞/04_冲突拍板单.md` 并排子问题数一致 | 1；1；❓ 已人工比对：拍板单 `:432-469` C12 段含全部四条子问题、`:275-292` L4 确认项恰好四条（文档框架排列 / 构建路径 / AI 内部推演优先级 / 注释措辞），逐条对上 | ✅ |
| T-027（`_提取清单` 半边） | `_提取清单` 清单表 + 新增「随附警告」节 | DONE | `诊断判定树.md` 行加"⚠️ 提取前先看下面「随附警告」两条"，并新建 `## ⚠️ 随附警告（提取对应素材时必须一起看）` 表，第一条写死：**伪负面的例子不要用"太操心了"**（`DIAGNOSTICS_DECISION.md:334` 把它列为成功打破扁平的产出）。同时把"挂起融合项时随附 ⚠️ 必须一起挂"这条通则写进该节抬头 | `grep -c "太操心了" SKILL交接包/素材/_提取清单.md`（≥1）；`grep -c "警告必须一起挂" SKILL交接包/10_新会话接手指南.md`（≥1，**不归我**） | `_提取清单` = 1 ✅；`10` = 0（`10` 不在我的范围，留给 `10` 的负责人） | ✅（我的半边） |
| T-127 ③（`01` 半边） | `01` §三 第 13 行 | DONE | 常见错误清单行补"呈现形态已定"：失败模式按严重度分三档（致命级毁掉角色 → 严重级降低质量 → 中等级影响细节），**AI 优先防致命级**，并点名致命级四条 = AI 扩写污染 / 衍生给 AI 写 / 强行拆面 / 调色盘与台词人设混用。①②④⑥ 归 `02`、⑤ 卡 §七 D-e 不做 | `grep -c "致命级" SKILL交接包/01_产物清单.md SKILL交接包/04_硬约束与红线.md`（至少一处 ≥1）；`grep -c "严重级\|中等级" SKILL交接包/04_硬约束与红线.md`（≥1，**不归我**） | `01:1` `04:6`；`04` 严重级/中等级 = 7（`04` 负责人已做） | ✅ |
| T-142（`05` 半边） | `05` | SKIPPED（条件不成立） | 台账写的是"`05_已定决策速查.md` **若有 D04 描述则同步**"。`05` 全文无 D04（只有 D-01 / D-02 两条已拍板），无可同步之处，未改。主体（`03` §组装期一致性验证 抬头改五时机）归 `03` | `grep -c "D04" SKILL交接包/05_已定决策速查.md` | 0 → 条件不成立 | N/A |
| T-144（`_提取清单` 半边） | `_提取清单` 题库行 | DONE | 题库行形态要求补：**+ §G「答案只有标签时的通用追问」四套**（`24问.md:23-28` / `80问.md:36-57` / `80问.md:1014-1028` / `工作流.md:80-88`），并写明 `03` §题库 的"卡住模板"（每模板 = 一个卡住场景 + 4-5 问的追问组）就从 §G 落、不用另提。已实地核对 `题库.md`:46 / `:2196` §G 确实存在 | `grep -c "卡住模板" SKILL交接包/03_references施工规格.md`（≥1，**不归我**）；本半边自查 `grep -c "卡住模板" SKILL交接包/素材/_提取清单.md` | `03` = 0（留给 `03` 负责人）；`_提取清单` = 1 | ✅（我的半边） |
| T-152（`01` 半边） | `01` §三 末行 | DONE | "4 诊断的外部问法与质量门"行由"⏳ 三线共同缺口"改为「⏳ 归宿卡冲突 C2/C3。**内容部分已有**：线 C `diagnostics/4-basic-diagnostics-precise-criteria.md` 每诊断各 1 句外部问法（`:77` 泛化 / `:242` 矛盾 / `:344` 结论 / `:529` 扁平）+ `§7.2` 语气映射 + `§7.3` 跨诊断质量门。**缺的是数量不是有无**——通用规格要求每诊断 2-4 句，现有 1 句」 | `grep -rc "内容部分已有" SKILL交接包/01 02 03 10`（四处均 ≥1）；`grep -rc "4-basic-diagnostics-precise-criteria" SKILL交接包/*.md`（≥4） | `01:1` ✅ `02:1`（`02` 负责人已做）`03:0` `10:0`；`4-basic…` 命中 `01`/`02`/`04`/`11`/`12` 五份 | ✅（我的半边；四处登记还差 `03`/`10`） |
| T-155（`_提取清单` 半边） | `_提取清单` 「随附警告」节 | DONE | 随附警告表第二条：**矛盾检测里的"放大器"是触发条件**（`自卑+成绩好` 不触发、`极度自卑+成绩好` 才触发），与调色盘 `03` 诊断 2 / `06` T2 的"去掉放大器"（清洗动作）**同名不同物**，不要当成已覆盖。`02` §3 信号识别表 / §10 可复现性原句两块归 `02` | `grep -c "放大器" SKILL交接包/素材/_提取清单.md`（≥1，现为 0） | 2 | ✅ |
| T-161（`00` + `05` 半边） | `00` §二、`00` §五、`05` 待拍板表 | DONE（部分改法按 T-001 实际落点调整） | ① 必读表 `01` 行注明"**产物格式规范（§一之二）**"——T-001 最终没有新建 `07`，格式规范落在 `01` §一之二，所以必读集仍是四份、指针改指 `01`。② `00` §二 新增「写第一个字符之前必须已经知道的四件事」：产物格式（指 `01` §一之二）/ reference **四段式**骨架 + "第 1、2 段必须物理分开是硬要求" / "形态"的六类结构类型 / C 编号三套。③ `00` §二 加一句"标 `⏭` 的小节写作 AI 跳过"，`00` §五 标题加 `⏭`，`05` 待拍板表标题加 `⏭` 并写明"写作 AI 跳过这一节" | `grep -c "必读的.\{0,2\}份" SKILL交接包/00_START_HERE.md` 里的数字应为"五"；`grep -c "⏭" SKILL交接包/00 04 05`（三处均 ≥1）；`grep -c "管哪个产物" SKILL交接包/04_硬约束与红线.md`（≥1，**不归我**） | 输出 `必读的四份` ❌（见下方 §二 台账错误 1，"五份"这个期望值已被 T-001 的实际落点作废）；⏭ `00:2` `04:1` `05:1` ✅；`04` 管哪个产物 = 0（`04` 负责人未做） | 🟡 见 §二-1 |
| T-162 | `00` 抬头 + §三 末尾 | DONE | ① 抬头由"这个包是自包含的"改为「这个包**在素材已提取的产物上**是自包含的……**素材未提的产物现在不要动工**——当前已提 **4/8**」。② §三"唯一的例外"补触发条件（含"篇幅预算与'必须包含'清单不相容"）+ 新增一行「**已知会触发的地方**：各手册的行数预算（见 `12_修复台账.md` T-002）。这一条已裁定为'估算不是硬上限'，`03` §篇幅与量级 已写明——先看那里，仍然对不上再回查」 | `grep -c "1/8" 00`（0）；`grep -c "4/8" 00`（≥1）；`grep -A3 "唯一的例外" 00 \| grep -c "行数预算\|已知会触发"`（≥1） | 0；1；1 | ✅ |
| T-301（`05` 半边） | `05` D-01 ② ❌ 区块第 3 句 | DONE | "因为这个词让她想起小时候被父母规训的记忆"补锚 `11.另一种写法:212-213`，并显式注明「**不在素材 E12 里**，E12 装的是另外两句」。已回原文核对：`性格调色盘/11.另一种写法…md` L212 = "解释性写法（错误）："、L213 = 该句 | `grep -c "11.另一种写法:212-213" SKILL交接包/04_硬约束与红线.md SKILL交接包/05_已定决策速查.md`（两处 ≥1） | `04:1`（`04` 负责人已做）`05:1` | ✅ |
| T-304 | `05` D-01 ③ 区块格式 | DONE | 三行压缩版恢复成素材 `G1` 逐字形态：`## 十一、AI做出的高可能推论` / 空行 / "这里只能写推论，不能写成事实。" / 空行 / "格式："独占一行 / "根据用户目前回答，角色可能存在……" / "但仍需要用户确认……"，并加一句"**'格式：'独占一行，后面两行分列，不要压成一句**" + 出处（素材 `G1`，`核心人格层_原文素材.md`:1295-1303，锚 `工作流.md:299-305`） | `grep -c "根据用户目前回答" SKILL交接包/05_已定决策速查.md`（≥1，现为 0） | 1 | ✅ |
| T-313 | `05` D-01 ① 行为题表 | DONE | 「她什么时候会暴露？」→「她什么时候会暴露**这个缺失**？」，并补一行逐字来源：素材 `C4` 深层缺失三题 = 083/086/087（`200问.md:551`/`:569`/`:575`）、核心恐惧三题 = 093/094/095（`:613`/`:619`/`:625`）。⚠️ 台账写"083/084/085"式的连号是错的，实地核对为 083/086/087（见 §二-3） | `grep -c "她什么时候会暴露这个缺失" SKILL交接包/05_已定决策速查.md`（≥1） | 1 | ✅ |
| T-331 | `05` D-01 ② ✅ 正确形态 | DONE | ✅ 区块抬头补锚：「节选自素材 A2 陈乖乖成品（`核心人格层_原文素材.md` :58-80，锚 `05.md:234-250`）」 | `grep -A8 "正确形态" SKILL交接包/05_已定决策速查.md \| grep -c "A2\|:58-80"`（≥1） | 1 | ✅ |
| T-332 | `05` D-01 ② ❌ 三条错误示范 | DONE | 三条各补锚：① 素材 E1 `7.md:470-474`（教程判"太虚"）② 素材 E2 `7.md:90-94`（教程判：这只是素材，还不是运行逻辑）③ `11.另一种写法:212-213`。三个锚都在 `核心人格层_原文素材.md`:999 / :1009 与教程原文核对过 | `grep -c "7.md:470-474" SKILL交接包/05_已定决策速查.md`（≥1） | 1 | ✅ |
| T-334 | （`05` 是两个可选落点之一） | SKIPPED（出范围） | 台账给两个方案："在 `05` 补融合项编号索引"或"把编号换成内容描述"。实测 `M-` 编号只出现在 `02`（2 处）和 `03`（1 处）、`04`（1 处），**我的四份文件里一处都没有**，改动主场在别人的文件；且往必读的 `05` 塞一张编号索引违反"必读集控制在一次读完"的约束。本次不做。旁证：`02` 负责人已在走方案二（`02`:281 现写「融合项 M-19 = 入口分流与五种入口的归并项」） | `grep -on "M-[0-9]\+" SKILL交接包/0*.md \| wc -l` | 4（`02`:281、`02`:496、`03`:451、`04`:205），我的四份文件 0 处 | N/A |
| T-340 | `_提取清单`:32 | DONE（核实为已修） | 行数已是 1659，无需动手 | `grep -c "1659 行" SKILL交接包/素材/_提取清单.md`（≥1） | 1 | ✅ |

**额外同步（不是台账条目，但属同一批文件的事实对齐）**

| 改哪 | 改了什么 | 为什么 |
|---|---|---|
| `01` §二 第 11 行 | 「⏳ 冲突 C5」→「⏳ 冲突 C5（**触发指针那一半不受阻塞**，见 `02` §11a）」 | T-005 已把触发指针从第 11 段拆成不受 C5 阻塞的 §11a，`02`:67 也已改成"⏳ C5（11a 不受阻塞）"，只有 `01` 还整行标 ⏳。不改的话写作 AI 会连触发指针一起跳过——正是 T-005 要防的失败 |

---

## 二、发现的台账自身错误

**1 · T-161 的验收期望值已被 T-001 的实际落点作废（会误判为未完成）**

T-161 验收写「`grep -c "必读的.\{0,2\}份" 00` 里的数字应为"五"（现为"四"）」，前提是 T-001 新建 `07_产物格式规范.md` 并进必读集。但 T-001 的"完成时填"记的是**新建 07 被否（用户要求不新增文件）**，格式规范写进了 `01_产物清单.md` 新增 §一之二，而 `01` 本来就在必读集里。所以必读集恒为四份，"五份"这个期望值永远不会达成。本次按实际落点执行（必读表 `01` 行指向 §一之二），验收命令原样跑出 `必读的四份`。**建议把 T-161 的验收改成**：`grep -c "一之二\|产物格式规范" SKILL交接包/00_START_HERE.md` 应 ≥1。

**2 · T-161 与 T-016 有一条重复条目**

T-161 的六条缺口里第 ④ 条（C 编号有三套）就是 T-016 的 `00` 那一半，两条指向同一处编辑。本次一次改完，两条都记 DONE。

**3 · T-313 的题号推断有误**

T-313 说「素材 `C4` 083 逐字是"她什么时候会暴露这个缺失？"（**另两题逐字无误**）」——句子本身对，但"另两题"的隐含题号不对。实地核对 `核心人格层_原文素材.md`:644-651：`05` 那三题对应的是 **083 / 086 / 087**（`200问.md:551` / `:569` / `:575`），不是连号的 083/084/085（084 是"她会把缺失误认成什么"、085 是"她会用什么替代真正缺失"）。本次按实际题号写锚。

**4 · T-144 的追问组数量是 3，素材里实际是 4 套**

T-144 说"把 `24问:23-28`、`80问教程:36/44-49`、`:1022-1028` 那三组落成卡住模板"。`题库.md`:46 / §G 实际收了**四套**：`24问.md:23-28` / `80问.md:36-57` / `80问.md:1014-1028` / `工作流.md:80-88`；且台账写的 `80问教程:44-49` 与 `:1022-1028` 两个区间与素材记的 `:36-57`、`:1014-1028` 边界不一致（素材范围更大，是超集）。本次在 `_提取清单` 里按素材实际的四套 + 实际区间登记。

**5 · T-027 的验收有一半落在 `10`，而 `10` 明确不在写作 AI 的必读链上**

T-027 第二条验收查 `10_新会话接手指南.md` 有没有"随附 ⚠️ 警告必须一起挂"这条通则。`10` 是给继续拍板的人看的，本次未动。但这条通则**对提取素材的人同样成立**，所以我把它写进了 `_提取清单.md` 新「随附警告」节的抬头——`_提取清单` 才是提取者真正会打开的文件。`10` 那一处仍待 `10` 的负责人补。

**6 · T-142 的 `05` 半边是空条件**

"若有 D04 描述则同步"——`05` 里没有 D04。这条对 `05` 是恒空，写在"改哪"里会让人以为 `05` 有活要干。

---

## 三、跨 agent 未闭合的挂账（我这边做完了，另一半在别人手上）

| T | 我做完的半边 | 还缺的半边 | 归谁 |
|---|---|---|---|
| T-016 | `00` 三套命名空间警告 + 我四份文件的编号限定词 | `03`/`06` 所有裸 C 编号加限定词、`06` 抬头 | `03` / `06` |
| T-024 | `05` C12 行 + L4 行议题扩写 | `03` item 8 与 `06` D8 的注释措辞标 ⏳ 或改回 L4 原文；`10` §六之二 加元规则 | `03` / `06` / `10` |
| T-027 | `_提取清单` 的"太操心了"警告 | `10` §7.1 的通则句 | `10` |
| T-144 | `_提取清单` 题库行指向 §G | `03` §题库 新增"卡住模板"一条 | `03` |
| T-152 | `01` §三 末行 | `03`:381 末行、`10` §7.3 第 1 行（`02` §10 已由 `02` 负责人做完） | `03` / `10` |
| T-155 | `_提取清单` 放大器同名混淆提醒 | `02` §3 信号识别表 6 行 + 五个常犯错误名、`02` §10 可复现性原句与新锚 | `02` |
| T-161 | `00` 必读集四条前置 + `00`/`05` 的 ⏭ | `04` 抬头的"红线 × 产物"索引表（`grep -c "管哪个产物" 04` 现为 0） | `04` |
| T-301 | `05`:51 补锚（`04` 那一半已由 `04` 负责人做完） | — | — |

---

## 四、必读集现状评估（读完 `00`/`01`/`04`/`05` 能不能开写）

**体量**：`00` 143 行 + `01` 159 行 + `04` 719 行 + `05` 140 行 = **1161 行**。`04` 一份占 62%，而 T-146 还要往 `04` 补 16 条左右硬约束、T-156 还要补对照表——`04` 会继续涨。"必读四份加起来能一次读完"这句话现在已经不成立，缺口不在 `00`/`01`/`05`，在 `04`。

**内容**：T-161 列的六条缺口，本次在 `00` 侧补上了四条（产物格式指针 / 四段式骨架 + 物理分开是硬要求 / 形态六类 / C 编号三套），第六条（必读集混着非必读内容）通过 `⏭` 标记解决了 `00` §五 与 `05` 待拍板表两处（`04` §七 由 `04` 负责人已加 1 处 ⏭）。**只剩第五条没补**：`04` 里没有"哪条红线管哪个产物"的索引，写任何一份 reference 都得把 23+ 条红线整过一遍才敢动笔——这一条不在我的文件里。

结论写在最终回复里。
