# FIXLOG_03 · `03_references施工规格.md` 按 `12_修复台账.md` 修补记录

> **执行范围**：只改 `SKILL交接包/03_references施工规格.md` 一个文件。台账本身、`00`/`01`/`02`/`04`/`05`/`06`/`10`/`素材/` 一律没动。
> **执行日期**：2026-08-15
> **筛选口径**：台账全部 132 条里，"改哪"指向 `03_references施工规格.md` 的 TODO 条目 —— P1 **7** 条 · P2 **19** 条 · P3 **23** 条，合计 **49** 条。
> **验收口径**：台账 §一 第 4 条要求"验收命令一律从仓库根目录 `/workspace` 执行"，下表命令均如此跑。命令里的 `03_*.md` 一律展开成 `SKILL交接包/03_references施工规格.md`。
> **跨文件子句的处理**：多条验收同时查 `03` 和别的文件。本表只对 `03` 侧的子句负责；别的文件的子句照跑照记，但**不是本次授权范围**，在"通过?"列单独标出。
>
> **⚠️ 本轮发现台账自身有 4 处错误，逐条记在文末 §三。**

---

## 一、P1（7 条）

| T 编号 | 状态 | 改了什么（具体到哪一节、加了什么删了什么） | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|
| **T-012** | DONE | §混色。① item 8 禁止清单第一项由"三个以上情绪名词"改成"**并列成清单的情绪罗列**"，并加一句"教程未给数量阈值，判的是句式不是个数"；八项与 `04` H-15、`06` X5 逐字对齐。② 整节补出处锚：节头加 `> **出处锚**` 一整块，再逐行内嵌 —— item 1 `04_混色篇:248`、item 2 `4.md:184`、item 3 `:33-41`、item 4 `:82-104`、item 5 `:57`、item 6 `:198`、item 7 `:102`+`:216-222`、item 8 逐项锚 `:210`/`:232`/`4.md:137`/`4.md:118`/`2.md:444`、成品集 `:250-308` | `grep -c "三个以上情绪名词" SKILL交接包/0[346]*.md`（应三处均 0）；`grep -c "04_混色篇:" .../03_...md`（应 ≥5） | `03:0 04:0 06:0`；`10` | ✅ |
| **T-016** | DONE | 全文。① 抬头新增一段 C 编号命名空间说明（待拍冲突 / 素材分节 / 验收项），并写死"本文件的 C 编号一律带限定词，裸编号按错误处理"。② 逐处加限定词：`C1`/`C2`/`C3`–`C9`/`C10`/`C12` → `素材 C1`…（核心人格层素材列）；依赖行 `⏳ C5` → `⏳ 冲突 C5`；意象 `⏳ 卡 C12`/`C11` → `⏳ 卡冲突 C12`/`冲突 C11`；落盘 `C10`/`C11` → `冲突 C10`/`冲突 C11`；§未定 reference 表全部 C 号加"冲突"。③ 范围写法改成两端都带限定词（`冲突 C1`–`冲突 C14`、`素材 C3`–`素材 C9`），否则区间尾号会被判成裸编号 | `grep -on '\(素材\|冲突\|06\) \?\`C[0-9]' 03 06 \| wc -l` 应等于 `grep -on '\`C[0-9]\+\`' 03 06 \| wc -l` | `03` 单独：`12 = 12`；`03`+`06` 合并：`16 = 16` | ✅ |
| **T-018** | DONE | §核心人格层 item 14。小节标题由"反面判据"改成"**产出物反面判据**"；形态要求加一句边界——"这一条判的是**产出物形态，不是用户输入**"，并写明用户自己说成因式句子时按 `04` H-02 处理（收下当素材、继续往运行逻辑推、**不打回**），只有成因式句子出现在产出物里才算命中 | `grep -A2 "反面判据" ... \| grep -c "产出物形态，不是用户输入\|不是用户输入"` 应 ≥1 | `1` | ✅ |
| **T-019** | DONE | §核心人格层 item 12 验收列。删掉机械口径"抽查成品，出现「因为…所以…」即不合格"，换成三分判断：检索到的每一处逐处判定是 (a) 本规格强制逐字引用的素材原文（item 13/14 要的，允许）/ (b) 推导中间步骤（允许）/ (c) 产出物自己的表述形态（禁止），**只有 (c) 不合格**，口径同 `04` H-02 | `grep -n "出现「因为…所以…」即不合格" ...` 应 0 命中 | `0` | ✅ |
| **T-020** | DONE | §核心人格层 第 3 段 item 8（原 item 7）素材列。`B2` 从"示范素材"降级——写明"**`B2` 只可作为下潜层次的参照，不可作为 AI 动作的参照**"，理由（B2 里引导者自己把结论说完了）与 `B4` 同理一并写入；示范正例只留 `B3` | `grep -A1 "摆素材反推的完整手法" ... \| grep -c "不可作为 AI 动作的参照"` 应 ≥1 | `1` | ✅ |
| **T-023** | DONE | ① §多面性 质量门新增 **item 8b「"功能"部件的来源规则」**：功能来自核心人格层**防御机制**的分支（`7.md:545-559`），核心人格层没挖时记**待定**、组装时回填，**禁止凭空编**。② §人物文档落盘 item 8 补上依赖链实际顺序「基础信息 → 调色盘 → 多面性 → 混色 → 核心人格层 → 二次解释 → 意象」 | `grep -c "功能" ...` 应从 1 升到 ≥3；`grep -c "基础信息 → 调色盘" ...` 应 ≥1 | `功能=3`；`依赖链=1` | ✅ |
| **T-024** | DONE（`03` 侧） | §人物文档落盘 item 8。把被定死的注释措辞**解锁**：保留现行写法"这是文档框架顺序，不是追问顺序"的同时，标明它其实出自确认项 L4 的**否决理由**，而 L4 原文规定的注释内容是"**这依赖什么先确认**"，两版并存并挂 ⏳ 待拍，**不要当成已定** | `grep -c "这依赖什么先确认" ...` 应 ≥1（`03` 侧口径） | `1` | ✅（`05`/`06`/`10` 三处不在授权范围） |

---

## 二、P2（19 条）

| T 编号 | 状态 | 改了什么 | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|
| **T-101** | DONE | §人物文档落盘 新增 **item 4a「素材区的『基础信息』小节」**，排在四类分组**之上**，五格：身份 / 外貌 / 背景 / 关系 / 习惯与偏好，带原文免责句"**按需记录，不要求完整**"；同时在 item 4 加一句"四类分组是**有推演价值的素材**的排序规则，不是素材区全部结构，基础事实见 4a" | `grep -c "身份 / 外貌 / 背景" ...` ≥1；`grep -c "按需记录" ...` ≥1 | `1`；`1` | ✅ |
| **T-102** | DONE | §人物文档落盘 新增 **item 4b「素材的记录规则」**三条：① 外貌**只写特征不写美感**（不写"精致的脸""皮肤白皙"）② 关系用**具体场景**写不用定性词（写"她妈打电话来她会先深吸一口气再接"，不写"母女关系紧张"）③ 每句做**删除测试**（删掉这句角色变了吗，没变就删） | `grep -c "删除测试" 03 04` 至少一处 ≥1；`grep -c "只写特征" 03 04` 至少一处 ≥1 | `03:1 04:4`；`03:1 04:3` | ✅ |
| **T-110** | DONE | §题库/卡住处理 新增 item 6 + **「五类场景库」小节**：按用户回答难度排序 日常 → 关系 → 冲突 → 情绪 → 抉择，每档给"什么时候用"+ 一句示范问句；末尾加 ⚠️ **待泛化** 警告（示范问句出自线 A 现代都市恋爱样例库，写产物前按题材换场景、**难度序不变**） | `grep -c "日常场景 → 关系场景\|抉择场景" ...` ≥1；`grep -A3 "抉择场景" ... \| grep -c "待泛化"` ≥1 | `2`；`1` | ✅ |
| **T-111** | DONE | §人物文档落盘 item 1。文件名规则旁并列加落盘目录——"**落盘目录：写到 `workspace/<角色名>/` 下**"，形态要求改成"规则（文件名 + 目录两条并列）" | `grep -c "workspace/<角色名>" ...` 应 ≥1 | `1` | ✅ |
| **T-113** | DONE | §意象 新增 **item 2「操作化前置步骤」**，排在主问句 item 3 **之前**：AI 先自己从调色盘和行为模式里提取**核心矛盾**（没挖核心人格层时用**核心张力** = 两条颜色/两种行为倾向拉扯最厉害那一处），**然后再**问"在自然界里长什么样"；标明第 1 步是 AI 内部动作、第 2 步才出声 | `grep -n "核心张力" ...` ≥1 命中，且行号 **小于** `grep -n "她像什么"` 的行号 | 核心张力首命中 **L299**（另 L307）< 她像什么 **L308** | ✅ |
| **T-114** | DONE | §未定的 reference 新增 **「素材流向路由图」小节**：五类素材 → 送到哪 → 加工规则。其中**用户原始切片 → 混色画面：原样进，不加工**，并写明它是混色手册"禁止解释"那条判据的**上游保证**；基础事实那一行指向落盘 item 4a/4b | `grep -c "原样进混色\|素材流向" ...` 应 ≥1 | `3` | ✅（❓ 语义子句"给一句『她 22 岁，学美术，住在城中村』能否说清落到哪一格"：路由图末行已覆盖，仍需人工判） |
| **T-122** | DONE | §人物文档落盘 新增 **item 9a「方向变更处理」**四步执行程序：① 问旧素材还算不算数（带逐字问法）② 清除旧方向的**构建区**内容 ③ 保留用户确认仍属于角色的素材 ④ 回步骤 1 重新初始化；末尾负向约束——**方向变更不适用脚手架/推演降级，它不是卡壳**；并标明它是 item 9 的执行程序不是另一条原则 | `grep -c "方向变更" 03 02` 至少一处 ≥1；`grep -A5 "方向变更" ... \| grep -c "不是卡壳"` ≥1 | `03:1 02:2`；`1` | ✅ |
| **T-129** | DONE | §未定的 reference 案例库那一行由"整体卡住"改成"**内容形态已定，只等选例**"，并拆出 **「案例库的内容形态规格」小节**（六条）：① 展示追问过程不是方法论成品 ② 保留 2-3 个回合**原始对话片段** ③ 逐轮标注引擎动作 ④ **不放反例** ⑤ 结果对齐 body 判停标准 ⑥ **必须包含"用户否决"那个瞬间**；末尾加"只看形态不要照搬"警告 | `grep -c "案例库" ...` 从 1 升到 ≥2 且新增一个 `^### ` 级小节；`grep -A10 "案例库" ... \| grep -c "用户否决"` ≥1 | `案例库=3`，新增 `### 案例库的内容形态规格…`（L517）；`用户否决=1` | ✅ |
| **T-131** | DONE | §题库/卡住处理 新增 item 7 + **「完整性检查的六组维度」小节**：角色核心 / 性格质地 / 来源 / 内在驱动 / 关系 / **AI 可执行性**，标明"AI 内部对照，不外显"；加**不点名纪律**（不对用户说"你漏了 X 维度"）+ 三句补白示范问法 + 一句"AI 可执行性最容易被整组漏掉" | `grep -c "AI 可执行性" 02 03` 至少一处 ≥1；`grep -c "你漏了" 02` ≥1 | `02:2 03:3`；`02:1` | ✅ |
| **T-142** | DONE | §组装期一致性验证 抬头。"三种检查**三**时机 / 第三个时机"整体改成"**五时机 / 第五个时机**"，补上漏掉的两格——**2 推演前**（推演进入闸门 + 素材充分性）、**4 推演后**（候选差异性检查），并注明这两格落点在 `02` §9、本文件只登记存在；小节标题同步改名 | `grep -c "五时机\|五个时机" ...` ≥1；`grep -c "三种检查三时机" ...` 应为 0；`grep -c "P9\|P10\|P11\|P12" 06` 应为 4 | `2`；`0`；`06:4` | ✅ |
| **T-144** | DONE | §题库 节标题改成 **「题库 / 卡住处理」**；新增 item 5 + **「卡住模板」小节**（三套，照抄即用）：A 标签落地通用四问（`24问:23-28`，主语用「它」照原文）/ B 单个标签深挖四问 + 落成判据（`80问教程:36`+`:44-49`+`:57`）/ C 一堆标签同时来五问（`80问教程:1022-1028`）；末尾加 ⚠️ A 与 C 不要合并（差一条"她什么时候会停下"、第四问措辞也不同） | ① `grep -c "卡住模板" ...` ≥1 ② `grep -A8 "^## ✅ 题库" ... \| grep -c "^\| [0-9]"` 应从 4 升到 ≥5 | ① `3` ✅ ② **`1`** ❌ | ① ✅ ② ❌ —— **台账命令有误，见 §四-1**。等价复核：该表实际行数 `awk '/^## ✅ 题库/,/^\*\*十组目标声明/' ... \| grep -c "^\| [0-9]"` = **7**（原 4） |
| **T-145** | DONE | §未定的 reference 世界观锚定/NSFW 那一行，"内容本身定了吗"由"是"补成带线索：NSFW 动机追问 =「把'做什么'变成'为什么做'」· 世界观锚定 =「程度到'**够锚定角色**'就停」，并标明只剩归宿等冲突 C2 | `grep -c "够锚定角色" 03 10` 至少一处 ≥1 | `03:1`（`10:0`，越界不改） | ✅（"至少一处"已满足） |
| **T-149** | DONE | §用户确认信号 新增 item 5/6 + **「七个质量信号 A–G」整表**（A 行为级密度 / B 独有性 / C 新增性 / D 模式可见性 / E 修正精度 / F 自发延伸 / G 自然收束意愿，逐条给 查什么 + 频率 + 通过样例，全部锚定在用户说出来的话上）+ **组合规则**「**≥3 个质量信号通过，其中至少含 1 个 D/E/F/G**」；并加一句"七信号与组合规则的收录**不等冲突 C12**" | `grep -c "A–G\|A-G" ...` ≥2；`grep -c "≥3 个质量信号" ...` ≥1；`grep -c "U5" 06` ≥1 | `3`；`2`；`06:1` | ✅ |
| **T-150** | DONE | §人物文档落盘 item 3。索引头六字段后加负向约束——"**不设阶段字段**"，不写"我在第 3 阶段"，进度靠 🔄/📌/✅ 三档 + 断点记录表达；验收列补"检索'第 N 阶段'零命中" | `grep -c "不设阶段字段" ...` ≥1；`grep -c "D2a\|D2b" 06` 应为 2 | `1`；`06:2` | ✅ |
| **T-151** | DONE | 三处。① 通用规格「每份都要有的四件事」补**通用验收**：`06` `G10` 逐份查四件事齐不齐，并写明"这四件事是每一份的下限，各手册『必须包含』表是加项，没显式列出的按本节补齐"。② §调色盘 必须包含表**顶部插入 item 1 进入判据 / item 2 不适用判据**（三条不适用各带转出目标），原 1–7 顺延为 3–9 并加编号变更说明；质量门 item 7 由只有衍生判据扩成**三层各自的质量门**（底色/主色调/点缀色各一条可当场判的判据）。③ 给缺"触发"行的 7 份手册补上节头触发行（调色盘/多面性/混色/二次解释/意象/快速路径/人物文档落盘） | `grep -c "^> \*\*触发\*\*" ...` 应从 3 升到 **10**；`grep -c "^\| G10 " 06` 应为 1；`grep -A12 "^## ✅ 调色盘操作手册" ... \| grep -c "进入判据\|不适用"` ≥2 | `10`；`06:1`；`2` | ✅ |
| **T-152** | DONE | §未定的 reference 表末行。"4 诊断的外部问法与质量门 / 内容本身定了吗：**否**"改成"**内容部分已有**"，把线 C `diagnostics/4-basic-diagnostics-precise-criteria.md` 四句现成外部问法逐字搬进来（泛化 `:77` / 矛盾 `:242` / 结论 `:344` / 扁平 `:529`，全部保留"用 X 而非 Y"的对照形态），并写明另有 `§7.2` 语气映射 + `§7.3` 质量门、**缺的是数量不是有无**（通用规格要 2-4 句，现有 1 句） | `grep -rc "内容部分已有" 01 02 03 10` 四处均 ≥1；`grep -rc "4-basic-diagnostics-precise-criteria" SKILL交接包/*.md` ≥4 | `01:1 02:1 03:1 10:0`；`01:1 02:2 03:1 04:1`（合计 5） | ✅（`03` 侧）；`10` 那一处越界未改 |
| **T-153** | DONE（`03` 侧） | §核心人格层 item 14 素材列 `E1`–`E11` → **`E1`–`E12`**，并注明"`E12` 是角色台词自陈成因，`04`:203 已引用，**本条是它唯一的规格落点**" | `grep -c "E1\`–\`E12" ...` ≥1 | `1` | ✅（❓ 第二子句"素材 A–H 未被引用的节加『暂无落点』标注"= 改 `素材/核心人格层_原文素材.md`，越界未做，现为 0） |
| **T-154** | DONE | §核心人格层 四处。① item 2 排除判据：两版仍并列，但加死**执行时按 5 条版判定（取严）**，4 条版只作差异标注。② item 6 行为题补**选题判据**（优先不含术语且指向可观察动作的题、按素材原顺序取前 2-4 题、同部件问同一件事只取一条）。③ item 15 兜底出口写明"**交叉引用 item 6，不重列题目**"，验收列加"行为题那一环是指针不是第二份题表"。④ item 1 进入判据补**前置条件：调色盘已完成（至少已有主色调）**，依据素材 `B8-1`。⑤ 段位重排后 item 12 曾与另一条同号，把"凝练命名 vs 产出结论的分界"改编为 **item 16** 并加编号变更说明（旧号不动，`06` 与台账按旧号引用） | `grep -A2 "行为题，按部件分组" ... \| grep -c "选题\|取前"` ≥1；`grep -A2 "排除判据" ... \| grep -c "按 5 条版\|取严"` ≥1；`grep -A2 "进入判据" ... \| grep -c "调色盘已完成"` ≥1 | `1`；`1`；`1` | ✅ |
| **T-163** | DONE（临时措辞·最终等 §七 D-b） | §篇幅与量级 表 意象行。轻重列"**总是尝试**"改成"**⏳ 冲突 C12**（"总是尝试"与"必须尝试，允许跳过"都是教程空白，见 §意象「❌ 教程无依据」表；**未拍板前按"轻"处理**）"。**这是台账"改成什么"自己给的第一个选项（中性标注），不是替 D-b 拍板** | `grep -n "总是尝试" ...` 命中行号里不应有 37（或该行同时出现 "C12"）；`awk 'NR==37' ... \| grep -c "C12\|轻"` ≥1 | 命中行号 = `55 322 327`，**不含 37** ✅；`awk NR==37` = `0` ❌ | ① ✅ ② ❌ —— **台账命令写死行号，文件已增长，见 §四-2**。等价复核：意象行现在是 **L55**，该行同时含 "冲突 C12" 和 "按'轻'处理" |

---

## 三、P3（23 条）

| T 编号 | 状态 | 改了什么 | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|
| **T-300** | DONE | §篇幅与量级 表 调色盘行"这个数从哪来"列，"线 C 直接采纳"改成「线 C `probe-tag-compress`(~20) + `probe-why`(~20) **相加推导**，非原文」 | `grep -c "probe-tag-compress" ...` ≥1 | `1` | ✅ |
| **T-302** | DONE | §用户确认信号 来源行。"线 B `§4.3.2`（线 A/C 都没有）"扩成「`§4.3.2`（假信号 + 权重 + 七个质量信号 + 组合规则）+ `§5.1.3` / `E_SECTION 决策 4`（三类有效确认 + 可观察特征表 + 三条防作弊规则）+ `§2.5`（敷衍与诱导）」，并加一句"`E_SECTION 决策 4` 的表比本节完整，写产物时直接采纳那张表" | `grep -c "E_SECTION 决策 4" ...` ≥1 | `1` | ✅ |
| **T-303** | DONE | §快速路径 item 2 切换语补回前导句与问号：**"我们换个方式——闭上眼，你第一个想到的关于她的画面是什么？"**；形态要求写明"前导句和句末问号都要，`06` F3 查逐字，缺一个字不通过" | `grep -c "我们换个方式——闭上眼" ...` ≥1 | `1` | ✅ |
| **T-305** | DONE | §调色盘 诊断行。"**四层定义**（底色/主色调/点缀/衍生）"改成"**三层颜色定义**（底色 / 主色调 / **点缀色**）+ **衍生定义**"，逐字锚 `02_调色盘篇:33/35/37/43`，并加 ⚠️ 教程 `:31` 明写"调色盘有三层颜色"、衍生是下挂物、**不要写成"四层颜色"**、点缀那层教程用词是"点缀**色**" | `grep -c "四层定义" 03 06` 应为 0 | `03:0 06:0` | ✅ |
| **T-307** | DONE | 并入 T-024，见上 | 并入 T-024，无独立命令 | — | ✅ |
| **T-310** | DONE | §混色 item 2「模糊她的轮廓，让她变得拧巴」补锚 `4.md:184`，并注明大总结版 `04_混色篇:246` 只有前半句；节头出处锚块同处也标了这条 | `grep -A0 "拧巴" ... \| grep -c "4.md:184"` ≥1 | `2` | ✅ |
| **T-311** | DONE | §混色 item 4 验收列写明：教程自己的四步是**另一组**（`04:82/94/98/102`：①找会撞的颜色 ②写成画面 ③不要解释 ④不要每个瞬间都混色），本表把教程第 1 步拆成两个动作、第 4 步移到质量门 item 7，**引用时不要标"教程四步逐字"**；item 4 正文另加锚 `04_混色篇:82-104` | `grep -c "04:82/94/98/102" ...` ≥1 | `1` | ✅ |
| **T-312** | DONE | §多面性 诊断 1 第③项"三问否决清单"补锚 `2.md:53-61`，并注明**问句形态是本规格的操作化**——教程给的是三类**描述**不是三个问句，引用时不要标成"教程原文" | `grep -A0 "三问否决" ... \| grep -c "2.md:53-61"` ≥1 | `2` | ✅ |
| **T-316** | DONE | §快速路径 item 5 核心机制改用线 B `G1` **原话**：「用"**看**"代替"**拆**"，用"**变成她**"代替"**分析她**"」（锚 `11.另一种写法:189`）；并在同一行标明"把要求分析换成要求描述"是**交接包的转述**、不要当原话引 | `grep -c "要求分析" 03 06` 应为 0，**或同处出现"转述"** | `03:1`，该行同时含"转述" ✅（`06:1`，其 F5 行同样含"转述"） | ✅（走"或"分支） |
| **T-320** | DONE | §多面性 整节补锚：节头加 `> **出处锚**` 整块（三层触发 `03_三面性篇:70-74` / 排除项 `:78` / 五部件 `:163-177` / 过渡 `:145`+`:157` / 渗透 `:84`+`:179-201` / 语料移入 `:47` / 情境注入 `:64` / 不必选 `:11-13`，另标出**只在分散单篇**的 `2.md:23-31`、`2.md:53-61`、`2.md:414`、`2.md:446`、`2.md:444`），再把锚逐行内嵌到 item 1/2/3/4/5/6/7/8/8b/9/10/11 | `awk '/^## ✅ 多面性/,/^## ✅ 混色/' ... \| grep -c "三面性篇:\|2.md:"` ≥6（原 3） | `14` | ✅ |
| **T-321** | DONE | 并入 T-012（§混色整节零锚），见上 | `awk '/^## ✅ 混色/,/^## ✅ 二次解释/' ... \| grep -c "混色篇:\|4.md:"` ≥5（原 0） | `10` | ✅ |
| **T-322** | DONE | §二次解释 补锚：节头加出处锚块，item 1 `06_二次解释篇:23`/`:25`/`:29` · item 2 `:11-17` · item 3 `:37` · item 4 `:67` · item 5 `:144` · item 6 `:200` · item 7 `:231` 逐行内嵌 | `awk '/^## ✅ 二次解释/,/^## 🟡 意象/' ... \| grep -c "06_二次解释篇:\|06:"` ≥6（原 3） | `12` | ✅ |
| **T-323** | DONE | §意象 补锚：节头加出处锚块（前置提取 线 A `§2.23` + 线 C `questioning-moves.md:421` · 主问句 `07:27`/`:29` · 卡住手法 `07:33`/`:39`/`:67-73`/`:87`/`:107` · 截断外部套用 `07:141` · 写法四条 `07:139-162`（原则二 `07:147`）· 完成标准 `07:204-214` + 线 B `G3`），并逐行内嵌到 item 3/4/5/6/7 | `awk '/^## 🟡 意象/,/^## ✅ 题库/' ... \| grep -c "07:"` ≥6（原 5） | `9` | ✅ |
| **T-324** | DONE | §快速路径 补锚：节头加出处锚块（核心机制 `11.另一种写法:189` · 小传录像带规则与删词检查 `:193-205`+`:250` · 情绪阈值三要素 `:396-401` · 采访与说谎处理 `:352-394`），item 5 另内嵌 `11.另一种写法:189` | `awk '/^## ✅ 快速路径/,/^## ✅ 人物文档/' ... \| grep -c "11.另一种写法:\|11.md:"` ≥3（原 1） | `3` | ✅ |
| **T-325** | DONE | §题库 补锚：节头加出处锚块（三档定位 `200问附录题库:9-13` · 十组目标声明 `80问教程:243`…`:1219` · 升档判据 `24问:280-292`+`80问教程:1001-1012` · 三块补救 `24问:294-302` · 卡住模板 `24问:23-28`/`80问教程:36`+`:44-49`/`:1022-1028` · 抽题路由 `工作流:28-51` · 一次只问一个 `工作流:214-229`），并把四处教程内部出入那段补上具体锚 | `awk '/^## ✅ 题库/,/^## ✅ 用户确认信号/' ... \| grep -c "工作流:\|80问教程:"` ≥4（原 0） | `4` | ✅ |
| **T-326** | DONE | §人物文档落盘 14 条逐条补锚：item 1=线 B `L1`+线 A `§2.2` / 2=`L2` / 3=`L8` / 4=`L3` / 4a=线 A `§2.3` / 4b=线 A `§2.3` / 5=`L5` / 6=`L6` / 7=`L7` / 8=`L4` / 9=`L9` / 9a=`E_SECTION_DESIGN 决策 7` / 10=`L10` / 11=`§12.3` / 12=`§12.4`+线 A `§2.4` / 13=线 A `§2.4`+`L12` / 14=`L11` | `awk '/^## ✅ 人物文档落盘机制/,/^## ⏳ 未定/' ... \| grep -c "L[0-9]"` ≥14（原 2） | `16` | ✅ |
| **T-327** | DONE | 通用规格 §内部结构。段式骨架 +「必须物理分开」下方新增一行引用块出处锚：段式骨架与"必须物理分开"**逐字取线 C** `ARCHITECTURE_DECISION.md:80-85` | `grep -c "ARCHITECTURE_DECISION.md:80-85" ...` ≥1 | `1` | ✅ |
| **T-333** | DONE | §二次解释 冲突裁定段。删掉悬空编号引用"线 B 内部张力 **B/T25**"，改写成内容描述——裁定"全部写完后统一生成"同时消掉线 B `§4.3`（把"第一轮二次解释已生成"当 MVC 前置）与 `§5.1.3`（每层做完就做）之间的时序耦合；并留一句警告：那个编号在线 B 全仓不存在，回查请回查 `§4.3` 与 `§5.1.3` 本身 | `grep -c "B/T25" ...` 应为 0 | `0` | ✅ |
| **T-334** | DONE（`03` 侧） | §素材流向路由图 来源行。删掉悬空的融合项编号引用（原写"融合项 M-08 第三块"），换成内容描述 + 可回查的真锚（线 A `§2.13` 组装期一致性验证七条 + 素材流向；线 A `DESIGN_HANDOFF §2.19a/b/c` 推演前后两个时机），并写明 `M-` 系列编号在交接包五份文件和三条线原始文档里都查不到定义、**不要再往回加编号** | ❓`grep -on "M-[0-9]\+" 0*.md` 的每一处都要能在 `05` 查到定义 | `03` 里 M- 编号数 = **0**（原 2）；`05` 全文无 M 编号索引（`grep -c "M-08" 05` = 0），故 `03` 侧只能走"换成内容描述"这条路 | ✅（`03` 侧；`02`:2 / `04`:1 / `10`:5 仍有 M- 编号，越界未改） |
| **T-341** | DONE | §调色盘 质量门"每色 3-5 条衍生"的形态要求由"**数量下限**"改成"**区间**"，并注明教程 `02_调色盘篇:51` 给的是 3–5 的区间、**含上限 5**，验收列改"写成区间，不写成'至少 3 条'" | `grep -c "数量下限" 03 06` 应为 0 | `03:0 06:0` | ✅ |
| **T-342** | DONE | §人物文档落盘 item 5 三档状态第一档"🔄 正在挖"补字为"🔄 正在**深**挖"，验收列加"第一档逐字是'正在深挖'，不要掉字" | `grep -c "🔄 正在深挖" ...` ≥1 | `1` | ✅ |
| **T-348** | DONE | 并入 T-005。通用规格新增小节 **「各手册节头的『触发』行是规格元信息，不是产物内容」**：写明它是写给写作者看的元信息，产物形态是 `SKILL.md` body 里那行 `<触发信号> → 读 <文件名>` 指针（见 `02` §11a），**不要原样抄进 reference 正文**，正文里对应的是"进入判据"那条 | 并入 T-005，无独立命令；本表用 `grep -c "规格元信息，不是产物内容" ...` 复核 | `1` | ✅ |
| **T-349** | DONE | 通用规格新增小节 **「出处锚要不要写进产物」**，定死：**不写**（本文件与素材里的出处锚是给写作者回查用的，产物读者是执行的 AI，既回查不到也不需要）；唯一例外是产物里**逐字引用**教程原句时可在句末缀短锚；并与 §篇幅与量级 的"出处锚不单独占行"口径对齐 | `grep -c "出处锚" ...` ≥1（原 0） | `10` | ✅ |

---

## 四、台账本身写错的地方（4 处）

### 1. T-144 的第二条验收命令窗口太小，与 T-151 / T-325 结构性互斥

台账写的是 `grep -A8 "^## ✅ 题库" ... | grep -c "^| [0-9]"` 应从 4 升到 ≥5。

`-A8` 只能看到节标题往下 8 行。而同一批修复里另外两条条目要求这一节的节头**必须变长**：T-151 要求每份手册节头都有 `> **触发**` 行、T-325 要求整节补 `> **出处锚**`。三条一起做完，节头就占掉 素材行 / 触发行 / 出处锚行 + 空行 = 4 行，表头 + 分隔行再占 2 行，`-A8` 窗口里只剩得下第 1 行数据。**这三条不可能同时通过**，除非删掉触发行或出处锚行——而那样 T-151 / T-325 就挂了。

- 建议改成：`awk '/^## ✅ 题库/,/^\*\*十组目标声明/' SKILL交接包/03_references施工规格.md | grep -c "^| [0-9]"` 应 ≥5。
- 现在跑这条：**7**（原 4，新增 卡住模板 / 五类场景库 / 六组维度索引 三条）。

### 2. T-163 的验收把行号写死成 37，文件一长就必然失败

台账写 `awk 'NR==37' ... | grep -c "C12\|轻"` 应 ≥1。但本轮修复给这个文件净增了 100+ 行，其中通用规格（在篇幅表**之上**）新增了出处锚、触发行性质、C 编号命名空间三段，篇幅表整体下移。意象行现在是 **L55** 不是 L37。

- 建议改成：`grep -n "| 意象 |" ... | grep -c "C12\|轻"` 应 ≥1（跟行号解耦）。
- 现在跑这条：**1**。同一条验收的第一个子句（"命中行号里不应有 37"）本来就通过。

### 3. T-012 的"三处词表逐项一致"验收，比对的是整份文件的词频，不是那张词表

台账写：用 `grep -o "但.字转折\|破折号\|不是…而是…\|因为\|本质上\|其实\|内心" 03 | sort` 与 `04`/`06` 的同款输出比对，"三份结果应完全相同"。

问题是这几个词在三份文件的**正文散文里到处都是**——"因为"在 `04` 里出现 20 次、`06` 里 5 次、`03` 里 6 次，绝大多数跟混色词表无关。三份输出**永远不可能相同**，与词表同不同步无关。

- 建议改成对那一行做字面比对：`grep -c '并列成清单的情绪罗列 / "但"字转折 / 破折号 / "不是…而是…" / 因为 / 本质上 / 其实 / 内心' <三份文件>`，三处应均为 1。
- 现在跑这条：`03:1 04:1 06:1`，八项逐字一致。

### 4. T-016 的等式验收对"区间写法"有隐藏要求，台账没说

验收要求 `(素材|冲突|06) ?\`C[0-9]` 的命中数**等于** `` `C[0-9]+` `` 的命中数。这等于要求**每一个反引号 C 编号**都紧贴限定词。但 C 编号常以区间出现（`C1`–`C14`、`C3`–`C9`），按自然写法只有区间**起点**带限定词，**终点**会被判成裸编号，等式必然差几个。

台账"改成什么"只说"加限定词"，没说区间两端都要加。实际必须写成 `冲突 C1`–`冲突 C14` / `素材 C3`–`素材 C9` 才能配平。已按此处理并在文件抬头写明该约定。

> 另记一条**不算台账错误、但影响复现**的情况：`06_验收.md` 与 `00_START_HERE.md` 在本次执行期间被**并行会话**改动过（T-016 的 `06` 侧与 `00` 侧、T-144 的 `素材/_提取清单.md` 侧都从 0 变成命中）。本表"跑出来是什么"记的是**最后一次复跑**的结果。

---

## 五、汇总

| 状态 | 条数 | 说明 |
|---|---:|---|
| **DONE** | **49** | `03` 侧改动全部落地，`03` 侧验收子句全部通过 |
| SKIPPED-待拍板 | 0 | §七 六条拍板项（D-a…D-f）没有一条要求**在 `03` 里替用户做选择**；唯一沾边的 T-163 走的是台账自己给的"中性标注"选项，见下 |
| BLOCKED | 0 | — |
| MOOT | 0 | T-343 台账已判 MOOT，未计入 49 条 |

**验收未 100% 通过的 2 条**：T-144（第二子句）· T-163（第二子句）。两条都是**台账命令本身的问题**（§四-1 / §四-2），内容改动已到位，等价复核均通过。

**碰到 §七 的唯一一条**：T-163 挂在 D-b（意象"总是尝试" vs "必须尝试，允许跳过"）下。本轮**没有替用户选边**——只把篇幅表里当既定事实用的"总是尝试"改成 ⏳ 冲突 C12 的中性标注，这正是该条目"改成什么"给的第一个选项。**D-b 拍板后这一格还要再改一次。**

**验收里跨到别的文件、因授权范围未跑通的子句**（`03` 侧均已通过，仅登记）：

| T 编号 | 越界子句 | 现状 |
|---|---|---|
| T-024 | `05` 待拍板表 C12/L4 行 · `06` D8 · `10` §六之二 | 未改 |
| T-152 | `10_新会话接手指南.md` §7.3 第 1 行"内容部分已有" | `10:0` |
| T-153 | `素材/核心人格层_原文素材.md` 加"本节暂无规格落点"标注 | `0` |
| T-334 | `02`(2) / `04`(1) / `10`(5) 里仍有 `M-` 悬空编号；或在 `05` 补融合项编号索引 | 未改 |
