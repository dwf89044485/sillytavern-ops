# FIXLOG_06 · `SKILL交接包/06_验收.md` 按台账修补记录

> **范围**：只改 `SKILL交接包/06_验收.md` 一个文件。台账 `12_修复台账.md` 里"改哪"指向 `06` 的全部 TODO / SPLIT 条目 + 验收命令会检到 `06` 的条目 + §六 P3 表格里落点在 `06` 的行。
> **执行日**：2026-08-15。所有命令从 `/workspace` 跑，**原样照抄台账"验收"栏**。
> **台账 §七（D-a…D-f）列的待用户拍板项一律跳过。**
> **一条重要背景**：本轮执行期间 `04_硬约束与红线.md` 被另一条工作线大改（23 条硬边界 → **45 条**，拆了 H-19，S-10/S-11 升硬，新增 H-31…H-50），`02`/`03` 也在同步改。台账里若干条"现为 0 / `04` 侧未写"的现状描述已经过期，逐条在下表和 §四 标注。

---

## 一、逐条执行记录

| T 编号 | 状态 | 改了什么（新增哪条自检 / 改了哪条的什么） | 验收命令 | 跑出来是什么 | 通过? |
|---|---|---|---|---|---|
| **T-010** | **DONE**（`06` 侧） | §9 新增 **P8**「"给试探方向"处有模式前置条件」——判据：每处"试探方向"附近必须有"仅在脚手架模式下允许"+ 进脚手架判据"最近 3 轮追问没有产生新的行为场景或关系细节"+ 一次只给一个 + 明说是猜的；§4 新增 **S4-7**「不给平铺候选清单」——可计数判据"单轮并列待选方向 ≥3 条 → 违反"，唯一例外"摆素材反推"。两条本轮已能引到 `04` 编号（H-31 / H-32，`04` 侧已落地） | `grep -A3 -B3 "试探方向" SKILL交接包/04_硬约束与红线.md \| grep -c "脚手架"` 应 ≥1；`grep -c "H-31\|H-32" SKILL交接包/04_硬约束与红线.md` 应 ≥2；`grep -n "最近几轮" SKILL交接包/04_硬约束与红线.md` 应 0 命中 | `8` / `6` / 0 命中 | ✅（三项全过；`04` 侧由另一条线完成） |
| **T-012** | **DONE**（`06` 侧） | §六 **X5** 改成"禁止词表**八项**齐全 + 判的是**句式不是个数**，不得写成数量阈值 + 八项须与 `03` 混色 item 8 / `04` H-15 逐项一致，改完做一次字面比对" | `grep -c "三个以上情绪名词" SKILL交接包/0[346]*.md` 三处均应为 0 | `03`:0 `04`:0 **`06`:0** | ✅ |
| **T-015** | **DONE** | §9 **P5 拆成 P5a / P5b**：P5a =「四条围栏逐字齐全（对照素材 `G` 段）」；P5b =「围栏 3、4 写成可检索形式」，并写明**围栏 1、2 是原则不要求机械化** | `grep -c "P5a\|P5b" SKILL交接包/06_验收.md` 应为 2 | **2** | ✅ |
| **T-016** | **DONE**（`06` 侧） | 抬头新增「⚠️ "C+数字"有三套命名空间，不要串」表（冲突 C1–C14 / 素材 C1–C14 / 06 C1–C21）+ 规定"每一处 C 编号必须带限定词"+ 给出自检检索式；全文裸 C 编号补限定词（C7 → 素材 `C3`/`C9`，C15 → 素材 `C12`，C18 → 素材 `C10`，其余改写为"冲突 C3/C5/C12/C14"） | `grep -c "三套命名空间\|三套" SKILL交接包/00_START_HERE.md` 应 ≥1；`grep -on '\(素材\|冲突\|06\) \?\`C[0-9]' 03 06 \| wc -l` 应等于 `grep -on '\`C[0-9]\+\`' 03 06 \| wc -l` | `1`；**16 == 16**（原 0 vs 13） | ✅ |
| **T-017** | **DONE** | §8 **S8-1** 改成「标"逐字"的必须与素材逐字一致，标"转写"的必须注明来源」，删掉原来的"逐字原句不是转写"；并加⚠️ 深度轴第五层素材零原句，未显式登记前只查"有没有登记" | `grep -n "逐字原句.*不是转写" SKILL交接包/06_验收.md` 应 0 命中 | **0 命中** | ✅ |
| **T-018** | **DONE** | §三 行为测试第 3 条备注改写：明写「`03` 核心人格层 item 14 的"反面判据"判的是**产出物形态，不是用户输入**」——用户说成因式句子按 `04` H-02 收下当素材、不打回，只有出现在产出物里才算命中 | `grep -A3 "第三个测试" SKILL交接包/06_验收.md \| grep -c "item 14"` 应 ≥1 | **1** | ✅ |
| **T-019** | **DONE** | §一 **G2** 改成三类判定：(a) 规格强制逐字引用的素材原文（`A2`、`E1`–`E12`）允许 · (b) 推导中间步骤允许 · (c) 产出物形态禁止。只有 (c) 不合格 | `grep -c "强制逐字引用的素材原文\|强制引用的素材" SKILL交接包/06_验收.md` 应 ≥1 | **2** | ✅ |
| **T-020** | **DONE** | §三 **C9** 补可判判据：凝练命名 = 把用户上一轮说过的词重组交回确认；产出结论 = 引入用户没说过的判断。判的是「**Y 的每个成分能不能在用户原话里找到**」；并要求用素材 `B2`（应不通过）/`B3`（应通过）各跑一次校准 | `grep -A1 "C9" SKILL交接包/06_验收.md \| grep -c "用户原话"` 应 ≥1 | **1** | ✅ |
| **T-021** | **SKIPPED** | 验收命令三项全部只检 `04`，`06` 侧无落点（H-19 拆分后 S6-6 要跟着拆，已登记进 §十四 待办 1） | 三项均针对 `04_硬约束与红线.md` | `^### S-` 已为 **0**、`H-19a/b/c` 已为 **3** —— `04` 侧已由另一条线做完 | — （不在本文件范围） |
| **T-023** | **DONE** | §五 新增 **M11**「每张面的"功能"都有防御机制来源，或显式标为待回填」——凭空编功能 → 不通过（会踩 `04` H-12 且很难被发现） | `grep -c "M11" SKILL交接包/06_验收.md` 应 ≥1 | **2** | ✅ |
| **T-024** | **SKIPPED** | 等 **D-a/L4 拍板**。`06` 侧只做了不越权的部分：**D8** 保留"有没有注释"这一查，注释**措辞**标 ⏳ 等 L4（不在 `05` 自认没定的同时在这里定死） | 命令检 `05_已定决策速查.md`，超出本文件范围 | — | — （台账 §七 关联项） |
| **T-025** | **DONE** | §一 **G1** 改成**引用 `04` H-01 的词表，本表不重抄**（两处各持一张表是最容易漂移的形态）；并加 🔍 裸词误报规则：「依恋」必须带"理论"二字才算命中（`1.入门:302` 有小节标题 `# 依恋衍生`）。`04` 已把「心理学」并入 H-01 词表，G1 里那半句临时检索**本轮已删** | `grep -A1 "G1" SKILL交接包/06_验收.md \| grep -c "H-01 的词表\|见 \`04\` H-01"` 应 ≥1；`grep -c "心理学" SKILL交接包/04_硬约束与红线.md` 应 ≥1 | **1**；`04`:**4** | ✅ |
| **T-026** | **DONE**（`06` 侧） | 新增 §二 **10. 4 个基础诊断与矛盾路由**：**S10-1** 要求矛盾检测的两条分流规则写成四列决策表（跨场景矛盾 → 多面性线索 → 收束时激活 / 同瞬间矛盾 → 当场追混色画面）；**S10-2** 🏃 运行时路由测试。节头写明**本节不被冲突 C3/C6 阻塞** | `grep -c "跨场景矛盾\|同瞬间矛盾" SKILL交接包/02_SKILL.md施工规格.md` 应 ≥2；两个输入能路由到不同手册 ❓ | `02`:**2**；`06` 侧静态查 S10-1 两行在纸上 ✅，真跑待 body 完成 | ✅（❓ 那半留给运行时） |
| **T-028** | **SKIPPED** | **等 §七 D-f 用户拍板。** 未动 G8 的 `H-27` 引用。只更新了 §十四 里的事实描述：`04` 已把这条内容落成 **H-41**，编号归属仍等 D-f | `grep -c "H-27" 04 06` | `04`:**3**（说明性提及，`^### H-27` 仍为 0）`06`:**2** | — （拍板前不动） |
| **T-029** | **DONE** | §一 表末加注「**编号只增不改**」——`05` D-02 落点表按编号引用本表（G9、S5-3/S5-4），改号会打断双写链路；新增自检一律取下一个未用号。`G9` 保持原义，新增项从 **G10** 起 | `grep -c "^\| G9 " SKILL交接包/06_验收.md` 应恒为 1；`grep -c "^\| G10 " SKILL交接包/06_验收.md` 应为 1 | **1** / **1** | ✅ |
| **T-107** | **DONE**（`06` 侧） | §9 新增 **P11**「候选差异性检查」——产出 2-3 个候选后"**遮住框架名和术语只看行为**"，判是不是同一件事换了说法；是 = 不合格重推 | `grep -c "差异性检查" 02 06` 两处均应 ≥1 | `02`:**2**（另一条线已补）`06`:**1** | ✅ |
| **T-115** | **DONE**（`06` 侧） | §5 新增 **S5-5**「一次一个交互单元」，可计数判据「**一轮输出里的问句数 ≤1**」 | `grep -c "问句数 ≤1\|问句数量" SKILL交接包/06_验收.md` 应 ≥1 | **1** | ✅ |
| **T-120** | **DONE**（`06` 侧） | §6 新增 **S6-4**「三层判停框架齐全」——步骤级 / 轮次级 / 里程碑级三层都在，**且遮名测试挂在轮次级下面**（它只是轮次级里的一个判据，不是全部） | `grep -c "步骤级" 02`、`grep -c "里程碑级" 02` 应均 ≥1；三情形路由 ❓ | `02`:**2** / **3**（另一条线已补） | ✅ |
| **T-121** | **DONE**（`06` 侧） | §6 新增 **S6-5**「单条追问判停的三条 AND 条件 + 换切面顺序逐字写出 **正面 → 反面 → 侧面 → 关系面**」 | `grep -c "正面 → 反面 → 侧面 → 关系面" 02` 应 ≥1；`grep -c "AND" 02` 应 ≥2 | `02`:**1**（另一条线已补） | ✅ |
| **T-128** | **DONE**（`06` 侧） | 新增 §二 **0. metadata** 整节，9 条自检 **S0-1…S0-9**：frontmatter 两字段 / 五类触发场景 / ⏳ 与 `char-creator` 撞车等冲突 C14 / 万能句收窄 / 🔍 删掉"角色"二字仍剩 ≥2 个信号 / 三个兄弟 skill 边界信号各一条 / **中英双写** / **内部术语零命中** / 灰区与误触发热点各四项 | `grep -c "metadata" SKILL交接包/06_验收.md` 应 ≥1 | **2** | ✅ |
| **T-130** | **DONE**（`06` 侧） | §1 新增 **S1-4**「活人 > 正确」；§5 新增 **S5-6**「只整理不总结」——收束句式须复述用户说过的具体场景和行为、明文禁止归纳句，检索"**所以她是**"应为 0 命中；并注明 `04` H-12 第 4 行管的是**时机**，管不到确认之后的**句式** | `grep -c "所以她是" 02 06` 两处均应 ≥1 | `02`:**1** `06`:**1** | ✅ |
| **T-140** | **DONE**（`06` 侧） | 新增 §二 **13. 边界与路由** 整节，7 条自检 **S13-1…S13-7**：段本身存在 / 三个兄弟 skill 的**运行时判据**各一条（⏳ 切换条件等 D-e）/ "帮我生成"的拦截措辞 + 替代动作 / 四条不触发判据 / **七项覆盖盲区**逐项 / 整体退出路径三个出口 / 🏃 三输入三处理 | `grep -c "^\| 13 \|" 02` 应为 1；`grep -c "边界与路由" 02` 应 ≥2 | `02`:**1** / **2**（另一条线已补） | ✅ |
| **T-141** | **DONE**（改到位，命令本身有 bug） | §7 新增 **S7-4**「五种入口各有第一动作」——数一次，第一动作条数必须等于入口条数 = **5** | ① `grep -c "S7-4" 06` 应 ≥1 ② `grep -A6 "^### 7. 入口意识" 06 \| grep -c "^\| S7"` 应 ≥4 | ① **1** ✅ ② **3** ❌ | ⚠️ ②不过——**台账命令 off-by-one**，见 §四 |
| **T-142** | **DONE**（`06` 侧） | §9 新增 **P9**（推演进入闸门三条，满足任一即可）**P10**（素材充分性闸门：三句话说不清楚 = 素材不够）**P11**（候选差异性检查）**P12**（候选带槽位标注，与 P3 标来源是并列两条不是二选一） | `grep -c "P9\|P10\|P11\|P12" SKILL交接包/06_验收.md` 应为 4 | **4** | ✅ |
| **T-148** | **DONE**（`06` 侧） | §4 **S4-1 拆成 S4-1a / S4-1b**：a = 边界表四行齐全且左右两列都有内容；b = **边界表与三条铁律是并列两块**，不是同一张表的四行 | `grep -c "S4-1a\|S4-1b" SKILL交接包/06_验收.md` 应为 2 | **2** | ✅ |
| **T-149** | **DONE**（`06` 侧） | §十 新增 **U5**「七个质量信号 A–G 齐全 + 组合规则的数量判据」，组合规则要逐字写出「**≥3 个质量信号通过，其中至少含 1 个 D/E/F/G**」 | `grep -c "U5" SKILL交接包/06_验收.md` 应 ≥1 | **1** | ✅ |
| **T-150** | **DONE**（`06` 侧） | §十二 **D2 拆成 D2a / D2b**：a = 索引头 20 行硬上限 + 六字段齐全；b = **没有阶段字段**（检索"第 N 阶段""我在第""当前阶段"应为 0 命中，进度靠 🔄/📌/✅ 与断点记录表达） | `grep -c "D2a\|D2b" SKILL交接包/06_验收.md` 应为 2 | **2** | ✅ |
| **T-151** | **DONE** | §一 新增 **G10**「这份 reference 的四件事齐全」——进入判据 / **不适用判据** / 外部问法 / 质量门，四个小节各查一次存在性，缺任一项不通过；并标注"不适用判据"最易漏。**按台账要求避开了 G9**（已被 D-02 占用） | `grep -c "^\| G10 " SKILL交接包/06_验收.md` 应为 1 | **1** | ✅ |
| **T-156** | **DONE**（`06` 侧，双向对齐的另一半登记为待办） | 按 AUDIT5 §3.3 补齐 8 条无自检的硬约束：**G13**(H-13 示范三约束) **G15**(H-14 对产物本身跑遮名测试) **S4-5**(H-18) **S6-6**(H-19) **G16**(H-20 情绪串只做防御性纠偏) **G11**(H-21 四禁) **S4-6**(H-22) **G14**(H-23) **G12**(H-24)；§十三 加元规则 2「`04` 每条硬约束必须有自检，每条自检必须有来源，单边即缺口」；§十四 建对照表 | `grep -c "H-1[3489]\|H-2[01234]" SKILL交接包/06_验收.md` 应 ≥8 | **20**（原 0） | ✅（"对照表无单边条目"❓ 部分见 §三） |
| **T-157** | **DONE** | 按 AUDIT5 §3.4 补齐 `02` 缺的 7 条 + `03` 缺的 22 条（明细见下 §二）；§十三 元规则 1 从"每新增一节规格"改成「**每一条规格条目都必须有一条自检**；新增规格时同步新增；**上线前跑一次全量对齐**」 | ① `grep -c "每一条规格条目" 06` 应 ≥1 ② 自检项总数比原来增加 ≥29 | ① **1** ✅ ② 台账口径 **193**（AUDIT5 记录的基线是 **120**，+73） ✅ | ✅ |
| **T-158** | **DONE** | **G6** →「三样东西在 reference 里**逐字复现**即违反：深度轴五层名 / 遮名测试原句 / `04` H-12 四行边界表；概念提及不算」+ body 未定段挂起；**B00** → 行数是估算不是上限（D-a），改查**记录动作做没做**：文件头写了实际非空行数 + 超预估 2 倍时有没有"已回查有无可外置内容"的说明，⏳ 第 10 段进不进 body 卡冲突 C3；**S5-1** → 每组 ❌ 必须能在教程 / 线 B 失败模式 / 线 C 诊断触发案例里找到实例**并标注出处**，有出处即通过 | `grep -c "与 body 对照" 06` 应为 0；`grep -c "不是稻草人" 06` 应为 0；`grep -A0 "B00" 06 \| grep -c "⏳\|≤ 300"` 应 ≥1 | **0** / **0** / **1** | ✅ |
| **T-159** | **DONE**（`06` 侧） | 抬头改成**三档 + 一个诚实标记**：*无标记* 可机械判定（默认档）· 🔍 半机械 · 🏃 运行时测试 · ❓ 只能靠人看；写明分档口径是"**能不能真跑**"（AUDIT3 口径）不是"判据写没写清楚"；逐条打标记；**行为测试单独分区**并注明静态阶段只能查规则在不在纸上；新增 §十五 统计与重跑命令 | `grep -c "全部可机械判定" 10` 应为 0；`grep -c "分三档\|半机械" 06 10` 两处均应 ≥1 | `06`:**3** ✅ ; `10`:**0** ❌ 且 `10` 的"全部可机械判定"仍为 **1** | ⚠️ `06` 侧过，`10_新会话接手指南.md` 不在本文件范围 |
| **T-305** | **DONE**（`06` 侧） | §四 **T1** 改成「**三层颜色定义**（底色 / 主色调 / **点缀色**）**+ 衍生定义**」，并注明教程 `:31` 明写"三层颜色"、衍生是下挂物不是第四层、用词是"点缀**色**" | `grep -c "四层定义" 03_*.md 06_*.md` 应为 0 | `03`:**0** `06`:**0** | ✅ |
| **T-316** | **DONE**（`06` 侧） | §十一 **F5** 改成要求**用线 B `G1` 的原话**「用"**看**"代替"**拆**"，用"**变成她**"代替"**分析她**"」；"把要求分析换成要求描述"判为**转述**，只有同处标"转述"并给例子锚 `11.md:189` 才通过 | `grep -c "要求分析" 03_*.md 06_*.md` 应为 0，**或同处出现"转述"** | `06`:1 命中，**同一行含"转述"** → 走替代分支 | ✅ |
| **T-341** | **DONE**（`06` 侧） | §四 **T6** 改成「每色衍生 **3–5 条**，是**区间**（含上限 5）。写成"至少 3 条"或只给下限 → 不通过」 | `grep -c "数量下限" 03_*.md 06_*.md` 应为 0 | `03`:**0** `06`:**0** | ✅ |
| **T-344** | **DONE** | §6 **S6-2** 补「⏳ 第 10 段卡**冲突 C3**，未定前只查有没有留一行**占位**注释（如 `<!-- 若 4 诊断进 body，此处与 §10 交叉引用 -->`），有占位即通过」 | `grep -A0 "S6-2" 06_*.md \| grep -c "占位"` 应 ≥1 | **1** | ✅ |
| **T-345** | **DONE** | §三 **C11** 保留"或等价可执行描述"，另起一张**四判例表**（两正两反）：✅`有人靠得太近的时候 → 她先退半步再说话`、✅`被夸的时候 → 她把话题岔到别人身上`、❌`她缺乏安全感`、❌`因为童年缺爱，所以她害怕亲密` | `grep -A2 "C11" 06_*.md \| grep -c "✅\|❌"` 应 ≥4 | **4** | ✅ |
| **T-347** | **DONE** | §三 **C8** 给五环各一个可识别标志：① 摆素材 = 出现 **≥3 条**已确认行为的列举 ② 问 = 列举后紧跟一个问号 ③ 不合格再追一层 = 第二个问号且比第一问更具体 ④ 用户说出词 = 回合里有用户台词含新名词/短语 ⑤ AI 凝练命名 = "你说的这两样，就可以叫 X，对吗"句式。整条标 🔍（③④ 要人判一次） | `grep -A0 "C8" 06_*.md \| grep -c "≥3 条"` 应 ≥1 | **1** | ✅ |
| **T-013 / T-014** | **已 DONE（本轮只复跑验收，未改动）** | I5「承载**关系**」+ 把"承载主要矛盾/核心矛盾"写成否决条件；I1/I2 标"⏳ **删除**，等冲突 C12" | `grep -c "承载关系" 06` 应 ≥1；`grep -c "⏳ \*\*删除\*\*" 06` 应为 2 | **2** / **2** | ✅ |

---

## 二、T-157 的 29 条明细（规格有、验收原来没有）

**`02` 缺的 7 条 → 本轮新增：**

| `02` 规格条目 | 新增自检 |
|---|---|
| §1 item 2 好坏的唯一标准 | **S1-3** |
| §2 item 1/2 两句话定义对照 | **S2-3** |
| §3 item 2 深度轴的用法句 | **S3-3** |
| §4 item 2 用户说"不知道"的三个替代动作 | **S4-4** |
| §4 item 4 不一次性砸完整档案 | **S4-5** |
| §6 item 2 判停的用法句 | **S6-3** |
| §7 item 2 五种入口各自的第一动作 | **S7-4** |

**`03` 缺的 22 条 → 本轮新增：**

| `03` 规格条目 | 新增自检 |
|---|---|
| 通用规格 · 每份 reference 的**四件事**（结构性缺口） | **G10** |
| 核心人格层 item 1 进入判据（素材 `F4`） | **C16** |
| 核心人格层 item 4 部件间依赖 | **C17** |
| 核心人格层 第 2 段通用推进短句（素材 `C10`） | **C18** |
| 核心人格层 item 11 整层硬要求（素材 `D8`） | **C19** |
| 核心人格层 item 13 成品参照逐字（素材 `A2`） | **C20** |
| 核心人格层 N4 不讲"AI 会怎么误读" | **C21** |
| 调色盘 item 4 三层骨干问句 | **T9** |
| 调色盘 item 5 衍生的三种失败形态 | **T10** |
| 多面性 item 7 骨干问句 + 极端假设情境注入 | **M12** |
| 多面性 item 8 附注 五部件的后果句 | **M13** |
| 混色 item 4 四步操作 | **X8** |
| 混色 item 6 "两种可能性住在同一个动作里" | **X9** |
| 二次解释 item 3 怎么找误读点 | **E6** |
| 二次解释 item 4 常见误读模式清单 | **E7** |
| 意象 item 5 卡住时的手法 | **I7** |
| 意象 item 7 完成标准四项 | **I8** |
| 题库 item 3 升档判据**数一次是不是七条** | **Q3**（改） |
| 快速路径 item 7 采访三项 | **F10** |
| 快速路径 item 11 引导强度动态调整 | **F11** |
| 落盘 item 1 文件名与标题用角色名 | **D14** |
| 落盘 item 10 自然语言投影确认 | **D15** |
| 落盘 item 14 最终卡导出用户手动触发 | **D16** |
| 组装期一致性验证 7 条（无验收节） | **V1–V8**（§十三 新增整节） |

---

## 三、改完后 `06` 的自检项统计

命令（`06` §十五 已内置，从 `/workspace` 跑）：

```bash
grep -cE "^\| [A-Z][0-9]+[a-z]?(-[0-9]+[a-z]?)? \|" SKILL交接包/06_验收.md          # 总数
grep -cE "^\| [A-Z][0-9]+[a-z]?(-[0-9]+[a-z]?)? \|.*🔍" SKILL交接包/06_验收.md      # 半机械
grep -cE "^\| [A-Z][0-9]+[a-z]?(-[0-9]+[a-z]?)? \|.*🏃" SKILL交接包/06_验收.md      # 运行时
grep -cE "^\| [A-Z][0-9]+[a-z]?(-[0-9]+[a-z]?)? \|.*❓" SKILL交接包/06_验收.md      # 只能靠人看
```

| 档位 | 条数 | 占比 |
|---|---:|---:|
| **可机械判定**（无标记，默认档） | **178** | **89.4%** |
| 🔍 半机械（命令定位 + 每处人工判一次） | 18 | 9.0% |
| 🏃 运行时测试（静态阶段只能查规则在不在纸上） | 2 | 1.0% |
| ❓ 只能靠人看 | **1** | 0.5% |
| **总数** | **199** | 100% |

> **对比基线**：AUDIT5 记录改前为 **120 条**（其中 🔴 靠感觉 4 条）。本轮 **+79 条**，满足 T-157 要求的 ≥29。
> **台账自己的计数式**（`grep -c "^| [A-Z][0-9]*\(-[0-9]*\)\? |"`）跑出 **193**，比 199 少 6 —— 它匹配不到带字母后缀的拆分项（S4-1a / S4-1b / D2a / D2b / P5a / P5b）。两个数都对，口径不同。

**🔍 半机械 18 条**：G1 · G2 · G5 · G6 · G8 · G11 · G13 · G15 · S0-5 · S1-1 · S4-3 · P6 · C6 · C8 · C9 · Q1 · F1 · F6
**🏃 运行时 2 条**：S10-2 · S13-7（另有 §三 末的 3 条行为测试不是 `| 编号 |` 形态，不计入总数，同为 🏃 档）
**❓ 只能靠人看 1 条**：**B0**（分界测试）——"拿掉它 AI 是每轮都可能犯错还是某阶段才犯"没有任何命令能定位该查哪几条，只能从头读一遍逐条判。如实标注，不假装可机械检查。

---

## 四、台账本身写错 / 已过期的地方

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| 1 | **T-141 验收②** `grep -A6 "^### 7. 入口意识" ... \| grep -c "^\| S7"` 应 ≥4 | **off-by-one。** 节头之后依次是：空行 / 表头 / 分隔行 / S7-1 / S7-2 / S7-3 / **S7-4**——S7-4 落在第 **7** 行，`-A6` 的窗口够不到它。所以哪怕补了第 4 条，这条命令**永远只能跑出 3**。应改 `-A7`（实跑 `-A7` = **4** ✅） | 唯一一条改到位却验收不过的；已按台账口径如实记 ❌ |
| 2 | **T-158 验收③** `grep -A0 "B00" ... \| grep -c "⏳\|≤ 300"` 应 ≥1 | 这条**早于 D-a 拍板**写的：它给的两个出口是"标 ⏳ 挂起"或"给临时上限 ≤ 300 行"，而 D-a 已经拍了**行数是估算不是硬上限**，两个出口都不再适用。本轮的过法是 B00 里有一处**真实**的 ⏳（第 10 段进不进 body 卡冲突 C3，预估基准会变），不是为了凑 grep | 命令能过，但**过的理由和台账设想的不是一回事**，下一个人别照着"≤ 300"去改 |
| 3 | **T-157 验收②** "总数应从当前 `grep -c ...` 的值增加 ≥29" | **没有把基线数字写进台账**，只给了"当前值"这个会随文件变化的表达式。改完之后无法复算基线（除非翻 git）。本轮用 AUDIT5:493 记的 **120 条** 当基线 | 建议台账把基线数字钉死 |
| 4 | **T-016 验收②** `grep -on '\(素材\|冲突\|06\) \?\`C[0-9]'` 与 `grep -on '\`C[0-9]\+\`'` 比对 | 这条检索式**要求限定词和反引号紧邻**，所以三种写法会误判：① 加粗的限定词 `**冲突** \`C1\`` 不算命中 ② 区间写法 `\`C1\`–\`C14\`` 的后半个永远不算命中 ③ 用来举反例的裸编号会被算成违规。本轮的处理是：**抬头的命名空间定义表和举例一律不加代码格式**，并把这条约定写进文件，否则它会污染自己的检索 | 命令可用，但要配一句约定才闭合 |
| 5 | **T-010 / T-015 / T-021 / T-025 的 `04` 侧** 全部标 TODO | **已经被另一条工作线做完了**：`04` 现有 `H-31`/`H-32`（6 处命中）、`H-19a/b/c`、`^### S-` 已为 0、H-01 词表已并入「心理学」、"每条都可机械检查"/"删词测试，可机械执行"两处虚假断言已 0 命中。台账状态栏没跟上工作树 | 会导致下一个人重做；本轮据此把 `06` 里对应的 ⏳ 占位（G1 的临时检索半句、P5b 的提醒、P8/S4-7 的"红线未落"）**改成了正式引用** |
| 6 | **T-028 的现状描述** "`04` 里没有这一条" | 已过期。`04` 现在把 H-27 的内容落成了 **H-41**（内部诊断与外部问法必须物理分开），并在 `04`:779 显式登记"编号归属等 D-f"。`04` 侧 `grep -c "H-27"` 现在是 **3**（都是说明性提及，`^### H-27` 仍为 0），台账里的 (a)/(b) 判定式会误读成"已经补了 H-27" | D-f 拍板时要按新事实描述；本轮只更新了 `06` §十四 的事实说明，**未动 G8 的引用** |
| 7 | **T-026 / T-120 / T-140 的"现为 0"** | 也已过期，`02` 侧都已补上（跨场景矛盾/同瞬间矛盾 = 2、步骤级 = 2、里程碑级 = 3、`^\| 13 \|` = 1） | 同 5 |
| 8 | **T-012 验收的三表逐项比对式** | `grep -o "但.字转折\|破折号\|因为\|本质上\|其实\|内心\|不是…而是…" … \| sort` 三份比对 —— 这条**永远不可能全等**：`因为`/`其实`/`内心` 是三份文件正文里的常用词，检索会把散文里的每一次出现都数进去（实跑 `因为`：`03` 6 次 / `04` 20 次 / `06` 5 次）。**去重后的词集合三份是一致的**（各 7 项），这才是这条想验的东西。本轮按可执行的那一半（数量阈值零命中）判定，并在 X5 里写明"八项必须与 `03` 混色 item 8、`04` H-15 **逐项一致，改完做一次字面比对**"作为人工步骤 | 建议台账把这半条改成"`sort -u` 后的词集合三份相同"或"八项逐项存在性"，别用带重复计数的 `sort` |

---

## 五、跳过的条目

| 条目 | 为什么跳 |
|---|---|
| **T-028** | 台账 §七 **D-f**（`H-27` 保留还是删除）待用户拍板。G8 的引用原样保留 |
| **T-024** | 台账 §七 **D-a / 确认项 L4** 待拍板。`06` D8 只查"有没有注释"，**注释措辞标 ⏳ 不定死** |
| **T-021** | 验收命令三项全检 `04`，`06` 侧的落点（S6-6 跟着 H-19 拆三条）属于"等 `04` 定稿后的双向对齐"，已登记进 `06` §十四 待办 1 |
| **I1 / I2 的恢复** | 卡**冲突 C12** + §七 **D-b**。两条维持"⏳ 删除"，不恢复成实际自检 |
| **E4** | 与教程 `06:44` 明文冲突，`06` 原文已标"🟡 待裁定，暂不作为通过条件"，本轮不动 |
| **S0-3**（description 与 `char-creator` 撞车） | 等**冲突 C14**，标 ⏳ 不作为通过条件 |
| **`10_新会话接手指南.md` 的"全部可机械判定"**（T-159 前半）· **`02`/`03`/`04`/`05`/`00` 的所有对侧改动** | 不在本文件范围（硬约束：只改 `06_验收.md`） |

---

## 六、`06` 结构变化速览

| | 改前 | 改后 |
|---|---|---|
| 节数 | §一–§十三 | §一–**§十五**（新增 §十四 待与 `04` 对齐、§十五 自检项统计） |
| `§二` body 段 | 全局 + 1–9 段 | 全局 + **0. metadata** + 1–9 段 + **10. 矛盾路由** + **13. 边界与路由** |
| 自检项 | 120 | **199** |
| 分档 | 无（抬头声称"全部可机械判定"） | **三档 + ❓ 诚实标记**，逐条打标 |
| 与 `04` 的关系 | 各持一张词表，8 条硬约束无自检 | G1 **引用** `04` H-01 不重抄；11 条硬约束已配自检；其余登记为 §十四 双向对齐待办 |
