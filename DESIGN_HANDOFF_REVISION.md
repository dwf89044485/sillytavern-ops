# DESIGN_HANDOFF 修订意见

> 本文件是对 `DESIGN_HANDOFF.md` 的修订建议。基于三次独立审查和后续讨论形成：
> - 审查 A（主审查）：完整阅读 handoff + 5 篇核心教程，形成独立判断
> - 审查 B（subagent）：阅读实战教程 + 大总结 6 篇，独立形成判断
> - 审查 C（subagent）：阅读进阶教程 5 篇，独立形成判断
> - 三轮审查后，三方汇总、讨论，逐条修订。

---

## 一、审查发现的核心问题

### 1.1 总设计规模偏大

**原 handoff §3-4：** 计划四批产出，第一批 SKILL.md + 3 个模板，第二批 4 个 reference，第三批 3 个 reference，第四批 2 个 reference + 示例库。handoff 本身约 510 行，预计 skill 总产出 2000+ 行。

**问题：**

教程的核心洞察可以一句话说完（"不要写标签，写行为"）。handoff 的翻译方式是把"人看的教程"换了一种写法变成"AI 看的教程"——信息结构没有变，只是换了个读者。2000+ 行的 skill 中，大量内容是教程里的解释、论证、教学案例的翻版。

skill-creator 的核心原则是"context window is a public good"——skill 共享整个系统的上下文预算。教程里的解释和论证是给人建立心智模型用的，AI 不需要。AI 需要的是**判据、流程、边界**，不是"为什么调色盘比标签好"的长篇论证。

**审查中的判断：**

- 审查 A 指出："教程缺少'最小可行角色'原则——应该告诉用户调色盘就够了，后面的都是可选增强"——这同时也是 skill 本身的问题。一个 2000+ 行的 skill 没有给 AI 优先级，AI 会全量处理所有层次，把简单角色也推进混色和核心人格层。
- 审查 B 明确提出"SKILL.md 的角色是决策引擎，不是知识库"，并给出了约 250 行的 SKILL.md 草案，证明在这个规模下是可行的。
- 主审查的判断：教程的价值密度是"行为的衍生机制"和"各层的判据"，这些可以压缩到 ~250 行 SKILL.md + ~600 行按需加载的 reference。其余教学性内容应移除。

**决策：** 目标规模从 2000+ 行压缩到 ~250 行 SKILL.md + ~600 行 references。需要裁撤的内容包括：
- 教程里的"Why"论证（"为什么手写比 AI 直出好""为什么调色盘比标签强"——AI 读到 skill 时不需要被说服）
- 教程里的文学比喻和教学故事
- 重复的技法解释（调色盘在三处教程里都有讲，但 skill 只需要一份参考）

**可逆性：** 后续运行中发现遗漏的场景，可以加回——比从臃肿里删要容易。

---

### 1.2 阶段声明强制格式（handoff §2.18）

**原 handoff 写了什么：**

进入五种动作（追问 / 推演候选 / 素材已够准备推演 / 开新维度 / 选择引导）前，**必须**输出阶段声明，包含四个要素：槽位、层级关系、用户动作、产出用途。结构如下：

> "这一步在填[核心人格层·深层缺失]，是核心人格层体系的第二个槽位。现在需要你回忆她有没有过'想要却不敢说'的时刻——这个信息定了，后面的核心矛盾推演会从这里展开。"

handoff 注明"这不是风格建议，是交互底线"，并在 skill 文档中标注 `<!-- 阶段声明检查点 -->` 作为 AI 自检锚点。

**问题：**

handoff 自己记录了这个格式在提出后的同一轮讨论中**两次被违反**——第一次是推演候选没标槽位，第二次是"往下挖一层"没说明在挖什么维度。这说明该格式的设计者自己在设计过程中都做不到遵守——不是执行者的问题，是格式本身与对话的自然节奏冲突。

实践中，如果每次追问、每次展示推演候选前都输出四要素声明，用户体验会非常差：像一个填表机器，不是协作伙伴。用户会感到被流程操控而非被 AI 引导。

**审查中的判断：**

- 主审查的判断："这会把对话变成填表。应该改为软引导——AI 心里清楚当前在做什么维度、结果会填到哪，但只在用户可能迷失时才主动说明。"
- 审查 A 在分析中提到了一个相关的观察："教程反模板但自己就成了模板"——阶段声明强制格式本质上是把灵活性消解成了模板化交互。
- 审查 B 在 SKILL.md 草案中采用了完全不同的方式——通过流程步骤本身让 AI 知道在做什么，不依赖额外声明。

三方都没有提出保留强制格式的意见。

**决策：** 废除"每次输出强制四要素声明"的硬格式。改为：

- AI 在内部始终清楚自己在什么维度、填什么槽位（这靠 SKILL.md 的流程结构本身保证，不需要强制输出格式）
- **仅在以下情况主动告知用户当前所处的位置**：
  - 从一个维度切换到另一个维度（如从素材追问切换到推演，或从调色盘进入核心人格层）
  - 推演候选产生后，标注每条候选对应哪个槽位，以及选定后对下游的影响
  - 用户主动问"我们现在在做什么"
- 不再在 skill 文档中插入 `<!-- 阶段声明检查点 -->`

**判据：** 用户知道自己在哪里、在做什么决策；但不需要知道 AI 的内部运行逻辑（"我现在在加载认知图式框架"不需要说出来）。

**可逆性：** 如果上线后发现用户频繁迷失（"我不知道我们在做什么"），可以根据迷失类型增加特定时机的位置说明，而不是回到全面声明。

---

### 1.3 双名并行：重新裁定（handoff §2.17）

**原 handoff 写了什么：**

教程中的维度名（"调色盘""三面性""核心人格层""混色""二次解释"）是"方法名"——面向学过这套教程的人。skill 面向的可能是没读过教程的用户，需要"产物名"让用户直观理解：

| 方法名 | 产物名 |
|--------|--------|
| 调色盘 | 性格质地 |
| 多面性 | 场景模式 |
| 核心人格层 | 内在驱动 |
| 混色 | 情感交织 |
| 二次解释 | 角色边界 |
| 意象 | 意象 |

输出对用户时用"产物名（方法名）"双名格式保留社区锚点（这套名字在社区已有人沿用）。

**最初的反对意见：**

主审查最初认为双名并行增加认知负担——每个概念在被提及时都要带括号标注，文档和对话都会冗长。内部文档直接用方法名，用户输出只用产物名，不需要括号。

**重新审查后的判断：**

- 用户指出：handoff 的产物名还有一个未被注意到的功能——**模型适配**。"调色盘"这个隐喻在某些模型上可能失效（审查 A 特别提到了"不同模型对隐喻理解能力不同"），但"性格质地""场景模式"是直白的，不存在理解障碍。产物名本身就是 fallback。
- 审查 A 提出"模型差异适配"是一个关键缺口——handoff 没有解决"调色盘隐喻在弱模型上不 work"的问题。双名并行恰好是解决方案的一部分：提醒 AI"如果用户反馈角色表现不对，先检查是不是模型没理解隐喻，理解不了就切换到产物名"。
- 括号标注方法名维持了与教程社区的连续性——教程读者可能知道"调色盘"，看到"性格质地（调色盘）"能对上号。

**决策：** 保留双名并行方案，使用规则明确：

- **面向用户输出**：用产物名，首次出现时带括号保留方法名（"现在我们来确定她的性格质地（调色盘）"）。后续提及只用户物名。
- **内部文档（SKILL.md、references）**：直接用方法名——因为读这些文件的是 AI，不会被隐喻困扰，方法名更精确。
- **人物文档标题**：产物名为主，首次带括号。

**从批评到采纳的转折逻辑：** 最初批评"双名增加认知负担"——这个批评成立的前提是 AI 能理解隐喻。当 Agent A 指出模型差异后，这个前提不成立了。产物名的存在不是冗余，是适配。

---

### 1.4 多框架推演：从强制清单到内部工具箱（handoff §2.11）

**原 handoff 写了什么：**

推演候选通过"多路径推演"生成——从 ≥2 个不同心理学框架各推一条，标注依据。核心四框架（依恋理论、防御机制、社会学习、认知图式）+ 扩展池，每轮推演从核心四挑 2-3 个 + 按角色类型从池里加 1 个。目标是**用结构性差异替代 AI 灵机一动**——推演的多样性不靠 AI 自己发散，靠从不同生成轴各推一条。

**问题：**

handoff 的框架选择逻辑在理论上是自洽的（四个框架覆盖了生成性、行为落地、结构性差异、领域覆盖、卡片映射五个筛选标准）。但实践中，强制 AI 每轮选择 2-3 个框架并按框架名推演，会把对话变成学术演练。

教程原文中，推演的自然形态不是"我现在用依恋理论推演"，而是"从这段经历里能长出什么性格？从家庭的角度看……从她怎么保护自己看……"。框架是**思考视角**，不是**必须命名的生成步骤**。

更重要的是，框架清单放在 SKILL.md 里会挤占 AI 的认知资源。SKILL.md 的目标是让 AI 快速判断"我现在该做什么"，而不是"我现在该选哪个框架"。框架细节是执行层的工具，不应出现在决策层。

**审查中的判断：**

- 主审查："框架作为 AI 内化的视角，不在 SKILL.md 里列清单。推演时自然使用'从关系角度看/从自我保护角度看'，不需要每次标注框架名。"核心逻辑：handoff 设计框架是为了解决"频率偏差"（AI 默认选择高频关联）——但解决方案不需要暴露框架名。只要要求 AI 从 ≥2 个不同视角各推一条，就已经强制突破了频率偏好。
- 审查 B 的 SKILL.md 草案完全没有提到心理学框架名——说明在 250 行预算内，框架清单不是必要内容。
- 审查 A 没有单独讨论框架，但审查 A 的核心隐喻"把 AI 从标签演员训练成行为机制模拟器"暗示了多框架推演的方向是对的——只是实现方式需要调整。

**决策：**

- **SKILL.md**（决策层）：不列出框架清单。写为"从 ≥2 个不同的角度推演（比如关系的角度、她如何保护自己、她从环境中学会了什么、她内心相信什么）"——保留多视角推演的强制性，但不命名框架。
- **references/palette.md**（执行层）：保留完整框架清单和选择规则，作为 AI 在推演阶段的参考工具箱。AI 打开这份 reference 后知道有哪些视角可用、怎么组合。
- 框架名称（依恋理论、防御机制等）仅出现在 reference 内部，不向用户暴露。
- 推演标注三要素（§2.11）保留：行为场景 + 心理学机制 + 从哪段经历推出来的。这是正确的闭环设计。

**为什么这样改：** handoff 的原设计是对的——"需要从不同角度强制推演，不能靠 AI 灵机一动"。错的是把框架清单放在决策层。决策层只需要"从多个角度推演"这条指令；执行层才需要"哪些角度、怎么选"的详细指引。

---

## 二、讨论后新增的设计决策

以下 5 项是原 handoff 未覆盖、审查和讨论中发现的缺口。

### 2.1 AI 两阶段边界：追问 vs 推演的硬边界

**这是原 handoff 的什么缺口？**

handoff §2.10（动机追问协议）定义了"先问后推演"的序列：先问作者 → 作者答上了就验证 → 作者无解才多路径推演 → 人裁决。§2.14（红线）第一条就是"禁止先给推演"。

但这些描述的是**序列**，没有说明**追问阶段的 AI 行为边界**——追问时 AI 能不能暗示方向？能不能说"你这个描述有点像被抛弃后的缺乏安全感"？

审查 A 提出了一个根本性问题：教程反复强调"AI 不能代替创作者"，但 skill 本身就是一个 AI 驱动的协作过程——**这个矛盾怎么解？AI 帮到什么程度算过度？**

**审查中的分歧：**

- 审查 A 认为这是一个"根本性的设计选择"——skill 的设计者需要明确边界划在哪。审查 A 自己的建议是"只问问题不从数据库补内容"或"可以建议但必须由用户确认"。
- 审查 B 在 SKILL.md 草案中写了一条禁止事项："不替用户写衍生。衍生必须从用户脑子里长出来。"——给出了边界但侧重在产出端，没有覆盖追问端。
- 主审查最初提了一个激进的方案："素材收集和动机追问阶段，禁止补充用户没说的任何内容。即使你知道'被抛弃过的人通常会 XYZ'，不许说。"

**后续讨论中的澄清：**

用户质疑了我最初的激进方案："你这跟推演冲突吗？推演就是用户自己没办法明确定义，希望 AI 辅助构建，不许说是怎么个不许说？"

这个质疑让我重新检查了教程本身的流程——教程里确实有两个不同的阶段：

1. **追问**：用户能描述但不够具体（"她很怕被拒绝"→"她在什么情况下会怕？怕到什么程度？"）。这个阶段 AI 只问，不补。
2. **推演**：用户说不出原因（"她为什么怕被拒绝，我也说不上来"）。这个阶段 AI 必须给候选。

我最初的"不许说"混淆了这两个阶段。"不许说"应该只约束追问阶段；推演阶段不说才是失职。

**决策：**

| 阶段 | 触发条件 | AI 做什么 | AI 不做什么 |
|------|---------|----------|------------|
| 追问（动机追问 + 具体化追问） | 用户有描述但不具体，或只有标签 | 追问具体场景、时间、对象、程度 | 不提示方向（"你是不是想说她缺乏安全感？"），不补充用户没说的内容，不基于自身数据库给推断 |
| 推演（多路径推演） | 用户明确说"不知道/没想过/说不上来"，或答案与已有素材矛盾 | 从 ≥2 个不同视角给出 2-3 条候选，每条标注行为场景 + 机制 + 依据 | 不给，是失职 |

**判据：** 追问阶段用户还有表达空间——AI 的任务是帮用户挖出自己已有的但未表达清楚的认知。推演阶段用户已经告罄——AI 的任务是用工具方法生成候选供用户裁决。

**这条规则的底层逻辑：** 教程作者的核心信念是"作者的解释永远比 AI 的推演独特"。追问阶段是在挖掘作者的解释——所以 AI 不能用自己的解释污染。推演阶段作者没有解释——所以 AI 必须提供，但仍然仅供裁决，不替作者决定。

---

### 2.2 用户角色定位前置

**这是原 handoff 的什么缺口？**

handoff §2.9（入口引导）的起点是"世界设定"（题材、世界基调）→ 开放式人物询问。关系设定放在素材区里，作为素材的一个子章节（§2.3）。

但在教程体系中，关系不是"素材之一"——它是"角色的行为模式所依赖的上下文"。一个角色面对恋人时的反应和面对陌生人的反应可能完全不同。定义关系不是在填素材表的某一格，是在定角色行为的坐标系。

审查 A 特别指出了这个缺口："全部教程都在教怎么写角色，几乎没涉及'用户在这个故事里扮演谁'。关系设定太薄"。

**决策：** 把用户角色定位从素材区的子章节提升到入口引导的第一步，与世界设定并列：

世界设定（题材/基调/世界观）→ **同时问**："你在这个世界里是什么身份？你和角色的关系是什么？还是说你是旁观者、观众？"

理由：世界设定决定了角色生活的环境；用户角色决定了角色行为的核心参照系。二者都是角色构建的地基，应该同时铺设。世界设定在回答"她在哪"，用户角色在回答"她对面是谁"。

**不在素材区重复放关系**——入口引导中已确定的关系直接写入素材区对应位置。这与 handoff §2.3 的"想到哪填到哪"一致：入口引导阶段有了关系，就填进去；后续有补充，再追加。

---

### 2.3 最小可运行角色：用户驱动的深度控制

**这是原 handoff 的什么缺口？**

handoff 的设计没有给用户一个"够了，可以停"的决策点。构建顺序（§2.6）是线性的推进路径，虽然每个人都知道"不是所有角色都需要每一层"（来自教程原文），但 skill 流程中没有把这个变成用户可见的岔路。

教程自己说："不是所有角色都需要三面性""不是每张卡都必须写核心人格层"。skill 应该尊重这个——为用户提供一个"最小可运行"的判定和选择。

**决策：**

调色盘 + 二次解释 + 意象完成时，角色已具备基本运行能力。此时：

1. 展示当前构建进度（哪些槽位已完成，哪些尚未涉及——参考 handoff §2.17 的蓝图格式）
2. 明确告知："现在这个角色已经可以跑起来了。你想继续深化（多面性 / 混色 / 核心人格层），还是先到这里？"

**为什么是这三个作为"最小可运行"：**

- **调色盘**（性格质地）：AI 需要的行为描述体系。没有它，角色只有背景故事没有运行逻辑。
- **二次解释**（角色边界）：防 AI 误读的最后防线。没有它，调色盘的行为描述可能被 AI 用刻板方式演绎。
- **意象**：角色的核心比喻。它锚定 AI 对角色的整体感觉。

这三样组合在一起，AI 知道"她做什么"（调色盘）+"别把她解码成什么"（二次解释）+"她核心像什么"（意象）——能跑了。

多面性、混色、核心人格层都是**跑起来之后的深度增强**，不是跑起来的前提条件。

**用户体验：** 这个决策点让用户掌控深度。有些用户只需要一个能跑的角色，三个步骤就够了；有些用户追求极致深度，会选"继续"。两种都是成功的构建结果。

---

### 2.4 步骤分级：必须 vs 条件触发

**这是原 handoff 的什么缺口？**

handoff §2.6 给出了构建顺序（素材收集 → 调色盘 → 核心人格层 → 多面性 → 混色 → 二次解释 → 意象），但没有标注哪些是"所有角色都要走"的，哪些是"有条件的"。

这对 AI 执行 skill 时的决策很重要——AI 需要知道什么时候自动推进、什么时候需要判断条件。

**决策：**

| 步骤 | 性质 | 触发条件 / 说明 |
|------|------|----------------|
| 入口引导（世界设定 + 用户角色） | 必须 | 开篇就走，此步跳过会导致后续构建缺少坐标系 |
| 素材收集 | 必须 | 地基，无素材则无推演原料 |
| 调色盘 | 必须 | 核心产出，将标签类素材转换为行为描述体系 |
| 二次解释 | 必须 | 防 AI 误读，是调色盘从"能被理解"到"能被正确理解"的关键 |
| 意象 | 必须 | 最后凝聚，需要等全部素材聊完。构建顺序最后，呈现顺序最前 |
| **→ 最小可运行决策点** | **必须** | 暂停，用户选择是否继续 |
| 多面性 | 条件 | 角色在不同场景下说话方式差异大到像两个人。一个场景或压力性质相同的多个场景 → 跳过 |
| 混色 | 条件 | 角色内心有两种以上会撞在一起的情感。情绪线条清晰、不纠结的角色 → 跳过 |
| 核心人格层 | 条件 | 角色有剧情驱动、内部矛盾、成长空间。纯日常/轻松陪伴角色 → 跳过 |

**与 handoff §2.6 构建顺序的关系：** handoff 确认的构建顺序（核心人格层在多面性之前）仍然成立。当用户在"最小可运行决策点"选择继续时，AI 按此顺序依次检查每个条件步骤的触发条件，而不是一次性全部推进。

---

### 2.5 AI 常犯错误合并到 SKILL.md

**背景：** 审查 B 提出独立 `references/anti-patterns.md` 文件，列出 AI 在构建角色过程中的常见错误模式（标签放大、频道切换、情绪点名、戏剧化升级、泛化填充），供 skill 自己内部参考。

**审查后的判断：** 独立文件不必要——原因有二：

1. 这些错误模式本质上就是"skill 要防范的东西"，写成禁止事项自然属于 SKILL.md
2. 分成独立文件导致相关信息分散——AI 执行 skill 时需要同时持有"红线不应该做什么"和"禁止事项不应该犯什么"，分开存放降低命中率

**决策：** 所有 AI 常犯错误的直接表现形式写入 SKILL.md 的禁止事项。每个引出的错误模式追溯到对应的教程技法，在相关 reference 中展开（如"情绪点名"在混色 reference 中详述，"标签放大"在调色盘 reference 中详述）。不增加独立文件。

---

## 三、handoff 中经审查确认有效的决策

以下 handoff 设计决策经过三轮独立审查和讨论，确认成立，不需要修改：

| 决策 | 依据 |
|------|------|
| 技能触发方式（用户主动使用，不做自动触发优化） | skill 是协作工具不是后台服务——不自启动是对的 |
| 文件结构（单文件人物文档，素材区 + 构建区一体） | 两区分工清晰（事实/性格），但同一个人不需要两份文档。审查中无异议 |
| 人物文档 ≠ 最终卡（人物小传 vs 运行时提示词） | 区分"记录角色全部信息"和"给 AI 运行用"是正确的分层 |
| 素材区记录标准（推演价值判据，不记纯审美标签/默认值/无关一次性事件） | 判据清晰可操作，AI 能据此判断记录与否 |
| 过程记录取消（否决原因进素材区，防重复推演） | 该决策背后的推理链完整且合理——否决揭示角色特征 |
| 呈现顺序（意象 → 素材 → 调色盘 → 核心人格层 → 多面性 → 混色 → 二次解释） | 先给锚点再展开的结构合理 |
| 构建顺序（素材 → 调色盘 → 核心人格层 → 多面性 → 混色 → 二次解释 → 意象） | 核心人格层在前（防御机制是多面性功能的前提）的逻辑成立 |
| 名称"多面性"替代"三面性" | "三面性"引入"必须三张面"的误解，修改是必要的 |
| 总流程迭代环（入口探测 → 素材收集 → 动机追问 → 多框架推演 → 组装轮廓 → 确认 → 补全 → 验证，否决回到素材收集） | 环状结构反映现实中构建的反复性 |
| 入口引导（第一步世界设定必走，第二步开放式询问，第三步按内容展开，第四步无构思才引导，永远不先问"她是什么性格"） | 听用户说 → 追问具体化 → 兜底才引导。避免诱导贴标签 |
| 动机追问协议（先问后推演，具体化问法） | 保留了核心交互顺序——先挖作者的认知，挖不到再推演 |
| 问法六类（镜像/反应/具象/关系/反差/决策类） | 按可答性排序的提问策略是可操作的工具箱 |
| 硬规则"推演出行为场景不许出形容词" | 这是整个教程体系最核心的可执行规则，不可弱化 |
| 记录粒度（特征闭环即写入，事件驱动非轮次驱动） | 不等用户说"存档"，闭环时立即写入——保障信息不丢失 |
| 组装先给轮廓确认再补全 | 防一次性信息过载 |
| 五条红线 | 所有红线都直接源自教程核心教训，没有需要推翻的 |
| 确认与同步边界（纯过滤同步不确认，产生新内容确认） | 区分得好：过滤时让用户知道，创造时让用户批准 |
| 引导而非说教（核心是帮用户构建角色，教学是附带） | 符合教程定位 |
| 问题目标层（探明什么 + 终点 + 够不够判据） | 防止"为问而问"的设计是对的 |
| 双名并行（产物名 + 方法名括号） | §1.3 已重新裁定，保留 |

---

## 四、最终文件结构与各文件内容概要

```
character-palette-collab/
├── SKILL.md                    (~250 行)
│   ├── 红线（行为不是标签）
│   ├── 角色定义（引导者：追问/推演两阶段）
│   ├── 入口引导（世界设定 + 用户角色定位 + 开放式人物询问）
│   ├── 决策树（必须步骤 → 最小可运行决策点 → 条件步骤）
│   ├── 交互原则
│   └── 禁止事项（含 AI 常犯错误的所有形态）
├── references/
│   ├── palette.md              (调色盘：三色定义 + 衍生规则 + 推演工具箱视角)
│   ├── multifaceted.md         (多面性：触发判据 + 五个部件 + 过渡渗透)
│   ├── color-mixing.md         (混色：物理事实反差的技法 + 禁止解释的原则)
│   ├── core-personality.md     (核心人格层：七个元素的写法 + 从旧设定提炼的方法)
│   ├── secondary-explanation.md (二次解释：识别 AI 可能误读的点 + 写法)
│   └── imagery.md              (意象：找到它的问法 + 写法 + 示例)
└── templates/
    └── character-doc.md         (人物文档：素材区 + 构建区骨架)
```

**在各文件中的命名使用：**

- SKILL.md、references：方法名（"调色盘""多面性""核心人格层"等）——AI 不会困惑
- 用户可见输出：产物名，首次带方法名括号——"性格质地（调色盘）"
- 人物文档：产物名为主标题，首次注释方法名
- 蓝图/进度同步：产物名

---

## 五、原 handoff §6 待补充项的处理

### 6.1 否决后重推机制

**handoff 的担忧：** 用户否决一条推演后，重推时是否换框架、用什么判据区分"不一样但本质类似"的方向、几轮推不出结果怎么处理。

**处理：** 设计成上下文判断而非预设机制。

SKILL.md 中写："用户否决推演 → 记录否决原因（原因本身可能是新素材）→ 回到追问或推演阶段，AI 根据以下信息判断方向：否决了什么（不重复推已被否决的方向）、用户补充了什么新的素材（如有）、还有哪些未探索的视角。AI 自行决定追问（如果问题在于信息不够）或推演（如果问题在于已给的视角不对）或换问法（如果用户无法用推演的形式做决定）。"

**不预设机制的理由：** 否决的模式太多样——角色原因否决（"她不是这样的人"）、风格原因否决（"太戏剧化了"）、偏好否决（"我不喜欢这个方向"）——每个对应的合理处理方式不同。预设分支会覆盖不全且误导 AI。不如给判据、信 AI 判断。

### 6.2 接管已有设定

**handoff 的担忧：** 用户直接丢一份完整角色卡进来，skill 怎么处理。

**处理：** 入口引导阶段问："你有现成的角色卡/设定吗？还是从零开始？"

**有现成→** AI 解析现有内容映射到素材区（身份/外貌/背景/关系/习惯偏好各归各位）→ 分析现有设定是否标签化（"她现在写的是'极度自卑'，这是一条标签还没有展开的行为描述"）→ 从标签化的点开始追问具体化 → 正常进入调色盘推演缺失部分。

**从零开始→** 正常入口引导。

**不要求重新走完整流程的决策逻辑：** 强迫用户把已有的、可能很好地写好的设定重走一遍构建流程是不尊重已有劳动的。skill 的价值是"帮你把不够好的设定变得更好"，不是"不管你现在有什么，都用我的格式重做一遍"。

### 6.3 二次解释的触发时机

**handoff 的担忧：** 二次解释是"组装时一次性生成所有"，还是"构建过程中遇到就写"。

**处理：** 组装阶段统一生成。理由见 §2.4——所有二次解释条目都依赖对角色整体的理解才能判断 AI 会怎么误读。在调色盘完成前无法可靠判断；在混色完成前对"情绪不稳定"类误读无法全面覆盖。组装阶段有了完整的调色盘（含条件步骤的结果），一次性过一遍防误读最可靠。

### 6.4 AI 能力边界声明

**handoff 的担忧：** 教程原文多处断言"AI 做不到 X"。需要在 skill 中表明"现在的 AI 能做到什么程度、什么需要人类作者的选择和裁决"。

**处理：** 不写进 skill 文档。理由：skill 的读者是另一个 AI 实例——它不需要知道"自己的能力限度"的声明。这属于设计笔记/文档作者的思考，不是执行者的指令。skill 的设计本身就体现了边界——AI 推演候选但用户裁决——不需要再在文档里说一遍"AI 能做候选但做不了裁决"。
