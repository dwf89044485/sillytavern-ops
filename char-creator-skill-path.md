# char-creator skill 路径设计

## 目标

把"性格调色盘"整套教程内化成 AI 主动引导用户完成角色构建的 skill。用户不需要自己学会这套理论方法，也能在 AI 的引导下创建好角色。

## 核心价值

1. **打破教程门槛**：把"需要深刻理解才能用好的方法论"变成"你回答关于角色的问题，剩下的交给我"的创作助手。用户目标是写出好角色，不是学会写角色。
2. **解决 AI 标签化根源**：AI 是输入法联想，给标签就输出刻板印象。skill 在用户与 AI 间架桥，把用户自然语言描述自动转化为"AI 无法偷懒"的格式，用户无需理解底层原理即享效果。
3. **降低决策压力**：用户只需回答问题，不需要知道"调色盘""三面性""混色"这些概念。skill 内化方法，用户只回答问题。
4. **自动存疑化防锁死**：用户每答一问，skill 自动将其转成"存疑化"格式，用户无需感知。
5. **用户决定深度**：一条路径两种出口——"最小可运行角色"（基础信息即可生成可用卡，用户说够就停）或"深度完整角色"（用户有兴趣则自然深入）。不是让用户选路径，而是路径上的自然分叉。

---

## 决策记录

### 决策 1：放弃多路径，改为单一默认路径

**背景**：原 handoff 设计了三条路径（新手/进阶/轻量），用户认为"分三路径太复杂"。

**讨论过程**：
- 分三路径本质是在"替用户做判断"，用户一打开就要选路径，又变成"需先理解才能用"，与"降低门槛"目标自相矛盾。
- 路径是 skill 内部的事，不应暴露给用户。

**结论**：改为单一默认路径。从最简单问题开始 → 渐进深入 → 按需深入而非强制走完。用户决定深度。

### 决策 2：入门写法作为 skill 内部的新手引导选项，不是独立 skill

**背景**：教程第 11 篇"入门写法"（用更感性、更低门槛方式构建角色），讨论它应该做成另一个 skill 还是 skill 内部的一个路径。

**讨论过程**：
- 目标一致：终点都是帮用户写出活角色，入门写法是调色盘平替。
- 用户场景连续：先快速出角色再深化，拆两个 skill 增加切换摩擦。
- skill 职责边界是"角色构建"而非"调色盘方法论"，用户不关心用哪种方法。
- 入口判断成本极低：开头问一句即可。

**结论**：一个 skill 两条路径共享同一入口引导，最终汇聚到"组装角色卡"和"验证"阶段。入口先问"想快速上手先让角色活起来，还是系统学调色盘构建方法"——前者走入门写法，后者走调色盘主路径。两条路不互斥：入门写法出的角色后续可用调色盘继续深化。

### 决策 3：台词人设剔除主路径

**背景**：教程第 3 篇"台词人设"（已被教程作者标记"废除"但可作灵感）。

**讨论过程**：
- 台词人设是针对"克制型模型"（如 Claude、GPT 某些版本）的特化方案，解决"模型太克制，不主动演"的问题。
- 核心做法：不写角色描述，只写台词 + 第三人称存疑式解读。
- 本质是"针对特定模型缺陷的补丁"，不是通用方案。如果模型本身就很会扮演，台词人设反而限制发挥。

**结论**：剔除主路径。做技能暂时不考虑。后面发现模型有这个问题再回过头来打补丁。

### 决策 4：混色教程保留为进阶可选内容

**背景**：教程第 4 篇"混色教程"（针对有内心矛盾的角色，难度高）。

**讨论过程**：
- 混色是调色盘的自然延伸，解决"同一笔里多种颜色同时跑"的问题。
- 门槛高、难度大，不适合塞进主路径强制用户走。

**结论**：保留在 skill 中，但作为进阶/可选环节。由用户是否需要"有内心矛盾的角色"来决定是否进入。

### 决策 5：用户信息写法保留为最后可选补充

**背景**：教程第 9 篇"如何写抢话党用户人设与不抢话党用户人设"。

**讨论过程**：
- 用户信息是写"自己"给 AI 读，角色卡是写"角色"给 AI 演，二者不同。
- 解决的是"AI 误读用户行为"的通用问题，不是角色构建本身。

**结论**：保留在 skill 中，但放最后作为可选补充。角色卡完成后问一句"需要帮你写用户信息吗？"

### 决策 6：全程不教概念只问问题

**背景**：用户明确说"用户不需要知道调色盘/三面性/混色这些词"。

**讨论过程**：
- skill 内化方法，用户只回答问题。
- 例如用户说"公司高管"，skill 自然追问"对下属和在家是同一个人吗"，从而引入三面性，但用户不需要知道这个词。

**结论**：skill 所有环节只以问题形式呈现，不出现方法论术语。

### 决策 7：存疑机制不是独立功能，而是 skill 在每个环节自动做的事

**背景**：原 handoff 将"存疑化"作为独立路径/功能。

**讨论过程**：
- 存疑机制是教程核心理念之一：对抗 AI 读到三句就下结论锁死的惯性。
- 用户每答一问，skill 自动转成"存疑化"格式，用户无需感知。

**结论**：存疑机制嵌入每个环节，不暴露为独立选项。

---

## 核心路径（一条路径，两种出口）

```
入口（用户打开 skill）
  │
  ├─ 问：想快速上手先让角色活起来，还是系统深入构建？
  │   （用户选）
  │
  ├─ [快速上手] ── 入门写法路径
  │   基础信息 → 小传 → 采访 → 情绪阈值表 → 意象
  │   （后续可继续用调色盘深化）
  │
  └─ [系统构建] ── 调色盘主路径
       │
       ├─ 基础信息（她是谁）
       │   角色档案 / 外貌特征 / 背景设定 / 关系设定
       │   （不写性格，只写事实）
       │
       ├─ 调色盘（她是什么质地）
       │   底色 → 主色调 → 点缀 → 衍生
       │   （每种颜色在具体场景"长什么样"，必须是画面不是解释）
       │
       ├─ 是否深入？ ── 是 → 三面性（条件性）
       │   │                 不同压力场景切不同档位
       │   │                 触发条件 / 能量状态 / 语料 / 身体行为模式 / 功能
       │   │                 （不限定三张面，由角色决定）
       │   │
       │   ├─ 是否有内心矛盾？ ── 是 → 混色（进阶可选）
       │   │                             物理事实反差技巧
       │   │
       │   ├─ 是否继续深入？ ── 是 → 核心人格层（决策层）
       │   │                             表层欲望 / 深层缺失 / 核心恐惧 / 防御机制
       │   │                             核心矛盾 / 道德底线 / 自我认知可能性
       │   │
       │   └─ 二次解释（防误读护栏）
       │      直接告诉 AI"我写的角色是这个意思，别脑补"
       │
       └─ 意象（总比喻 / 总画面）
           收束全部内容为一个画面
       │
       └─ 组装角色卡
       │
       └─ 验证（AI 扮演测试）
       │
       └─ 可选：需要帮你写用户信息吗？
```

### 关键设计原则

1. **用户只回答问题，不学概念**。所有方法论术语（调色盘/三面性/混色/核心人格层）不出现在用户对话中。
2. **自然深入，不强制走完**。每个深入节点以问题形式出现（"是否继续深入？"），用户说够就停，输出"最小可运行角色"。
3. **存疑化自动嵌入**。用户每答一问，AI 自动转成存疑格式写入角色卡，用户无需感知。
4. **两条路径不互斥**。入门写法出的角色卡，后续可继续用调色盘体系深化。
5. **进阶内容可选**。混色/核心人格层/用户信息等，由用户按需决定是否进入。
6. **验证闭环**。组装角色卡后必须验证（AI 扮演测试），确保角色在对话中"活"起来。
