# HANDOFF：三线决策合并 + 源头层补齐

> **给接手的 AI**：读完本文件你应该知道——现在是什么状态、上一轮做了什么、上一轮错在哪、下一步按什么顺序做、哪些坑不要再踩。
> **创建**：2026-08-13 · **上一轮分支**：`cursor/merge-three-decision-tracks-1e3a`（PR #4）
> **本文件的位置**：`books/xingge-tiaosepan/design/HANDOFF_TRACK_MERGE.md`

---

## 0. 30 秒速读

项目要把"性格调色盘"教程蒸馏成可执行 skill。**过去存在三条各自独立的探索线**，都做到了相当深度，都有完整决策记录，**互相不知道对方存在**。上一轮把三条线做了碰撞融合，产出 `TRACK_MERGE_ANALYSIS.md`：22 条共识、33 条净增量、8 处咬合、**17 条真冲突待用户裁决**。

**但上一轮有一个已知缺陷：整个碰撞是在"零教程原文阅读"的条件下做的，已抽查证伪两条结论。** 所以当前状态是——**碰撞分析可以当讨论议程，不能当裁决依据。下一步是补齐源头层，而不是开始裁决。**

用户**一条都还没拍板**。不要假设任何冲突已有结论。

---

## 1. ⚠️ 三件必须先知道的事

### 1.1 有三条决策线，但 AGENTS.md 只写了其中一条

AGENTS.md 的「必读文件」只列了 A 线，并称 `design/CHAR_BUILD_GUIDE_DESIGN.md` 是「唯一权威主文档，先读这个」。**那句话在三线合并之前成立，现在不成立了。** 只读它开工，你会完全不知道 B 线和 C 线存在，然后重新发明它们已经解决的问题。

### 1.2 上一轮的碰撞分析有已知缺陷

上一轮只读了三条线的**衍生决策文档**，未读教程原文、`_AI_READ_THIS_FIRST.md`、`工作流/` 题库，以及 **A 线 `candidates/` 那 240KB 的一阶提取物**。

抽查后已证伪两条结论（详见 §4）。错误模式是系统性的，不是偶发。`TRACK_MERGE_ANALYSIS.md` 开头有「效力边界」一节，给出了三级风险分类，**动手前先读那一节**。

### 1.3 用户还没裁决任何一条

17 条真冲突全部悬空。**不要替用户裁决，也不要假设"看起来 2:1 就按多数来"。** 三条线的票数在文件里只是参考信息，不是结论。

---

## 2. 三条线速查

| | **A 线** | **B 线** | **C 线** |
|---|---|---|---|
| 位置 | `books/xingge-tiaosepan/` | `性格调色盘/books/char-palette/` | `没有明确要求禁止读/` |
| 代号 | 蒸馏线 | 追问线 | `character-palette-collab` |
| 核心范式 | **追问引擎** | **诊断 + 追问** | **推演引擎**（人给素材 → AI 用心理学框架推演 → 人裁决） |
| 深度在哪 | 执行框架 · 节奏 · 运行时承载 · 快速路径 · 引导阶梯 | 4 个基础诊断的精确判定标准 · 外部问法样例 | 推演机制 · 交互契约 · 问题目标层 · 命名方案 |
| 主文档 | `design/CHAR_BUILD_GUIDE_DESIGN.md`（1263 行） | `ARCHITECTURE_DECISION.md` + `DIAGNOSTICS_DECISION.md` | `DESIGN_HANDOFF.md` + `_REVISION.md` + `DESIGN_DISCUSSION_01.md` |
| 时间 | 08-10 → 08-13 | 08-13 落盘 | **08-04 → 08-07（最早）** |
| 一句话 | "引擎怎么跑起来、跑多久、跑完东西放哪" | "引擎每一轮判什么、判完说什么话" | "用户说不出来时，AI 凭什么造候选、怎么交付" |

**三条线是平行探索，没有取代关系。** 不能用"谁写得晚"裁决——D3/D4 两处出现了**两条线各自标注"用户已确认"**的直接冲突。

---

## 3. 当前产出

| 文件 | 状态 | 说明 |
|---|---|---|
| `design/TRACK_MERGE_ANALYSIS.md` | ✅ v2.1 | **主产出**。三线碰撞全记录 + 效力边界声明 + 待办登记 |
| `design/HANDOFF_TRACK_MERGE.md` | ✅ | 本文件 |
| 三条线的原有文件 | **未改动** | 上一轮按用户要求"不要改现有文件"，一个字没动 |

`TRACK_MERGE_ANALYSIS.md` 的编号体系（后续沟通直接引用编号）：

- 🟢 §一 共识：三线收敛 9 条 / A×C 7 条 / A×B 6 条
- 🔵 §二 净增量：`B-1~B-9`（B-3/B-4 已撤回）/ `A-1~A-10` / `C-1~C-16` / 咬合 `K1~K3、K5~K9`
- 🔴 §三 真冲突：`C1~C8`（原 A/B）+ `D1~D9`（C 线引入）
- ⚪ §四 伪冲突：`F1、F3~F11`
- ⬜ §五 空缺：`G1~G8`

---

## 4. 已知错误（已在文件中撤回，不要再当结论用）

抽查 `books/xingge-tiaosepan/candidates/counter-examples.md` 后证伪：

| 原结论 | 判定 | 实情 |
|---|---|---|
| B-3「放大器规则是 B 的净增量」 | ❌ **错** | A 线 `ce43` 已有，且比 B 更完整（带大总结 02 篇原文引用 + 机制解释 + 三条警示信号） |
| B-4「结论检测是 B 的净增量」 | ❌ **错** | A 线 `ce44` 已有完整 failure_mode + mechanism |
| B-5「扁平检测 A/C 都没有」 | ✅ 属实 | A 线 candidates 确无对应条目 |

### ★ 这个错误暴露出一条全新的工作线

这两条**不是"B 的净增量"，是"A 线自己的收敛损耗"**——A 在阶段 1 就提取到了，从 `candidates/` 收敛进 `CHAR_BUILD_GUIDE_DESIGN.md` 时丢了。B 只是独立重新发现了 A 弄丢的东西。

两种定性对应完全不同的动作：

- 若是「B 的净增量」→ 动作是"把 B 嫁接进 A"
- 若是「A 的收敛损耗」→ 动作是"**审计 candidates/ → design 的完整丢失清单**"

**只用几个关键词抽查一个文件就撞出两条。240KB 里还有多少，未知。这是必须单独做的一件事。**

---

## 5. 下一步执行路径

> 严格按阶段推进。**阶段 I 未完成时不要进阶段 III。**

### 阶段 I — 补齐源头层（当前所在位置）

按性价比排序，不是按直觉排序：

| # | 做什么 | 为什么是这个优先级 |
|---|---|---|
| **1** | 读 `性格调色盘/_AI_READ_THIS_FIRST.md`（1.9KB） | AGENTS.md 点名"读教程前必读"的翻译层定义，上一轮跳过了 |
| **2** | 读 A 线 `candidates/` 全部 5 份（240KB：frameworks / principles / cases / counter-examples / glossary） | **性价比最高**。它是一阶提取物，每条带 `source_chapter` + `source_quote`，等于"带出处的教程内容"——既补上下文，又直接修 §2.1/§2.2 净增量表 |
| **3** | **收敛损耗审计**：逐条比对 `candidates/`（18 框架 / 110 原则 / 20 案例 / 53 反例 / 34 术语）与 `CHAR_BUILD_GUIDE_DESIGN.md`，列出"提取到了但没进设计文档"的完整清单 | 见 §4。ce43/ce44 是抽查撞到的，总量未知 |
| **4** | 读教程原文，重点是三条线互相引用打架的段落 | 见下方清单 |
| **5** | 读 `性格调色盘/工作流/`（24 问 / 80 问 / 200 问 / AI 循环问答） | A 的 question-bank、B 的 6 维度清单、C 的十组目标层**都源自这里**，三个粒度的关系待定（⬜ G8） |
| **6** | 复核全部 ⚪ 伪冲突（F1、F3–F11） | 这些裁定全部依赖未经核对的教程引用，是风险最高的一批 |

**第 4 步的定点清单**（哪几段必读，因为三条线在这里给出了对立的事实主张）：

| 教程位置 | 谁在争什么 |
|---|---|
| 大总结 02 调色盘篇 §三 | B 的诊断锚点 / A 的 G5 / A 的 ce43 都引它 |
| 教程 2 号 + 大总结 03 三面性篇 | 🔴 **D4**：A 说"先有调色盘再有多面性"，C 说"防御机制决定面功能，核心人格层必须在前" |
| 教程 6 号 + 大总结 07 意象篇 | 🔴 **D3**：C 说"教程只说意象必须克制字少，并没要求每个角色都有，秋明月就没有"，A 说"依赖链最后" |
| 教程 7 号 `:220` | C-4 的"最低限度要知道"清单，C 给了精确行号，未核 |
| 教程 11 号 | A 的快速路径 G3 五步**全部**依据此篇 |

### 阶段 II — 修正碰撞分析

用阶段 I 的收获重写 `TRACK_MERGE_ANALYSIS.md` 的 §2 净增量表、§四 伪冲突判定，并把收敛损耗清单作为新的一节加入。撤掉「效力边界」里的 🟠/🟡 风险标注。

### 阶段 III — 逐条裁决（需要用户参与）

分五批，按依赖顺序（详见 `TRACK_MERGE_ANALYSIS.md` §六的依赖图）：

1. **范式底座**：`D1`（追问阶段能不能推演）—— 这条不定，C 线 60% 的增量无处安放
2. **体量与形态**：`C3`（判定树 vs 原则驱动）→ `D9`（SKILL.md 规模）→ `C2`（4 诊断放哪）→ `C1`（references 拆分轴）
3. **交互契约**：`D2`（静默 vs 同步）→ `D5`（分批呈现 vs 每构件投影）；`D6`（术语可见度）→ `D7`（开场先不先收标签）
4. **文档形态**：`D3`（意象位置）→ `D4`（构建区依赖链）
5. **边界**：`C6 / C7 / C8`
6. **随时可插队**（无依赖）：`C4`（skill 数量）/ `C5`（命名）/ `D8`（触发方式）

### 阶段 IV — 回写落盘

`TRACK_MERGE_ANALYSIS.md` §7.2 有完整的回写动作清单。其中两条是硬性的：

- **G3 去重**：A 的质量信号 A/B/C 与 B 的泛化诊断是同一把尺子的两种表述，不去重会让 SKILL.md 出现两套判"够不够具体"的标准
- **D1 若判"允许推演"**：A 的 §2.2 硬约束必须按 §〇 铁律 2 标注修订，**不能直接改写**

---

## 6. 硬约束（违反即错）

1. **教程 ≠ skill**（AGENTS.md 最高优先级约束）。读教程时每看到晴空妈妈一个行为，先分类：是**追问角色**（融入 skill）还是**教柚子方法论**（不融入，用户主动问才简答）。AGENTS.md 里有完整的七行对照表。
2. **派任何子任务给 agent，必须注入上述区分 + `design/AGENT_PROMPT_PREFAB.md`。**
3. **决策记录按 §〇 5 问**（做什么 / 怎么做 / 为什么 / 代价 / 不要怎样做）。三条铁律：被否决方案必须记录、被取代的决策不删除只标注、推理链必须保留。
4. **不替用户裁决。** 17 条冲突全部要用户拍板。
5. **命名统一用"三面性（多面性）"。**

---

## 7. 陷阱清单

| # | 陷阱 | 怎么避 |
|---|---|---|
| 1 | 只读 `CHAR_BUILD_GUIDE_DESIGN.md` 就开工 | 它是 A 线的主文档，不是三线的。先读 `TRACK_MERGE_ANALYSIS.md` |
| 2 | 相信碰撞分析里"某线完全没有 X"式的断言 | **B-3/B-4 就是这么翻车的。** 每条这类断言在引用前都要去对应线的**原始提取物**里核一遍，不能只看收敛版 |
| 3 | 读 C 线时读了被取代的版本 | C 线内部有三处自我修正（F9/F10/F11）：MVC 含不含意象、阶段声明强制格式、问法六类排序。**一律以 `_REVISION.md` / `DESIGN_DISCUSSION_01.md` 为准，不要用 `DESIGN_HANDOFF.md` 正文的旧版** |
| 4 | 以为 A 线已经审过 C 线 | A 的 §12.7 有「范围说明」，明确写着只审了承载机制，**MVC 时机 / 构建顺序 / 意象位置 / 产物名 / 多框架推演五项不在审查范围**。那五项正是 D1–D5 的来源 |
| 5 | 把三条线的"票数"当结论 | 2:1 只是参考。C 线在 C3 上站 A，但 C 站 A 的理由（自我违反实证）比票数本身有用得多 |
| 6 | 直接改三条线的原有文件 | 上一轮的约定是不改。要改先问用户，且按铁律 2 标注而非覆盖 |
| 7 | 忘了 `char-creator` 这个遗留 skill | `.claude/skills/char-creator/` 是更早的雏形，B 线的 questioning-moves #7 还在引用它。C5 命名裁决时要一并处理去留 |

---

## 8. 文件地图

```
AGENTS.md                                    ← 项目目标 + 教程≠skill 约束（⚠️ 必读文件清单已过时，只列了 A 线）

性格调色盘/                                   ← 教程原文（源材料）
├── _AI_READ_THIS_FIRST.md                   ← ⚠️ 读教程前必读，上一轮跳过了
├── 0~11 共 11 篇 + 10.大总结教程/ 7 章
├── 工作流/                                   ← 24问 / 80问 / 200问 / AI循环问答
└── books/char-palette/                      ← 【B 线】
    ├── ARCHITECTURE_DECISION.md             ← 大脑 vs 操作手册，9 个 probe
    ├── DIAGNOSTICS_DECISION.md              ← ★ 4 个基础诊断的精确判定标准
    ├── verified.md / PIPELINE_STATE.md
    ├── candidates/questioning-moves.md      ← 13 个追问动作完整定义 + 外部问法
    └── diagnostics/4-basic-...-criteria.md  ← 32KB 原始产出，收敛时丢了 §6.3/§7.2

books/xingge-tiaosepan/                      ← 【A 线】
├── candidates/                              ← ★ 240KB 一阶提取物，收敛损耗审计的对象
│   ├── frameworks.md (18) / principles.md (110) / cases.md (20)
│   ├── counter-examples.md (53)             ← ce43 放大器 / ce44 结论检测就在这
│   └── glossary.md (34)
├── verified.md / ARCHITECTURE.md / DESIGN_DECISIONS.md / PIPELINE_STATE.md
└── design/
    ├── TRACK_MERGE_ANALYSIS.md              ← ★★ 主产出，先读这个
    ├── HANDOFF_TRACK_MERGE.md               ← 本文件
    ├── CHAR_BUILD_GUIDE_DESIGN.md           ← A 线主文档（1263 行）
    ├── AGENT_PROMPT_PREFAB.md               ← 派 agent 必须注入
    └── E_SECTION / I_SECTION / A1 / A2 / B_SEGMENT / KNOT  ← 历史审查输入

没有明确要求禁止读/                            ← 【C 线】目录名是"没有明确要求禁止读"，即可以读
├── DESIGN_HANDOFF.md         (695 行)       ← 正文，⚠️ 有三处已被取代
├── DESIGN_HANDOFF_REVISION.md (372 行)      ← ★ 修订，优先级高于正文
├── DESIGN_DISCUSSION_01.md   (376 行)       ← ★ 最新，优先级最高
├── char-creator-skill-path.md
└── char-creator-full-context.md             ← 逐轮原文记录，上一轮只读了第 1 轮

.claude/skills/char-creator/                 ← 遗留雏形 skill，去留待定（C5）
```

---

## 9. 需要用户拍板的完整清单（17 条）

**范式**
- `D1` 追问阶段能不能推演 / 推演的生成轴能否用 AI 外部框架 / A §2.2 措辞是否加限定

**体量与形态**
- `C3` 判定树 vs 原则驱动
- `D9` SKILL.md 规模：80–120（A）/ 150–180（B）/ 250–300（C）
- `C2` 4 个基础诊断放 body 还是 reference
- `C1` references 拆分轴：方法论模块（A+C）vs 追问动作（B）

**交互契约**
- `D2` 静默记录（A 的 L6）vs 过滤必须同步（C 的 §2.15b）
- `D5` 分批呈现（C）vs 每构件投影（A 的 L10）
- `D6` 术语可见度：完全不说（A+B）vs 产物名双名并行（C）
- `D7` 开场：先收标签（A+B）vs 永远不先问性格（C）

**文档形态**
- `D3` 意象在呈现顺序的最前（C）还是最后（A）—— ⚠️ 两次"用户已确认"冲突
- `D4` 构建区依赖链要不要补"面功能 ← 防御机制"这条边

**边界与元信息**
- `C4` skill 数量 / `user-info-calibration` 归属 / 要不要"角色卡设计 skill" / 要不要"角色验证体检 skill"
- `C5` 命名：`char-build-guide` / `char-builder` / `character-palette-collab` / `char-creator` 四选一 + 旧 skill 去留
- `C6` 快速路径切换：连续卡住即切（A，且 A 明确否决过 C 的方案）vs 入口显式选（C）
- `C7` 世界观：划给 worldbook-config（A）/ 条件触发（B）/ 必走第一步（C）
- `C8` NSFW：不覆盖（A+C）vs 条件触发（B）
- `D8` 触发方式：优化 description（A）vs 用户主动调用不做触发优化（C）

**外加一条元决策**
- 三条线的 `PIPELINE_STATE` 怎么合并；AGENTS.md 的必读清单与进度表怎么更新

---

## 10. 上一轮值得复用的方法论观察

C 线是三条线里唯一做了这三件事的，合并后的验证方案建议吸收：

1. **逐条反面验证** —— 不问"这样做好不好"，问"**如果不这样做会发生什么**"
2. **实操演练** —— C 用一个具体角色跑了 9 轮演练，**演练直接发现了一条纯推理发现不了的规则遗漏**
3. **自我违反检测** —— C 发现自己设计的强制格式"在提出后的同一轮讨论中两次被违反"，用这个事实否决了自己的设计

A 线有多轮 agent 交叉验证，B 线有 5 agent 独立设计后综合，**但都没有"演练"和"自我违反检测"**。建议在阶段 4 压力测试之前，先跑一遍 9 轮级别的手工演练。
