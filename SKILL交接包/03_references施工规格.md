# 03 references 施工规格

> 每个 reference 一节。写哪个文件读哪一节，不要通读。
> **文件清单本身卡在 C2**（按方法论构件切 vs 按追问动作切 vs 二维共存），但**每份手册的内容规格已经可以定**——C2 只决定它们怎么归组和叫什么名，不改内容。

---

## 通用规格（所有 reference 都适用）

### 内部结构：四段式，顺序固定

```
1. AI 内部诊断      —— 完整判断标准（SKILL.md 里放的是摘要，这里是完整版）
2. AI 对用户的外部问法 —— 只放能直接说出口的问句，自然语言
3. 手法示范        —— 完整追问回合（摆素材→问→不合格再追一层→用户说出词→凝练命名）
4. 质量门          —— 怎么判断用户的回答"够了"，可以推进
```

**第 1 段和第 2 段必须物理分开。** 诊断是 AI 心里的判断，用户看不到；外部问法是 AI 实际说出口的话。混在一起写，执行的 AI 会把诊断词说给用户听。

> **为什么有第 3 段（2026-08-15 裁定，原为三段式）**
> 完整追问回合天然含判断和用户台词，塞进第 2 段必然违反"问法段不能有判据"，塞进第 1 段又丢掉了它作为**可照做的动作序列**的性质。所以单列一段。
> **第 3 段是唯一允许跨越诊断/问法边界的地方**，它的内容必须标注为「形态示范」，且旁边要有一句显式警告：**只看形态，不要照搬角色专属内容。**
> 第 3 段不是每份手册都必须有——只有需要示范多轮追问节奏的手册（核心人格层、多面性、核心问法密集的那几份）才写。轻量手册可以只有 1/2/4 段。

### 读者与语言

读者是 **AI，不是用户**。用执行语言：`✅ "追问用户哪个特质是一直在的"` / `❌ "底色是无论什么场景都隐隐存在的东西"`。

**外部问法段里的问句写成大白话**（`04` H-06）——`❌ "她的防御机制是什么？"` / `✅ "有人靠得太近的时候，她第一个动作是什么？"`。**内部诊断段照常用术语**，那是写给 AI 自己看的。

### 篇幅与量级

线 C 给了每份手册的量级标注，**直接采纳**（这个信息对 AI 判断"要不要进这一层"很有用）：

| 手册 | 行数量级 | 撑几轮对话 | 轻重 |
|---|---|---|---|
| 调色盘 | ~40 | 4-7 | 常用 |
| 多面性 | ~50 | **8-15** | **重模式** |
| 混色 | ~25 | 3-6 | 中等 |
| 核心人格层 | ~55 | **8-15** | **重模式** |
| 二次解释 | ~20 | 2-3 | 常用 |
| 意象 | ~15 | 1-2 | 总是尝试 |
| 世界观锚定 | ~15 | 1-2 | 一次搞定 |
| 快速路径 | ~40 | 全程 | 分支 |

### 不要重复 body

body 已经说过的（深度轴、判停标准、硬铁律、姿态校准），reference 里不重复，只写"到了这一步具体怎么做"。

### 每份都要有的四件事

1. **进入判据**——什么信号说明该进这一层
2. **不适用判据**——什么角色不需要这一层（教程对每层都给了，不要漏）
3. **外部问法**（与诊断分开）
4. **质量门**——做到什么算够，可以推进

---

## ✅ 核心人格层操作手册

> **内容已被 D-01 全部定死，可直接开工。** 文件名待 C2（`core-layer-guide.md` 或 `probe-core.md`）。
> **素材**：`素材/核心人格层_原文素材.md`（1659 行 / 321 锚，八块 A–H）。下表"素材"列直接指节号。
> **量级**：重模式，~50-55 行，撑 8-15 轮。
> **触发**：追问到了"为什么选这条路"但用户说不清。

### 第 1 段 · AI 内部诊断

| # | 必须包含 | 形态要求 | 素材 | 验收 |
|---|---|---|---|---|
| 1 | **进入判据**：什么样的角色需要这一层 | 判据清单，可判定 | `F4`（⚠️ 不是 `F1`——`F1` 只讲"这一层是进阶选择"，没有判据清单） | 能回答"要不要挖这层"，不靠感觉 |
| 2 | **排除判据**，含「作者自己还没想明白角色核心矛盾的角色」 | 清单。⚠️ 教程两版（`7.md` 5 条 / `05.md` 4 条），**两版都列并标差异** | `F2`、`F3` | 排除项里明确有"用户想不明白" |
| 3 | 七部件是什么 | 名称 + 一句话定义 | `A0` | — |
| 4 | 部件间依赖 | 哪个部件的输入依赖哪个 | `A0` + `B8-1`~`B8-6` + `D2` + `B7`（⚠️ 不是 `D8`——`D8` 是整层硬要求，不含依赖信息） | 自我认知可能性那条标明依赖核心矛盾与深层缺失 |

### 第 2 段 · AI 对用户的外部问法

| # | 必须包含 | 形态要求 | 素材 | 验收 |
|---|---|---|---|---|
| 5 | 七部件各自的骨干问句 | **问句**，不含理论词 | `C1`、`C2` | 每句能直接说出口 |
| 6 | 卡住时的行为题，按部件分组 | 每部件 **2-4 题**（素材 `C3`–`C9` 共 **76 题**，是弹药库不是清单） | `C3`–`C9` | 抽查任一题不含术语 |
| 7 | 通用推进短句 | 直接可用（"再往下一层""这是表面""再具体一点。用行为来说"） | `C10` | — |

> ⚠️ 本段**只放能直接说出口的问句**。完整追问回合移到第 3 段（2026-08-15 段位裁定，见通用规格）。

### 第 3 段 · 手法示范

| # | 必须包含 | 形态要求 | 素材 | 验收 |
|---|---|---|---|---|
| 8 | **摆素材反推的完整手法** | **完整回合形态**：摆出三条已确定的行为 → 问 → 不合格再追一层 → 用户说出词 → AI 凝练命名。**整段必须标注「形态示范」** | `B3`（核心恐惧四层下潜、中途换素材）。⚠️ **`B2` 只可作下潜层次的参照，不可作 AI 动作的参照**——`B2` 里引导者自己把结论说完了（"对了。她的深层缺失是……"），违反 item 12；`B4` 同理 | 手法里 AI 的动作只有「摆素材 / 问 / 命名」三种，没有产出结论 |
| 9 | **形态参照警告** | 紧贴回合旁一句：**只看形态，不要照搬角色专属内容** | `C12`（那 9 句是角色专属问句） | `grep "形态参照\|只看形态"` 命中 ≥1 |

### 第 4 段 · 质量门

| # | 必须包含 | 形态要求 | 素材 | 验收 |
|---|---|---|---|---|
| 10 | 七部件各自的合格判据 | 逐部件，可判定 | `D1`–`D7` | 防御机制那条含"能落到行动上，不能只写一个词" |
| 12 | **「凝练命名」与「产出结论」的分界** | 可判定的判据：**凝练命名** = 把用户上一轮说过的词重新组织并交回确认（"你说的这两样，就可以叫 X，对吗"）；**产出结论** = 引入用户没说过的判断（"她的 X 是 Y"且 Y 的成分不在用户原话里）。判据是"Y 的每个成分能不能在用户原话里找到" | — | 有这条判据，且能用它判 `B2`（不通过）与 `B3`（通过） |
| 11 | 整层的硬要求 | 清单 | `D8` | — |
| 12 | **产出形态：条件-动作触发表** | `当…… → 角色会……`。⚠️ 教程只给了**部件层面的通用表**（`H1`），**没有**整体改写实例——不要凭空造 | `H` 段 | 抽查成品，出现「因为…所以…」即不合格 |
| 13 | 成品参照 | 逐字给陈乖乖七部件 | `A2`（`A3` 顾钧可选） | — |
| 14 | 反面判据 | ⚠️ **只写判据，不做正误并列展示** | `E1`–`E11` | 没有"错误写法 vs 正确写法"并排表格 |
| 15 | 兜底出口 | 追问一次 → 抽行为题 → 仍说不出则**不写这一层** | **`04` H-04**（⚠️ 不要用 `F` 段——`F5` 明写教程未提供答不出来怎么办，且 F 段没有"抽行为题"这一环） | 规则里有 `不写` 分支 |

### 禁止写进这个文件

| # | 不要写 | 为什么 |
|---|---|---|
| N1 | 心理学框架名 | `04` H-01 |
| N2 | 成因式表述作为**产出示范** | `04` H-02。⚠️ 素材 `B8` 有教程"从背景反推恐惧"的成因式示范——**那是推导过程，可引用为中间步骤，不能写成产出形态** |
| N3 | 正误并列对照表 | B 类教学手段 |
| N4 | "AI 会怎么误读"的原理讲解 | B 类 |
| N5 | 操作系统 / 界面 / APP 那套比喻 | B 类术语教学 |

### 依赖

先写完 body（不重复判停与姿态）· 触发措辞依赖 ⏳ C5 · ~~外部问法能不能出现"核心人格层"这个词依赖 C9~~ ✅ **D-02 已定**：外部问法写成大白话，见通用规格 §读者与语言

### 自检

`06_验收.md` §核心人格层操作手册

---

## ✅ 调色盘操作手册

> 内容确定，可开工。文件名待 C2。量级 ~40 行 / 4-7 轮。
> **素材**：✅ `素材/构件规格与判据.md` **§A 调色盘**（A.1 定义逐字 / A.2 部件清单 / A.2b 标签→调色盘转化路径与骨干问句 / A.3 判据 / A.4 成品原文）+ ✅ `素材/追问原句库.md` **§A2 探底色 · §A3 探主色调 · §A4 探点缀 · §A5 探衍生**。

### 必须包含

| 段 | # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|---|
| 诊断 | 1 | 四层定义（底色 / 主色调 / 点缀 / 衍生），**逐字取教程** | 一句话一层 | 与教程 `02_调色盘篇:33-43` 逐字一致 |
| 诊断 | 2 | **标签 → 调色盘的转化路径**：收标签不纠正 → 问"哪个是无论什么时候都隐隐存在的" → **去掉放大器**（"极度"）→ 用背景事实反测标签是否打架 → 并存两项时并起来命名 → 主色调往里走一层 → 点缀问反差时刻 | 有序步骤 | **七步**齐全，尤其"去放大器"不能漏 |
| 诊断 | 3 | **转化完成度校验**：逐个回收用户原标签，确认每个都有落点 | 可执行的检查 | 有"没有一个标签消失"这条 |
| 问法 | 4 | 每层的骨干问句，逐字取教程 | 问句 | 见下方参考 |
| 质量门 | 5 | 衍生的四条判据 + 三种失败形态 | 判据清单 | 含"具体到你能在脑子里看到画面" |
| 质量门 | 6 | **无效衍生判定**：这条是不是 AI 数据库里该标签下最常见的关联？是 → 写了等于没写 | 可判定 | — |
| 质量门 | 7 | 每色 **3-5 条**衍生 | 数量下限 | — |

参考原句（教程逐字）：

```
这七个标签里，哪个是无论什么时候都隐隐存在的？     ← 底色
但"安静"和"乖"太表面了。我们往里走一层。          ← 主色调
你想想，她在什么时候会和平时表现得完全不同？        ← 点缀
那个松下来的部分是什么？                          ← 点缀
```

### 禁止写

衍生里写"为什么"（`02:167`「不要解释为什么，只写她会做什么」）· 正误并列对照表 · "超级压缩包""七个频道"这类比喻教学。

⚠️ **已知教程内部冲突**：`1.入门:274` 说「衍生的本质是……**并给出背后的原因**」，与 `02:167`/`:363` 直接矛盾。**裁定：`1.入门:274` 位于 NSFW 调色盘小节下，适用对象是 NSFW 衍生；通用衍生按 `02:167` 执行——不写为什么。** 且这与 `04` H-02（成因式当梯子不当成品）一致。

### 转出信号（写进本文件，供路由用）

> 「当调色盘的衍生已经不够描述角色在不同场景下的巨大差异时，你就需要下一个工具了。」（`02_调色盘篇:370`）

---

## ✅ 多面性操作手册

> 内容确定，可开工。文件名待 C2。量级 ~50 行 / **8-15 轮，重模式**。
> ⚠️ **命名统一用"多面性"**，不用"三面性"。教程依据只在分散单篇 `2.进阶，三面性.md:21-31`（大总结版把这一整节丢了），引用时必须引单篇。

### 必须包含

| 段 | # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|---|
| 诊断 | 1 | **三层触发判据**：① 行为模式有没有**根本性切换**（明文排除"稍微放松一点""语气变了一点"）② 能不能找出**两个以上"压力性质截然不同"的场景** ③ 三问否决清单全否即不拆 | 判定树 | 三层齐全 |
| 诊断 | 2 | **关键闸门**：不是"场景不同"就拆面，是"**压力性质不同**"才拆 | 一句话，加粗 | 有这句 |
| 诊断 | 3 | **面数由角色决定**，可以是两面三面四面。**千万别为了凑"三面"硬拆** | 一句话 | 有这句 |
| 诊断 | 4 | **去重判据**：如果删掉多面性只看调色盘，内容已经包含在里面了 → 你在重复 | 可判定 | ⚠️ 这条只在单篇 `2.md:414`，大总结丢了 |
| 诊断 | 5 | **合并判据**：两张面的压力性质差不多 → 合并成一张 | 可判定 | ⚠️ 同上，`2.md:446` |
| 诊断 | 6 | **解释混入检查**：有没有「因为」「本质上」「其实」「内心」 → 有就移到二次解释 | **可机械匹配的词表** | ⚠️ 同上，`2.md:444`。这是全教程最可机械化的一条判据，不要漏 |
| 问法 | 7 | 骨干问句 + 极端假设情境注入（只给场景不给行为） | 问句 | — |
| 质量门 | 8 | **每面五部件缺一不可**：触发条件 / 能量状态 / 语料 / 身体行为模式 / 功能 | 清单 + "缺一个 AI 对这张面的理解就会出现漏洞" | 五个齐全 |
| 质量门 | 9 | **过渡至少两组**：最常见的切换路径 + 最剧烈的切换路径（后者"没有渐变"） | 数量下限 | — |
| 质量门 | 10 | **渗透**：一张面运行时其他面的元素会不会泄漏 | 定义 + 判据 | — |
| 质量门 | 11 | 语料从调色盘**移入**各面（不是复制） | 操作 | — |

### 降级出口

不到拆面标准时：**把差异压成衍生里的一句话**（`03:62`），不硬拆。

### 禁止写

解释"为什么"、分析"她的内心"（那是二次解释的活）· "三面性不是三种性格"整节术语纠偏（B 类）· 发动机换挡 / 颜料画布这类比喻教学。

---

## ✅ 混色操作手册

> 内容确定，可开工。量级 ~25 行 / 3-6 轮。

| 段 | # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|---|
> **出处锚**：触发判据 `04_混色篇:248` · 定义 `:33-41` · 四步操作 `:82-104` · 物理事实反差 `:57` · 质量判据 `:198` · 不要每个瞬间都混色 `:102`/`:216-222` · 禁止词「因为」`:210`、"但"字转折 `:232` · 成品集 `:250-308`。⚠️ **这几条只在分散单篇**：不适用判据"模糊她的轮廓，让她变得拧巴" `4.md:184` · "不是…而是…" `4.md:118` · 破折号 `4.md:137` · 「本质上」「其实」「内心」 `2.md:444`。

| 诊断 | 1 | **一句话判据**：这个角色在同一个瞬间里会有两种以上的东西在跑吗？会 → 适合；不会 → 调色盘就够了 | 一句话 | — |
| 诊断 | 2 | **不适用**：情绪线条清晰、做了决定就往前冲、不纠结的角色。硬加会"模糊她的轮廓，让她变得拧巴" | 判据 + 后果 | — |
| 诊断 | 3 | 输入是**调色盘**（叠加，不是替代） | 一句话 | — |
| 问法 | 4 | 四步操作：回读颜色 → 问"哪两种最容易撞在一起" → 把撞色瞬间写成只含动作和物理事实的画面 → 不解释 | 有序步骤 | — |
| 问法 | 5 | **物理事实反差技法**：一句话 + 一个同时存在的客观事实（"她说'我没事'。她的手在抖。"） | 技法 + 示例 | — |
| 质量门 | 6 | 质量判据：**两种可能性住在同一个动作里** | 一句话 | — |
| 质量门 | 7 | **不要每个瞬间都混色** | 负向约束 | — |
| 质量门 | 8 | 禁止清单（八项）：并列成清单的情绪罗列 / "但"字转折 / 破折号 / "不是…而是…" / 因为 / 本质上 / 其实 / 内心。⚠️ **教程未给数量阈值，判的是句式不是个数**——"并列成清单"指"她同时感到 A、B、C……"这种罗列形态 | **可机械匹配** | 八项齐全，且与 `04` H-15、`06` X5 逐项一致 |

⚠️ **教程未规定停止条件**（做到几个算够）。成品集给了 7 个画面，只能当量级经验值，**不要写成硬性数量要求**。

### 禁止写

梵高的星空比喻（B 类）· "AI 会怎么误读情绪"的原理讲解（B 类）。

---

## ✅ 二次解释操作手册

> 内容确定，可开工。量级 ~20 行 / 2-3 轮。

| 段 | # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|---|
| 诊断 | 1 | 它是什么：**防误读说明书 / 反向护栏**——直接告诉 AI"我写的这个角色是这个意思，你不要自己脑补别的" | 定义 | 定位是"规定理解方式"，不是"补充原因" |
| 诊断 | 2 | **时机：全部写完后统一生成**，不在构建过程中分散触发 | 硬要求 | ⚠️ 见下方冲突裁定 |
| 诊断 | 3 | 怎么找误读点：先列"AI 最容易把她写成什么样子" | 操作 | — |
| 问法 | 4 | 常见误读模式：安静→冷漠 / 不接受帮助→傲娇 / 为妈妈拼→热血少年漫 | 清单 | — |
| 质量门 | 5 | 格式：「关于 XXX：」+ 一段你的理解 | 模板 | — |
| 质量门 | 6 | 🟡 **每条至少含一句"不要写成 X"** —— **见下方冲突，先别照写** | 可判定 | ⏳ 待裁定 |
| 质量门 | 7 | **不是越多越好**；调色盘衍生已经足够具体时可以少写 | 负向约束 | — |

⚠️ **冲突裁定（第 2 项）**：线 B `§5.1.3` 给这个文件标的触发条件是"**每个深度层完成后**"，与线 A `§2.13`/`REVISION §五 6.3` 的"组装阶段统一生成"矛盾。

**裁定：按线 A + 教程执行——全部写完后统一生成。** 依据：教程 `06_二次解释篇:5-7` 是柚子说"我把前面所有东西都写完了"才开始；教程**没有**"每层做完就做"的表述（素材提取时检索六组词确认）。理由也硬——每个二次解释条目都依赖对角色整体的理解才能预判误读，调色盘完成前无法可靠判断。

**这条同时修掉线 B 的内部张力 B/T25（时序耦合）。**

---

⚠️ **新冲突（第 6 项）· 素材提取时发现 · ⏳ 待裁定**

教程**明文写法规则**与它**自己的全部成品**互相打脸：

| 哪一侧 | 原文 | 锚 |
|---|---|---|
| 明文规则 | 「写法上有一个核心原则：**直接叙述你想要的样子。不要先列举错误然后否定它。** 直接说"她是这样的"。」 | `06:44` |
| 全部成品 | 陈乖乖 9 条 **9/9** 以"不要写成 X"收尾；姜糖 4 条 **4/4** 同样 | 见 `素材/构件规格与判据.md` §D |

同一章里还有 `06:42`「拦住 AI 最容易犯的错误」把"拦错误"写成这个构件的**职能之一**。

**所以质量门 6 现在同时是"教程明文禁止"和"教程 13/13 成品都这么做"。** 这条不能由写作 AI 自己判断，必须先裁定。

**倾向**（供拍板参考，不是结论）：`06:44` 禁的是**结构**——不要"先摆一段错的，再说不对，再说对的"这种教学式并排；成品里的"不要写成 X"是**一句收尾的护栏**，不是先列举后否定。若采纳这个读法，两边不矛盾，质量门 6 保留，但要加一句形态约束：**"不要写成 X"只能作为条目末尾的一句护栏，不能作为条目的开头或主体。** 与 `04` H-02「成因式当梯子不当成品」是同一类形态裁定。

---

## 🟡 意象操作手册

> 量级 ~15 行 / 1-2 轮。**素材**：`素材/构件规格与判据.md` §E。
> ⚠️ **本节经素材提取核查后降级为 🟡**——原表里有四条被写成教程规格，实际教程零命中（见下方"❌ 教程无依据"表）。**教程有依据的部分（3/5/6/7）可开工；无依据的四条卡在 C12，不要照写。**

### 有教程依据（可开工）

| 段 | # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|---|
| 问法 | 3 | 主问句：**"她像什么？"**——"不是外貌像什么。是她存在的方式像什么。"（`07:27`/`:29`，逐字） | 问句 | 两句都在 |
| 问法 | 4 | **截断外部套用**：「不要去网上搜"好看的比喻"然后往角色身上套。意象要从你对角色的理解里自然生长。」（`07:141`，逐字） | 纠偏句 | 用这句原文，**不要**用"不要从'她代表什么'出发"那个说法 |
| 问法 | 5 | 卡住时：给质感方向不给答案 → 用户自己出候选 → 引导者判否并加限定（`07:33`「太消极了」、`:39`「太功利了」）→ 再把 {{user}} 放进比喻 | 有序手法 | 含"引导者不给答案"这条 |
| 质量门 | 6 | **写法四条（逐字取 `07:139-162`）**：原则一 从角色本身长出来 / 原则二 **能承载关系**（角色与 {{user}} 的关系动态放得进这个比喻）/ 原则三 要短 / 原则四 不解释自己 | 清单 | 四条齐全，**第二条必须是"承载关系"** |
| 质量门 | 7 | 完成标准（`07:204-214` 课后笔记的判据部分）：有具体象征物 / **承载关系动态** / 篇幅短 / 无解释性语言 | 可判定 | 同上，不写"承载核心矛盾" |

⚠️ **"承载主要矛盾"是错的。** 教程主路径 `07:147` 写的是「原则二：意象要能承载**关系**。」——"主要矛盾"是平替路径 `11.md:456` 的措辞，两条路径不要混用。**原表格两处都写成了"矛盾"，已改。**

⚠️ **不要凭空补"意象三问"**——教程 `07:25` 宣布"你问自己三个问题"，但全章只给出了第一个（`07:27`）。第二、三问不存在，不要编。也**不要**把 `11.md:398` 的同名三问搬过来充数，那是情绪阈值三问，不是意象三问。

### ❌ 教程无依据（⏳ 卡 C12，不要写）

| 原 # | 原文写的 | 核查结果 |
|---|---|---|
| 诊断 1 | 「**总是尝试**，不跳过」 | **教程零命中。** 这是线 C 的设计（`books/char-palette/PIPELINE_STATE.md:59`、`ARCHITECTURE_DECISION.md:69`），不是教程 |
| 诊断 2 | 「2-3 个方向都不贴才跳过」 | **教程零命中。** 这是线 A `HANDOFF §2.23`。检索"不贴/更糟/硬套/勉强/想不出/找不到意象/可选"，`07.md`/`11.md`/`6.md`/`0.md` 全部无命中 |
| 诊断 2 | 「**不贴的意象比没有意象更糟**」 | **教程零命中**，同上，线 A 原创 |
| 问法 4 | 「不要从"她代表什么"出发」 | **教程零命中。** "代表什么"全库仅 `80问.md:905` 一处且与意象无关。已替换为 `07:141` 的真实原文 |

**教程实际的态度**：意象是全套教程里**唯一没有"可选/不适用"条款的构件**——对比 `03:7`（三面性不是必选）、`04:242`（混色不适合谁）、`05:319`（核心人格层不适合谁），意象章**什么都没说**。所以"总是尝试"和"允许跳过"**都是在填教程的空白**，两边都得靠设计裁定，不能声称教程支持。这一条直接影响 **C12**（MVC 含不含意象）的论证基础。

**位置**：⏳ **C11**。⚠️ **C11 的教程证据已被素材提取动摇**——`06_二次解释篇:162-170` 用与意象篇**逐字同构**的"记忆残留最强"论证主张**二次解释**放最后，且它给的顺序表里根本没有意象。详见 `04_冲突拍板单.md` C11。

---

## ✅ 题库

> 内容确定，可开工。**素材**：✅ `素材/题库.md`（§A 三档定位 / 十组目标声明等）。
> **触发**：用户明确卡壳、开放式追问连续多轮无产出 → 翻题库找新角度。

| # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|
| 1 | **三档定位**：24 问=角色还没轮廓时用 / 80 问=正式整理角色时用 / 200 问=某一块卡住时抽题用 | 一句话一档 | 说清三档是**并列分工**，只有 24→80 是递进 |
| 2 | **按 80 问十组的目标声明分组**，每组带目标句原文 | 分组 + 目标句 | 十组齐全，见下 |
| 3 | 24→80 的升档判据（七条） | 清单 | — |
| 4 | 不达标时只补三块：她的底色是什么 / **她真正缺什么** / 她被触发后会做什么 | 清单 | — |

**十组目标声明（逐字，直接用）**：

| 组 | 目标声明 |
|---|---|
| 一 角色核心 | 这一组解决的是：这个角色到底是什么。 |
| 二 调色盘 | 这一组解决的是：这个角色有哪些颜色。 |
| 三 背景来源 | 这一组解决的是：角色为什么长成这样。 |
| 四 欲望、缺失、恐惧 | **这一组开始进入核心人格层。** |
| 五 防御与决策 | 这一组解决的是：角色遇到压力时怎么运行。 |
| 六 关系与{{user}} | 这一组解决的是：为什么是{{user}}。 |
| 七 三面性与场景 | 这一组解决的是：不同场景下怎么分流。 |
| 八 混色瞬间 | 这一组解决的是：同一个动作里怎么同时有几种颜色。 |
| 九 台词与行为 | 这一组解决的是：角色怎么说话，怎么动。 |
| 十 底线、弧光、二次解释 | 这一组解决的是：角色会不会长出来，又怎么防止AI写偏。 |

⚠️ **教程内部有四处出入，提取时避开**：抽题路由 16 类 vs 10 类 / 一处悬空引用（`角色理解80问AI循环问答工作流.md` 全仓不存在）/ 120-160 问档位只有名字无题库 / 80 问十组与 200 问二十组维度命名不对齐。

---

## ✅ 用户确认信号

> 内容确定，可开工。量级 ~15 行。
> **触发**：MVC 暂停或收束时，检查用户确认信号是否有效。

| # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|
| 1 | 有效确认的类型：正面认可 / 补充确认 / 修正确认 | 分类 | — |
| 2 | **假信号黑名单**：回答变长、用了术语、说"我知道了"、情绪激动——**都不算** | 清单 | 四项齐全 |
| 3 | 敷衍的"嗯"不算确认；AI 引导的"是不是有点 XX？"得到的肯定不算 | 判据 | — |
| 4 | **权重**：修正精度、自发延伸这两类信号权重最高 | 排序 | 理由："模糊的角色只能产生模糊的接受/拒绝，精确纠偏需要清晰的内部参照" |

**来源**：线 B `§4.3.2`（线 A/C 都没有，单线独有但无争议）

---

## ✅ 快速路径

> 内容确定（线 B `G1`–`G9`），可开工。量级 ~40 行。
> **本质**：不是降级方案，是**同一套追问引擎换一套更轻的追问语言**。用户不存在抗拒问题，只有能力高低。

| # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|
| 1 | **切换条件**：深入模式**连续卡住**（换 ≥2 个切面 + 脚手架推演 ≥2 次被否）→ 不追问原因直接切 | 可判定 | 有具体数量 |
| 2 | 切换语：**"闭上眼，你第一个想到的关于她的画面是什么"** | 逐字 | — |
| 3 | **五步流程**：基础信息 → 小传 → 采访 → 情绪阈值 → 意象 | 有序 | 五步齐全 |
| 4 | 每步的追问动作 + 完成标准（AI 从文本可观察） | 逐步 | 每步都有可观察的完成标准 |
| 5 | **核心机制**：把"要求分析"换成"要求描述"——用户写不出"她的底色是焦虑"，但一定能看到"她骂人骂到一半突然停了" | 一句话原理 | — |
| 6 | 小传的**录像带规则**：摄像头拍不到心理活动 + **删词检查**（删掉"她觉得/因为/其实/内心"后画面还完整吗） | 可机械执行 | — |
| 7 | 采访：暴露型问题，完全代入用她的语气；说谎的两种处理 | 操作 | — |
| 8 | 情绪阈值：写**阈值**不写样子，每种情绪三要素（触发/过程/结束），五种喜怒哀惧衰 | 格式 | ⚠️ 教程标题写"喜怒哀乐衰"但正文是"惧"，以正文为准 |
| 9 | **标签优先**（G5）：对能力最弱的用户先收标签**不纠正** → 标签阶段扫矛盾最锋利 → 四问转化出行为画面 → 标签退场 | 有序 | 有"先不纠正"这句 |
| 10 | 肯定 + 追问**成对出现** | 节奏规则 | — |
| 11 | 引导强度按已建立的素材量**动态调整**（初期重引导，素材积累后轻引导） | 规则 | — |

⏳ **待定**：MVC 锚点位置（C12）· 双向切换保留吗（C13——**教程只支持单向递进**，`11.md:491` 升级出口指向调色盘，没写创作中途反向退回）· 与 G6 引导阶梯的分工（C13）。

---

## ✅ 人物文档落盘机制

> 内容确定（线 B `L1`–`L12` + `§12.1`–`12.7`），可开工。
> **素材**：`素材/人物文档骨架.md`（⬜ 待提取，依赖 C10/C11）。

| # | 必须包含 | 形态要求 | 验收 |
|---|---|---|---|
| 1 | 文件名和文档标题都用**角色名**；未定名时 `<角色名/待定>` | 规则 | — |
| 2 | **四层布局**：索引头 → 素材区 → 构建区 → 归档区 | 骨架 | 四层齐全 |
| 3 | **索引头 20 行内**，含：进度统计 / 正在挖（🔄）/ 未深挖清单（📌 ≤5 条）/ 已确认 / 断点记录 / 已推翻简写 | 模板 | 20 行硬上限 |
| 4 | 素材区**四类分组 + 按检索频率排**：关系动态切片 → 行为切片 → 标志性偏好习惯 → 经历事件 | 有序 | 顺序不能改 |
| 5 | **三档状态**：🔄 正在挖 / 📌 已记录未深挖 / ✅ 已消化 | 标记 | — |
| 6 | **轮后静默拆解记录**：按信息点拆（一段 0-5 条），格式 = 原话锚 + 最小加工 + 状态 + 归组 | 操作 + 格式 | 有"原话与整理分栏" |
| 7 | **埋没点登记制**：产生那一刻登记进索引头，不是事后全文搜索。回收**不是清空队列**，是每次选新方向时按需取用 | 机制 | 有"没挖完不丢人"这层意思 |
| 8 | 构建区按依赖链排 + **依赖链注释**（注释里写清"这是文档框架顺序，不是追问顺序"） | 有序 + 注释 | 注释里有那句区分 |
| 9 | **归档区推翻不删除** + 影响范围。**素材是事实不动，构建是解读可推翻** | 规则 | — |
| 10 | **自然语言投影确认**：构件确认时用自然语言给用户看，不用术语格式 | 操作 | — |
| 11 | **记录是事件驱动不是轮次驱动**：特征闭环即记录，每轮不自动写 | 规则 | — |
| 12 | 角色相关的否决 → 原因进素材区；**纯协作否决 → 不落盘** | 规则 | — |
| 13 | **不设独立的过程记录/追问日志区** | 负向约束 | 线 A `§2.4` 与线 B `L12` 独立得出同一结论 |
| 14 | 最终卡导出**用户手动触发**；怎么转世界书格式是 `worldbook-config` 的事 | 边界 | — |

⏳ **待定**：素材过滤要不要同步告知用户（C10——目前"素材静默"与线 A"必须同步"冲突）· 意象在构建区排第一还是最后（C11）。

---

## ⏳ 未定的 reference

| 内容 | 卡在 | 内容本身定了吗 |
|---|---|---|
| 路由逻辑（多入口路由 + 模式切换 + MVC 判读 + 收束规则） | C5、C12、C13 | 部分 |
| **组装期一致性验证** | C5（属于收束环节，环节未定） | ✅ **内容已定，只是没地方放**。见下 |
| 案例库 | ~~C9~~ ✅ 已解锁 + 仍卡"案例选哪几个"（线 B `§八` 三个 🔴） | 否 |
| 常见错误清单 | C2（12 失败模式 + 6 执行风险与 KNOT 1.5 去重后落哪） | 部分 |
| 世界观锚定 / NSFW 动机追问 | C2（线 C 有这两个 probe，线 B 无对应，归宿未定） | 是 |
| 4 诊断的外部问法与质量门 | **三线共同缺口**，C2/C3 一起解决 | 否 |

### 组装期一致性验证（内容已定，等 C5 定落点）

线 A 独有、三线无争议。它是"三种检查三时机"里的**第三个时机**（前两个：线 C 的 4 诊断管每轮输入，线 B 的质量信号 A–G 管 MVC 判停）。

组装/收束时逐条跑：

| # | 检查 |
|---|---|
| 1 | 性格打架吗 |
| 2 | 衍生互相矛盾吗 |
| 3 | 多面性里混入二次解释了吗 |
| 4 | 语料从调色盘移出去了吗 |
| 5 | 混色画面解释情绪了吗 |
| 6 | 防御机制落到行为了吗 |
| 7 | 意象是从核心矛盾长出来的吗 |

**来源**：线 A `§2.13` · 融合项 M-08 第三块
