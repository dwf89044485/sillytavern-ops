# 素材提取清单

> `素材/` 是**按产物切好的教程原文逐字摘录**，让写 skill 的 AI 不必打开教程。
> 提取源：`三线碰撞/_work/01_教程底册_组*.md`（586 条带 `文件:行号` 锚的逐条事实，已做 A/B 类标注）+ 回教程原文补全整段。
> **只提 A 类（追问角色本身的动作）。B 类（教方法论的教学手段）一律不进素材。**

---

## 素材是"引用源"，不是"理解源"

写作 AI **不需要理解教程的方法论**，只需要在施工规格要求它放一句原话时，从素材里取。所以素材的质量标准是：

1. **逐字准确**——不转述、不润色
2. **出处锚齐全**——每条带 `文件:行号`，让它能一键回原文核对（**例外路径**，不是默认路径）
3. **按用途分组**——按"给哪个产物用"组织，不按教程章节组织

---

## 提取时机规则

| 类别 | 什么时候提 | 理由 |
|---|---|---|
| **决策无关** | 随时可提，不会返工 | 无论剩余决策怎么拍，内容都一样，只是放进哪个文件不同 |
| **决策相关** | 对应决策拍完后 | 提早提会因决策变化返工 |

---

## 清单

| 素材文件 | 给哪个产物用 | 形态要求 | 时机 | 状态 |
|---|---|---|---|---|
| **`核心人格层_原文素材.md`** | 核心人格层操作手册（`03` §核心人格层）+ `SKILL.md` 推演权限段（`02`） | 八块 A–H：成品原文 / **完整反推回合** / 行为题 / 硬判据 / 错误示范 / 跳过条件 / 高可能推论区块规格 / 触发表 | D-01 已拍板 | ✅ **已提取**（1660 行 / 321 锚） |
| **`追问原句库.md`** | body 的措辞样本 + 各 reference 的外部问法 | ⚠️ **必须是完整追问回合**（问→答→再问→落成），不是孤立问句。按"探什么"分组 | 决策无关 | ✅ **已提取**（3197 行 / 443 锚） |
| **`构件规格与判据.md`** | 调色盘 / 多面性 / 混色 / 二次解释 / 意象 各手册 | 每构件：定义逐字 + 部件清单 + 判据 + 正误对照的**判据部分**（不含对照展示） | 决策无关 | ✅ **已提取**（3319 行 / 554 锚） |
| **`题库.md`** | 题库手册 | 按 **80 问十组目标声明**分组（"这一组解决的是：……"），每组带目标句 | 决策无关 | ✅ **已提取**（4466 行 / 719 锚） |
| `诊断判定树.md` | body 的 4 个基础诊断 | 判定树 + 灰色地带 + 正例反例 + 跨诊断优先级。**外部问法与质量门要分开列** | 依赖 C6（扁平检测去留） | ⬜ |
| `案例原文.md` | 案例库 | ⚠️ **必须含追问对话过程**，不能只有方法论产物 | ~~C9~~ ✅ 已解锁；仍等"案例选哪几个" | ⬜ |
| `反例与失败模式.md` | 常见错误清单 | ⚠️ 只要**判据**，不要正误并列对照表 | 依赖 C2（去重后落哪） | ⬜ |
| `人物文档骨架.md` | 落盘机制 | 可直接抄的骨架模板 | 依赖 C10、C11 | ⬜ |

---

## `追问原句库.md` 的额外要求（这一份最重要，单独说）

**追问品味是这个 skill 唯一没法靠规格传递的东西。** 线 B 明确判定它必须在 body 内化，而不是外置成范例库。

所以这份素材不能只给问句列表，必须给**完整回合**：

```
【探底色】
引导者的问句：<逐字>
用户的回答：<逐字>
引导者的反应：<逐字>
最终落成什么：<逐字>
出处：<文件:行号>
```

按**探什么**分组（底色 / 主色调 / 点缀 / 多面性触发 / 混色触发 / 意象 / …），每组 2-4 个完整回合。

**同时要提"引导者卡住时做了什么"的回合。** 引导者代劳的比例随层次递减，这个梯度本身就是要传递给写作 AI 的行为规范，不提就丢了。

⚠️ **数字已按实测更新。** 原先这里写的是"42 个实例 / 65% / 25% / 0%"，那组数来自更早的一份分析、口径不明。提取时重新逐条数过（逐条台账在 `追问原句库.md` §B0-2，可复核）：

| 层次 | 卡住实例 | 代劳 | 比例 |
|---|---|---|---|
| 基础信息 | 5 | 3 | 60% |
| 调色盘 | 5 | 1 | 20% |
| 多面性 | 2 | 1 | 50% |
| 混色 | 1 | 0 | 0% |
| 核心人格层 | 11 | 2 | 18% |
| 二次解释 | 0 | — | 无实例 |
| 意象 | 8 | 0 | **0%** |
| **合计** | **32** | **7** | **22%** |

口径：卡住 = 作者交出的内容被判不可用／答不出／反问要答案／提出的写法被否；代劳 = 引导者没把球抛回去，直接给出答案文本。**递减梯度成立，意象层 0% 与旧数完全吻合**；总数与分层比例与旧数不符，以本表为准。

（核心人格层部分的完整回合已在 `核心人格层_原文素材.md` 的 B 段，`追问原句库.md` 用索引表交叉引用，不重复。）

---

## 不提取的东西（明确排除）

| 不提 | 为什么 |
|---|---|
| 正误并列对照表 | B 类教学手段。只提其中的**判据**，不提对照展示形式 |
| 课后笔记 | B 类教学检验 |
| 术语纠偏整节（"三面性不是三种性格"） | B 类。skill 不澄清术语 |
| 比喻教学（梵高的星空 / 操作系统 / 超级压缩包 / 七个频道 / 输入法联想） | B 类原理讲解 |
| "AI 会怎么误读"的机制讲解 | B 类。skill 直接执行检测，不解释检测背后的模型行为 |
| "90% 的人都写错了"式教学修辞 | B 类 |
| 教程内部的出入本身 | 已裁定过，给写作 AI 只会引起重新纠结。裁定结论写进施工规格 |

⚠️ **但"教程明确标为错误示范的具体句子"要提**——它们是给写作 AI 的反面判据。不提的是围绕它们的教学论述和并排展示形式。

---

## 提取中发现教程内部矛盾怎么办

**如实记录在素材文件的附录里，不要合并、不要选一版偷偷用。** 然后在**施工规格里裁定**它。

已发生的实例：`核心人格层_原文素材.md` 发现 `7.md:344-347` 教"从背景找核心恐惧"用的示范，恰恰是 `7.md:92` 判为错误的成因式句式。这条已在 `04_硬约束与红线.md` H-02 裁定——**成因式表述可以当梯子，不能当成品**。

**第二轮（追问原句库 / 构件规格与判据 / 题库）新记 34 条**，分布在三份素材各自的附录里。其中 **5 条已回流改了上游文档**，回流记录见 `三线碰撞/99_过程日志.md` Phase 10：

| 发现 | 回流到哪 |
|---|---|
| `06_二次解释篇:162-170` 用与意象篇**完全相同**的"记忆残留最强"论证主张二次解释放最后，且其顺序表根本没有意象 | `04_冲突拍板单.md` C11 教程证据 |
| 意象的"总是尝试 / 2-3 方向都不贴才跳过 / 不贴的意象比没有意象更糟 / 不从'她代表什么'出发"**四条教程零命中** | `03_references施工规格.md` §意象、`04` C12 |
| 意象写法第二条教程原文是「**承载关系**」，不是"承载主要矛盾" | `03` §意象（已改） |
| 「往里走一层**最多下探两级**」不是教程明文，且与 `05:116`「核心恐惧要挖到底层」冲突 | `04` C13 教程证据（已撤） |
| 二次解释「每条至少含一句'不要写成 X'」与 `06:44`「不要先列举错误然后否定它」直接冲突 | `03` §二次解释（待裁定） |
