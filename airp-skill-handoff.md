# AIRP 人物生境 skill · Handoff

> 这份文档给接手继续构建这个 skill 的 AI 看。
> 教程原文（方法的最终来源、冲突时的仲裁依据）：`/Users/josephdeng/Documents/sillytavern/AIRP人物生境教程整理.md`
>
> **重要前提：落地文件未定稿。** SKILL.md 和 layer-1 都已经过独立审查（2026-08-17，五个独立 agent 拿教程原文逐条核对），**确认存在多处教程依据错误和结构性问题，均未修**。这份 handoff 是设计意图的显式记录，不是已落地文件的对账单——它和落地文件是同一个主 agent 在接近的认知水平下写的，可能有同样的盲区。**真正的地面真相是教程原文。** handoff、落地文件、教程三者冲突时，以教程为准；教程没覆盖的（标注「主 agent 判断」的），留给用户裁。
>
> **审查报告在 `.claude/skills/char-habitat/review/round-1-review.md`**（完整证据、教程原文对照、agent 结论的采纳/驳回及理由）。§七 是它的结论摘要，§5.10 是它引出的未决事项。改任何已落地文件前先读那份报告。
>
> handoff 里的设计决策分两种可信度：
> - **有教程依据**——标注了教程小节，可信，写文件时遵循。
> - **主 agent 判断**——标注「无教程直接依据」，可质疑，发现不对就提出来。

> **如何往这份 handoff 里加内容**（文档持续更新，加东西前先判断是哪一类，别混记）：
> - **已定决策**（结论清楚了）→ 进 §五，用四段式（定了什么/为什么/操作要求/注意），标可验证性。落地状态不进 §五，进 §七。
> - **决策做了一半**（有想法没定论，或部分定部分卡住）→ 进 §五末尾「在途/未决」区，标「未定」。写清：已想到什么、卡在哪、当前倾向。不强行凑四段式——半成品如实记半成品。定全后升级进 §五正式条目，从在途区移除。
> - **工作进度**（文件写到哪了）→ 进 §十，用 `[x]`已定稿 / `[~]`已写未审 / `[ ]`未做。
>
> 记录原则（防重蹈上一版"审查报告流水账"的覆辙）：
> 1. **记决策和理由，不记思考过程。** "我怎么想到的""几个 agent 怎么讨论的"不写。接手者要的是结论和为什么，不是得出结论的过程。
> 2. **决策变了直接改原文，不留"修正"痕迹。** 别写"此处修正了前面的过度概括"——直接把前面改对。让接手者读一遍，不是读两遍对照。
> 3. **不假装已落地。** 记的是设计意图，落地状态进 §七标"待验证"。
> 4. **标可验证性。** 能追溯教程的标出处；自己加的标「主 agent 判断」。
> 5. **说人话，不造词。** 内部讨论词（"转译思想""微原理"等）不写进 handoff。

---

## 一、这是什么

**目标**：把《AIRP人物生境教程整理.md》的人物生境写法，转成一个 Claude skill（`char-habitat`）。skill 让 AI 内化这套方法后，以「懂方法的引导者」身份，带着未必懂方法的用户，把用户的人物构想整理成一份能让演绎模型演活的角色卡（世界书正文）。

**方法来源**：教程作者经上千次实测提炼，核心是四层整理（基础信息 / 生活结构 / 人物性情 / 场景表达）+ 一套判断纪律（什么该固定、什么该留空、什么是过程不入产物）。它要解决的问题：AI 直出人设容易把人物做成木偶（标签吞人、动作复读、补造事实、套公共模板），人物生境写法让人物「能在陌生情境继续生活」。

**最终产物**：一份干净、连贯、自然的人物正文，放进 SillyTavern 给演绎模型用，让模型演出一个连续、变化、可迁移、独立、有时间连续、能成长的人。

**成功标尺（产出的人要过这六关，也是审查产物的主标尺；六条细化见教程 2.3，绝对不入产物）**：
连续性（换场景还认得出是同一个人，没换成公共模板）· 变化性（不把一组动作当永久表演）· 迁移（碰上没写过的事也能作出合乎她的选择）· 独立性（{{user}} 不在场她仍有自己的事）· 时间连续（发生过的事成为后续经历，不回到起点）· 成长（能被经历改变，改了也还是她）。

skill 本身的成功还要看：AI 真能带着不懂方法的用户产出有质量的卡；AI 自己不犯教程实测暴露的错（补造事实、象征化、心理自白、强行反差、动作绑定）；产物不含方法/过程痕迹。

---

## 二、设计原则（用户给定，不可违背）

### 2.1 两条转译思想

这是整个 skill 的设计公理，SKILL.md 体现了但没显式声明。接手者若不知道这两条，写层文件时可能滑回「教程口吻」（解释方法论原理、展示正误对比）或「执行器模式」（让用户填表自动吐卡）。

1. **skill 不是给人读的教程。** 教程的使命是让人理解方法；skill 的使命是让 AI 按方法工作。教程教人学会理论自己塑造角色，skill 是把教程内化后 AI 直接带着用户塑造。
2. **AI 是引导者不是执行器。** AI 懂方法，用户是创造角色但未必懂方法的人。AI 用方法引导（提问、证据、建议、必要时基于用户素材代笔草稿），用户做决定。用户不需要懂方法就能在 AI 引导下产出好角色。教程里「供作者反思的问题」在 skill 里变成 AI 问用户的问题——用户回答就是在塑造角色，但不需要知道这是在挖哪一层。

### 2.2 起草边界

引导为主，必要时代笔草稿。用户在某层给足素材后，AI 可基于用户回答起草整段，标「草稿，请确认或改」。用户是最终决定者。

代笔的三条硬约束（防用户对着流畅草稿点头了事，最后八成内容是 AI 按标签推的公共模板；教程原话「未经作者确认的采访回答，只是探索中出现的一种可能，不会因为由模型说出便自动成为人物事实」）：
1. 只许重组用户说过的，不许补他没说的细节。
2. 碎片信息不代笔，继续问。素材够的标准是这一层该有的用户已讲出来，AI 只是帮理顺文字。
3. 代笔后逐段确认，不整段确认。

### 2.3 入口

必须支持三种入口：已有原始资料要整理 / 从零开始创造 / 粗糙旧卡要重构。三种入口的具体处理见 §5.9。

### 2.4 范围

现在专注「塑造角色」本身。转平台格式（SillyTavern 角色卡字段、世界书条目格式等）是另一件事，不属于塑造，有触发机制即可，不展开。

---

## 三、产物铁律（写任何文件前先读这条）

**最终产物只留一个干净、连贯、自然的人。** 以下全部不入产物（教程 2.1/2.3/4.14/5.7/5.8/6.3/6.5/6.6）：

- 演绎指令（「应该怎样演绎」「要充分表现」等）
- 工程分析词（指挥回复的工程术语）
- 栏目说明（「基础信息负责…」这种结构说明）
- 实验术语（方法论的实验术语）
- 作者的检查过程、检查方向（2.3 的观察方向是作者自用，不入世界书）
- 向模型解释这套方法（方法解释）
- 反思提问（供作者认识人物的提问，不入最终人设）
- 采访、小传、探索过程、原始问答、长篇生平（退回写作后台，不进角色卡——对应「角色档案」承载过程）
- 共同世界规则、剧情、关系阶段、地点任务、用户资料、平台输出要求（归其他模块）

**为什么**：人设对模型是「参与后续生成的上下文」，不是记忆库，也不是规则清单。写进提示词的每一个例子，都可能同时成为内容示范、语言示范和镜头示范——模型会学着生成。所以混入任何方法论痕迹（指令、术语、栏目、方法解释），都会被模型当成要生成的东西，污染人物。产物只能有那个人。

> 这条铁律的完整禁入清单是 `to-final-text.md` 的核心内容之一。`to-final-text.md` 还没写，目前这份清单是唯一完整记录——写那个文件时照它来。

---

## 四、文件架构

### 4.1 命名

- 文件名/目录名用英文（路径标识符，稳）。
- 文件内容全中文。

### 4.2 文件树

```
<char-skill>/  （实际目录：.claude/skills/char-habitat/）
├── SKILL.md
├── references/
│   ├── layer-1-basic-info.md
│   ├── layer-2-life-structure.md
│   ├── layer-3-temperament.md
│   ├── layer-4-scene-expression.md
│   └── to-final-text.md
└── assets/
    ├── character-archive-template.md   ← 角色档案模板（四层骨架+四状态标注+待定区）
    └── samples-and-skeleton.md          ← 正确示例与标签骨架（card-template 与 correct-samples 已决定合并）
```

> **命名待统一**：layer-1 §九 引用的是 `assets/character-archive-template.md`，SKILL.md 路由表曾用 `assets/character-file-template.md`，handoff 旧版用过 `card-template.md`。三个名字指角色档案模板。本 handoff 统一用 `character-archive-template.md`（与 layer-1 一致，因 layer-1 经用户审改）。改 SKILL.md 路由表时同步。

### 4.3 各文件职责

**SKILL.md（常驻，工作流编排）**
AI 立场（引导者，用户是决定者，可代笔草稿）· 三种入口分流 · 四层判断序列（何时进/跳/合并某层）· 角色档案（怎么建、怎么往里积累素材）· 从素材转世界书的指引（路由到 `references/to-final-text.md`）· 跨层错误常驻（补造事实是唯一真正跨层的失败模式）· 自检 + 制卡触发 · 指向各文件的路由指针。

**references/layer-1 ~ layer-4（按需，每层一个文件，自包含）**
每层文件包含四部分（设计规格，来自 §五决策）：本层方法说明 · 本层提问 · 本层易错 · 本层示例。（注：layer-1 实际写成了九节，超出四部分规格——见 §八已知问题，待裁切/归类定夺。）
加载时机：AI 进入某一层深入引导、SKILL.md 速查不够时，读对应层文件。

**references/to-final-text.md（按需，最后一步才加载）**
转写流程 · 删减检查（教程 6.4 五条自查为主）· 产物禁入清单。第一条指令必须是「先列出哪些素材是用户确认的、哪些是 AI 推的用户没回应、哪些是有意留空」，然后才准动笔（见 §五决策）。

**assets/character-archive-template.md**：角色档案模板，四层骨架 + 四状态来源标注 + 每层待定区。

**assets/samples-and-skeleton.md**：正确示例（转写好的正文长什么样，刻意不等长，给口吻锚）+ 标签骨架（纯 XML 结构，去占位符）。两者合并因转写时需同时看格式和内容示范。

**assets/checklist.md**：检查清单，转世界书时逐项过。主体是教程 6.4 五条删减自查 + 产物痕迹排查。

---

## 五、已定设计决策清单

每条四段：**定了什么 / 为什么 / 衍生的操作要求 / 注意**。注意栏写落地文件可能偏离本决策时的处理（以教程原文+用户为准，不默认本决策对）。

### 5.1 按层切文件，不按内容类型切

- **定了什么**：文件按四层组织（每层一个文件，含方法+提问+易错+示例），不按内容类型拆（所有方法合一、所有提问合一、所有错误合一、所有证据合一）。每层一个文件，不合成一个大文件带目录。
- **为什么**：同一工作流时刻被同时需要的内容放一起——AI 处理某一层那一刻，同时需要该层的方法、提问、易错、证据，按类型拆成四文件要跨文件各读一节，割裂。教程原文本身就按层组织（提问、错误分析、角色证据都嵌在所属层章节内）。分开还能在可省略某层时直接不加载该文件。
- **注意**：曾有人提议「四层合成一个 method.md 带目录」，已驳回（理由：合并后 400-600 行，四层顺序推进永远不需同时拿四层，分开是每次读一百多行 vs 每次读五六百行）。如无新信息不重开。落地文件如偏离此结构，以本决策+教程组织方式为准。

### 5.2 跨层错误常驻 SKILL.md，层特定错误跟层走

- **定了什么**：层特定错误（心理自白主要犯性情、动作复读主要犯场景表达、象征化主要犯生活结构/性情）跟层走。只有「补造事实」真正跨层（哪层都可能犯），常驻 SKILL.md。
- **为什么**：补造事实是实测里最稳定复现的失败（Claude 给只有「优秀兄长」的贺明珠凭空加弟弟、编旧信年代/任职年限/公司领导/童年时间表），模型缺信息时的本能是补不是留空。
- **衍生的操作要求**：防补造事实的机械手段是「让空白有地方待着」——角色档案每层底下留「本块待定」、末尾留「全局待定」（教程「空白并不是人设缺陷」的落地）。没有答案往那儿放，不填。
- **注意**：layer-1 目前未实现「本块待定/全局待定」机制（见 §八）。写 layer-2~4 时要含此机制。

### 5.3 转正文独立成 `references/to-final-text.md`，最后一步才加载

- **定了什么**：转正文单开一个文件，按需加载，不写进 SKILL.md 常驻，也不拆成独立 skill。
- **为什么**：转写时必须知道每条素材的确认状态，这信息只在对话过程里，换会话就丢，等于蒙眼做手术——独立 skill 不可行。写进 SKILL.md 常驻有慢性病：AI 前期引导时就带着「这段以后好不好删」的念头，会提前替用户砍还没想清的素材。更根本的是两阶段判断标准互斥——前期要「欢迎模糊、空白是对的」，转写要「不容忍模糊、果断删」，同一时刻只该激活一套，按需加载实现同会话内的时间隔离。
- **衍生的操作要求**：这个文件第一条指令必须是「先列出哪些是用户确认的、哪些是 AI 推的用户没回应、哪些是有意留空」，然后才准动笔。主体是教程 6.4 五条删减自查 + 产物禁入清单。
- **注意**：该文件尚未写。写时照此要求。

### 5.4 角色档案按四层做骨架 + 四状态来源标注 + 待定区

- **定了什么**：档案骨架用四层（标题说人话：`一、她是谁` / `二、她的日常生活` / `三、她的性格与内心` / `四、她怎么说话做事` / `五、全局待定`），每层底下有「本块待定」。每条素材标来源（AI 后台做，用户看不见，标到段不标到句）。**不**按教程 4.12 的事实/推断/空白三分法做骨架——四层和三分法是垂直两维度（四层管「哪类内容」，三分管「多确定」），三分法做骨架会导致一条素材分两次类。
- **为什么**：来源标注是防补造事实的机械手段，也是转正文时唯一可靠的判断依据。判断依据只存在于对话过程里，对话可能跨天/跨会话/被截断，档案里不标，转写时分不清哪条是用户亲口说的、哪条是 AI 推的。
- **衍生的操作要求**：状态设四个（比三分法多一个，因「跨天转写记录已丢」和「用户会对草稿点头了事」两条风险）：
  | 标注 | 含义 | 能否进产物 |
  |---|---|---|
  | 「你说的」 | 用户直接说的 | 能 |
  | 「一起定的」 | AI 提出、用户确认或改过 | 能 |
  | 「我起草的，等你确认」 | AI 代笔，用户未过 | **不能** |
  | 「先空着」 | 没答案，等剧情 | 不能 |
  用户改过草稿后状态从「我起草的」升成「一起定的」。
- **注意**：状态标注系统是用户已拍板的核心决策，但 layer-1 目前完全未提及（见 §八）。档案模板文件未建。

### 5.5 教程第二部分（模型原理）不新开文件，压成三条硬约束进 SKILL.md

- **定了什么**：教程第二部分（讲模型原理，不属四层任何一层）压成三条硬约束写进 SKILL.md 常驻，不单独开文件。外加六条检验方向（教程 2.3），明确标注绝对不入产物。
- **为什么**：第二部分不属于四层任何一层，文件树里没有它的位置。其中「写进提示词的每个例子都可能同时成为内容示范、语言示范和镜头示范」这条，直接决定各层文件里的示例怎么写——AI 写示例时看不到这条，示例本身就成污染源。三条硬约束：① 例子会被模型连句式镜头一起学；② 人设是每次参与生成的条件不是记忆库；③ 稳定不等于永远做同一个动作。
- **注意**：SKILL.md 是否已正确体现此决策——待验证。

### 5.6 追问停止条件：同一个点最多三轮

- **定了什么**：同一个点最多追三轮，用户给不出更多就接受，标进「本块待定」，往下走。
- **为什么**：教程没有这个（它假设作者自己深挖，不存在把用户追跑的问题）。不设停止条件，用户说「她很温柔」AI 追问十轮，用户觉得「我只想写个角色怎么变成心理咨询」然后放弃。
- **注意**：主 agent 判断，无教程直接依据。可质疑。layer-1 目前未提及（见 §八）；这条跨层通用，更适合放 SKILL.md。

### 5.7 card-template.md 与 correct-samples.md 合并

- **定了什么**：这两个文件合并成一个 `samples-and-skeleton.md`。
- **为什么**：转写那一刻，AI 要同时看格式（标签骨架）和内容示范（正确示例），同一时刻需要的放一起。
- **注意**：合并决定已下但未执行。SKILL.md 路由表仍分列两项，layer-1 §九 仍引用独立的 `correct-samples.md`——都待同步。合并后的文件尚未写。

### 5.8 透明度三层 + 风险提示与正面对比各占一半

- **定了什么**：AI 透方法给用户走中间态——透明引导。三层透明度：方向（默认，每进新阶段一句话说清在干嘛）/ 决策（岔路，遇分类歧义或用户答案有坑，2-3 句点出 tradeoff 让用户选然后切回）/ 原理（用户问才讲，2-3 句讲透然后下一句必是创作问题或动作）。硬约束：不许连续两段讲原理。用户说「别解释了直接写」降到纯引导；用户连问三次以上「为什么」可问「要我把这套方法系统讲一遍吗？还是继续写角色？」。
  风险提示式（「如果不这样做会怎样」）和正面对比（「加了这层之后人物变成什么样」）**各占一半，不定主导形式**。
- **为什么**：纯黑箱用户无法纠偏、离开 AI 不会写；全程教学打断创作、用户来是写角色不是上课、教程已存在不需第二个。最有力的范例全来自教程的实际输出对比和实际翻车记录（程雾有/无性情、唐酥有/无场景表达、贺明珠补造弟弟、程雾摘眼镜复读）——微原理不来自方法论推导，来自实测中发现的问题。
- **注意**：曾有一版措辞说「风险提示式优先」，后纠正为「各占一半不定主导」。**layer-1 §6.2 用的仍是纠正前的「优先」措辞（见 §八）**——以「各占一半」为准。两个层面的 why 必须区分：写给 AI 的 why（建判断框架，用户看不到）vs 透给用户的 why（赋能决策，极简行动导向），不能混。

### 5.9 三种入口，从零起手不专门照顾空白用户

- **定了什么**：支持三种入口（已有素材整理 / 从零创造 / 旧卡重构）。「从零起手」里用户脑子里完全空白的，不专门照顾——这种用户也不会来用。SKILL.md 写成：这个 skill 从素材开始工作，什么都没有的先请他说一句台词 / 一个画面 / 一件正在较劲的事，从那儿起手；不负责替用户凭空造人。从零起手时抓住用户能说出的那一点往外推身份/生活/性格，别从「她叫什么、多大」开始盘问——那样用户会顺口编，编的身份上再编性格，两层没真实因果。
- **为什么**：用户原话「这种人也不会来用，不用花精力」。从零盘问会编出两层无因果的内容，角色内部对不上。
- **注意**：用户已拍板。这条已写进 SKILL.md 入口分流——待验证是否体现到位（§7.1）。

### 5.10 在途/未决事项

> 记于此处的是**有想法没定论、或部分定部分卡住**的事。**不要塞进 5.1–5.9 已定决策里**（会污染已定决策的可信度）。定全后升级为 §五正式条目（四段式 + 可验证性标注），从本区移除。

以下六条来自 2026-08-17 的独立审查（完整依据见 `review/round-1-review.md`），**都需要用户拍板才能动**。

#### [未定 1] 删减检查从流程末端下沉到每层收尾 ← 最重要

- **卡在哪**：教程的写作哲学是**删除即写作**（6.4 标题「用删减完成角色」；6.7「当新增文字只是在重复证明她已经成立，写作就应当停下」；开篇第一句「用**尽可能少**的内容」）。但 skill 的结构是「逐层引导 → 素材**攒**进档案 → 最后转写时删」——教程的默认方向是往外拿，skill 的默认方向是往里放。§5.4 的四层档案 + 四节模板 + 逐层推进流程共同构成「填满」的引力，SKILL.md 里那句「四层不一定都要写」是口头声明，抵不过结构。
- **三个独立 agent 从三个角度撞上同一件事**：① 顶层设计审查直接指出这是结构性相悖，不是措辞问题 ② 教程覆盖度审查发现**夏燃案例整个丢了**——教程用夏燃证明「不写比写更好」（3.7「生活结构在她身上的新增价值很小」；5.9「不是必须继续加重提示的失败，而是说明前一层已经足以支撑，场景表达可以删短」），这是教程唯一系统性证明「少写是对的」的案例 ③ 教程 3.7 后三条结论丢了，尤其「写得过多、过满或过度解释时，也会增加模型复读、补写和忽略其他信息的机会」。
- **当前倾向**：不推翻四层结构。把教程 6.4 的删减检查从流程末端移到每层收尾——每攒完一层就问「这层有没有只换说法不换作用的内容？有没有删掉也不影响角色判断力的内容？」，让减法成为每一步的默认动作而不是最后的总清算。同时补夏燃案例作为「可以跳过」的实证支撑。
- **教程依据**：6.4、6.7、开篇第一句、3.7、5.9。这条不是主 agent 发明，是教程本来的重心。

#### [未定 2] 十一条判据模糊怎么处理

- **卡在哪**：流程的核心判据全是定性说法，不同 AI 会做出完全不同的判断。严重的七条：「素材够的标准是这一层该有的东西他已经讲出来了」（"该有的东西"没定义）·「身份标签已经足够带出具体生活时」·「前三层已经能让模型自然推出她怎么开口」（要 AI 模拟一个它不认识的模型）·「模型现在缺什么」（四层推进的核心判据）·「这个人已经能在陌生场景继续生活」（决定"攒够了"的终止条件）·「碎片信息」vs「素材够了」的边界 ·"空洞"的判据。中高两条：「一旦能预判了说明性情越界」的阈值 ·「删了会不会误判」的阈值。中两条：「只纠会真正造成后果的错」·「关键域」。
- **当前倾向**：不硬造判据。很多"模糊"是教程本身就定性的（教程是给人读的，人有直觉），硬造数字会把方法做死。倾向逐条决定走哪条路：给可操作判据 / 给权衡框架（明确说「这里靠判断，以下是要权衡的东西」）/ 明确交给用户定。
- **注意**：这是量最大的一块，建议单独一轮处理，不要混在改错里。

#### [未定 3] 「动态记录」等四个词要不要定义

- **卡在哪**：SKILL.md 说过期状态「交给开场、正文历史或状态记录」；layer-1 §3.2 说「开场、按时间排列的正文历史或会随故事更新的状态层」；§5 说「交动态记录」；§8.1 说「交给开场、正文历史或动态记录」。四个概念提了多次、**零处定义**，而且三处用了三个不同的词（状态记录 / 状态层 / 动态记录）指同一件事。AI 只知道"别写进静态人设"，不知道该写到哪去。
- **当前倾向**：统一成一个词，并说清这是 skill 范围之外的东西（属于 §2.4 说的"另一件事"），AI 的责任止于"识别出这条会过期、不放进档案的静态部分、告诉用户它该去哪"。但要不要在 skill 里给个最小定义，待定。
- **教程依据**：教程 1.3 说这些该「出现在首次开场、按时间排列的正文历史、剧情阶段记录或会随故事更新的状态层中」——教程自己也是列举，没定义。

#### [未定 4] 「演绎指令」的边界

- **卡在哪**：§三 产物铁律第一条就是「演绎指令不入产物」，SKILL.md 多处强调，但**从没定义什么算演绎指令**。「她表现得很克制」是性格描述还是演绎指令？「她说话总是很温柔」是事实还是指令？AI 只能凭感觉，而感觉偏向保留（删掉可能丢信息，保留至少"安全"）。
- **当前倾向**：给一条判断线而不是清单——大意是"这句话的主语是角色还是模型"。「她说话总是很温柔」主语是她（描述），「要充分表现她的温柔」主语是模型（指令）。但边界情况（「她表现得很克制」）还是模糊，待定。
- **教程依据**：教程 4.14「这部分不写模型应该怎样演绎，也不使用工程分析词指挥回复」、5.7「不需要塞满'应该充分表现''避免过度克制''增加大动作'等演绎要求」——教程给的也是举例，没给判断线。

#### [未定 5] 用户自己拿不准的内容归哪个状态

- **卡在哪**：§5.4 的四个状态（你说的 / 一起定的 / 我起草的等你确认 / 先空着）没有覆盖"用户自己也不确定"——用户写「可能是因为…吧？」。标「先空着」？追问确认？还是加第五个状态？
- **当前倾向**：不加第五个状态（状态越多标注负担越大）。倾向归入「先空着」，因为它和「先空着」共享同一个后果：不能进产物。但这样会丢掉"用户有个模糊方向"这个信息，待定。

#### [未定 6] 四处内部矛盾的取舍

- ① **「不当场分类」vs「把素材归位」**：SKILL.md 同一段里既说「你先全部接住，不当场分类不当场纠错」又说「然后建角色档案，把素材归位」——归位就是分类。没说时间边界。
- ② **「用户是决定者」vs「碎片信息不代笔」**：用户说「你帮我想吧」但给的是碎片时，两条同时触发导向相反，没给优先级。
- ③ **档案给不给用户看**：既说「他不需要知道自己在填哪一层」，又说档案「标题说人话，用户扫一眼就懂」。若不可见，"说人话"没意义；若可见，标题本身就暴露了分层。
- ④ **「逐层引导」是默认做法还是硬底线**：教程 6.1 的顺序暗示逻辑依赖（先有坐标才有意义）；但 §5.9「从零起手」说「从这一点往外推身份、生活、性格」暗示可从任一点切入。用户说「别问基础信息，我直接告诉你她什么性格」时，AI 不知道该坚持还是接受。
- **当前倾向**：① 归位是 AI 后台动作、分类是对用户说的话，把两者措辞分开就解决 ② 用户授权优先，但代笔时明确说「我只能按你说的这些写，可能会比较薄」③ 档案对用户可见（用户随时能要来看），所以"说人话"有意义 ④ 逐层是默认不是底线，用户要跳就跳，但 AI 提一句"少了坐标后面可能得回头补"。四条都待用户确认。

#### [未定 7] 提问框架：从「填档案」转向「服务演活」← 影响所有层的提问怎么写

- **卡在哪**：layer-1 §七 提问按六域组织（每域给追问目标+示例问法+深浅+边界路由），用户批评「为问而问」——举例「医生身份自己选还是家里安排」单独看不知道问的目的，即便说「探关系背景」也不知为什么要探、探到对演绎有什么用。这是整个 skill 的问题：问什么、为什么问、服务于什么、切入角度、深度、时机，涉及访谈方法论。
- **已想到什么**（本会话：主 agent + knot 提问方法论 + 4 个 agent 研究 dot-skill）：
  - 方向：提问依据从「域→问」（填档案）转向「人物要怎么演活→需要什么→问什么」。一个问有没有价值，看它的答案会不会改变模型在用户关心场景里怎么演她。
  - knot 四步法（拟写进 SKILL.md 常驻）：定向（故事方向/角色核心/素材盘点）→ 诊断（模型在什么场景卡住）→ 提问（每个问能说清「知道了 X，模型就能做 Y」）→ 停止（角色能继续生活就停，不是档案填满）。
  - dot-skill 实证（它是离线蒸馏不是交互引导，但「问完怎么用」有参考）：①产物结构倒推提问（每提取维度对应产物一节，不存在无去处信息）②celebrity 问「你要他帮你做什么」作价值锚点 ③标签→行为规则翻译 ④决策函数（优先级+触发条件，非预写答案）⑤三重门验证（跨场景复现/生成力/排他性，筛通用信息）⑥矛盾保留不强行统一 ⑦诚实降级（不足标「原材料不足」不编造）。完整综合和主 agent 思考已落在本 handoff 本条，不另存文件。
  - dot-skill 没给、必须自造：追问引擎、访谈技巧（跟随用户关注点/用用户语言/从具体到抽象/留沉默/问「为什么重要」不问「是什么」）、显式价值判断。
- **三个待用户定**：①价值标准用不用「三重门」简化版（排他性+生成力，去掉跨场景复现因用户口述难满足）②标签翻译建不建显式翻译层还是靠追问追到行为 ③矛盾保不保留显式「分类+保留」机制。
- **关联**：和 [未定 1]（减法 vs 加法）、§7.4 失败场景 2（事实域太详细逼用户填）同根——都是「怎么不让 AI 为填表而问」。改 layer-1 §七 提问部分前先定这条。
- **教程依据**：2.3「能在陌生情境继续生活」是终极目标，4.6「写倾向不写唯一答案」，4.11「停在根部」。四步法/dot-skill 借鉴属主 agent 判断，无教程直接依据。

---

## 六、写层文件须知

### 6.1 layer-1 是部分参考，不是定稿范本

layer-1 经过用户审改但未定稿，方向较接近用户想要，细节仍可能有遗留问题。写 layer-2/3/4 时：

- **照做的（结构约束，来自 §五设计决策）**：四部分基本框架（方法说明/提问/易错/示例，§5.1）· 三层透明度（§5.8）· 风险提示与正面对比各占一半（§5.8，注意 layer-1 这条写偏了）· 两个层面 why 区分（§5.8）· 微原理用实测证据（§5.8）· 四状态标注系统（§5.4）· 每层待定区（§5.2）· 代笔三条硬约束（§2.2）· 追问三轮停止（§5.6）· 产物禁入清单（§三）· 精选范例的调子（一句嵌追问、口语化、有具体角色名和画面感，不列清单式问法）。
- **只参考可质疑的（layer-1 作者发挥，非设计决策）**：事实域映射（必要域/条件域）、三组核心边界（事实vs解释/静态vs动态/识别vs美感）、「过度排除」对称风险、「中间态」分类、和其他层的分界独立成章、关系坐标独立成章、克制纪律四条具体规则（先接住再整理/只纠会造成后果的错/一次最多提醒一件事/用我们不用你）、用户拒绝 AI 判断时的处理流程、易错分两档（实测证据/机制推断）、提问按六域组织、每层「完成标准」的具体表述、三种入口在基础信息层的操作差异。
  这些是 layer-1 作者从设计精神推导出的执行细化，方向多对，但不是设计决策——写 layer-2 时可参考，发现不对可质疑，不必照抄。

### 6.2 教程 → layer 映射（写每层时去读对应教程小节）

教程六部分结构整齐，四层各自对应一个完整部分：

| skill 文件 | 对应教程部分 | 关键小节 |
|---|---|---|
| layer-1 基础信息 | 第一部分（1.1–1.6） | 1.1必要信息 / 1.2条件信息 / 1.3静态与动态 / 1.4外貌 / 1.5关系坐标 / 1.6推荐结构 |
| layer-2 生活结构 | 第三部分（3.1–3.7） | 3.1可供行动的现实 / 3.2什么可入选（六类）/ 3.3不是日程表物品栏世界书 / **3.4与基础信息性情的区别（重叠节，写 layer-2 必读）** / 3.5用较少内容提供更多可能（判断标准）/ 3.7对照输出分析 |
| layer-3 人物性情 | 第四部分（4.1–4.15） | 4.1性格不是常驻状态 / 4.2标签给方向 / 4.3赋予事情意义 / 4.4真正在乎什么 / 4.5看待自己 / 4.6写倾向不写唯一答案 / 4.7完整幅度 / 4.8不强求反差 / **4.9不让一段关系代替整个人** / 4.10跨场景价值 / 4.11停在根部 / **4.12分清事实解释空白（与档案四状态呼应）** / 4.13不写成新固定反应 / 4.14六步操作法 / 4.15对照输出分析 |
| layer-4 场景表达 | 第五部分（5.1–5.9） | 5.1发言不是装饰 / 5.2表达体量 / 5.3情绪有来路有经过 / 5.4身体进入更大空间 / 5.5不只回应时存在 / 5.6沉默不等于没表达 / 5.7不写成表演说明书 / **5.8从性情写到场景表达（两层边界判断标准）** / 5.9对照输出分析 |
| 模型原理（写每层前提，不属四层） | 第二部分（2.1–2.4） | 2.1写的是提示词 / 2.2模型原理与六条写作推论 / 2.3六条观察标准 / 2.4稳定与自由不矛盾 |
| to-final-text（整合输出） | 第六部分（6.1–6.7） | 6.1五步整理流程 / 6.2四部分不必一样长 / 6.3只留下人物 / **6.4五条删减自查** / 6.5采访小传留后台 / 6.6人设之外另有位置 / 6.7写到能继续生活为止 |

注意：6.1 覆盖全部四层，是整理流程总入口，每层实现者都该读。3.7、4.15、5.9 是各层的对照输出分析（实证部分），说明每层能解决什么、不能解决什么，避免过度设计。

### 6.3 接手者任务优先级

新前提下，接手者面临「写剩余文件」和「发现已有文件问题」两件事。优先级：
- **首要**：写剩余文件（layer-2/3/4、to-final-text、档案模板、assets）。
- **次要**：写的过程中如发现 layer-1/SKILL.md 的写法与设计意图冲突，标记下来（记在哪都行，格式：handoff 决策是什么 / 落地文件实际写了什么 / 为什么认为冲突 / 教程依据），但**不专门逐行审查已有文件**。
- **不做**：逐行审查已有文件、重写已有文件。那是用户后续统一审查的事。第一轮审查已做完，结论在 §七——不要重做。

### 6.4 写作纪律

- 说人话，不造词。handoff 旧版用过「转译思想」「透明引导」「微原理」「岔路原理」「深度原理」「两层 why」「风险提示式」等内部讨论词——这些不是正式术语，别写进 layer 文件。
- 占位写「待定：等 X 决策」并写清将覆盖的范围和依赖哪个决策，不用 TODO。
- **引用教程必须逐字核对，包括章节号。** 第一轮审查查出 10 处教程依据错误，全是凭记忆写的（把 4.15 的性情对照说成生活结构、引了不存在的「教程 1.0」、把教程的「可能」写成「都会」、把主 agent 自己的因果链说成教程实测结论）。写每一条带「教程 X.X」的句子时，回原文对一遍。
- **别替教程加确定性。** 教程大量使用「可能」「往往」「通常」「不能证明」，且明确说过「不能证明某种写法在所有模型和所有场景中必然获胜」。skill 里出现「唯一」「必须」「多数」「都会」「根源都是」时，先回教程看它是不是同样确定。
- **不给"教程没有但必须自己定"的东西假造教程依据。** AI 怎么跟用户说话、追问几轮、代笔边界——教程压根不管这些（它假设作者自己深挖）。自己定就自己定，标「主 agent 判断」，不要往教程身上挂。
- **用案例时正反都要用。** 教程的四个角色（程雾/夏燃/贺明珠/唐酥）既有翻车记录也有正面前后对照。第一轮审查发现 skill 只用了负面，AI 因此只知道"不要做什么"、不知道"应该做什么"（§7.1-2）。

### 6.5 怎么评估这个 skill 的质量（复用这套维度，别临时发明标准）

评估标准全部能追回 §一 的成功标尺，追不回去的就是自己加戏。分两类，成本差一个数量级。

**A 类 · 审文档（不用跑，随时能做）**

| 维度 | 测什么 |
|---|---|
| 忠实度（**必须双向**） | 教程→skill：教程每条规定、警告、实测证据在 skill 里有没有归宿？<br>skill→教程：skill 每条断言能不能指回教程某节？指不回去的，是从原则推的、是交互层面必须自己定的、还是编的？ |
| 教程的不确定性有没有被保留 | 教程非常诚实地记了哪些改善**没**出现（夏燃加生活结构基本没变化、唐酥前后都不稳定、补造事实一直没消失、贺明珠仍会缩回职场谦让）。skill 有没有把"可能有用"写成"必须这么做" |
| 转译质量 | 教程里大量定性词（"给足素材""够了""过多"）。skill 有没有变成 AI 真能执行的判据，还是照抄了一遍 |
| 内部一致性 | 文件之间对不对得上、路由指的文件存不存在、有没有两条规则打架 |
| 复杂度是否必要 | 用教程 6.4 那五条反过来审 skill 自己：每段删了会怎样 |

**B 类 · 跑起来测（得文件写全）**

| 维度 | 测什么 |
|---|---|
| AI 执行会不会卡 | 拿具体情境从头走一遍，看每步能不能从 skill 里找到依据。卡住的地方就是规则没覆盖或两条冲突 |
| 产物纯净度 | 机械查：转出来的卡里有没有演绎指令、工程术语、栏目说明、过程痕迹。最容易查，最不容易蒙混 |
| 实际效果 | **抄教程自己的方法**：拿它冻结的四份人物资料（程雾/夏燃/贺明珠/唐酥），用 skill 走一遍，跟教程里作者自己整理的结果比——skill 带出来的东西够不够接近懂方法的人自己整理的水平。教程 3.6 记录了它就是这么做的（74 份有效对照） |
| 用户会不会跑掉 | 追问轮数、代笔占最终卡的比例、用户"行吧就这样"的次数 |
| 触发准不准 | 该触发时触发、不该触发时不触发。description 那行的质量 |

**两条关键做法（第一轮审查验证有效，照做）**：

1. **忠实度不能自评。** 主 agent 若误读了教程某处，让它再核对会用同一个误读去核对，什么都查不出来。必须交给独立 agent。
2. **不要把 handoff 给审查者。** handoff 是主 agent 对教程的转述，转述本身可能就是误读源头。**只给教程原文 + skill 文件**。第一轮正是这么查出「程雾案例归错层」和「五处假借教程之名」的——这些主 agent 自己看多遍都没看出来。
   附带做法：查"AI 执行会不会卡"那个 agent **刻意不给教程**，让它保持"只拿到 skill"的状态（真实使用时执行者就是这样），否则它会不自觉用教程知识补上 skill 的缺口，缺口就测不出来。

---

## 七、落地文件现状与已知问题

> 2026-08-17 已做第一轮独立审查：五个 agent（knot / `deepseek-v4-pro` 最高强度推理）拿**教程原文**逐条核对 SKILL.md 和 layer-1。关键设置：**一个都不给这份 handoff**——handoff 是主 agent 对教程的转述，转述本身可能就是误读源头。
> 完整报告（证据、教程原文对照、agent 结论的采纳与驳回及理由）：`.claude/skills/char-habitat/review/round-1-review.md`。
> **下面全部是已确认的问题，一条都还没修。** 改之前先读报告。

### 7.0 必改：教程依据出错（已逐条回教程原文确认）

这批是**假借教程之名说错话**，性质最严重——写 layer-2/3/4 的人会把它们当实测结论继续引用，误读会复制。

| # | 文件 | skill 写的 | 教程实际 |
|---|---|---|---|
| 1 | SKILL.md | 程雾「推眼镜→修信」的变化是**补上生活结构**的功劳 | 教程 4.15 明说这是「基础信息＋**人物性情**」的对照。把最有力的证据安在了错误的论点上 |
| 2 | SKILL.md | 「**唯一的**解法是让空白有地方待着」 | 教程从没说这是唯一解法（3.4 区分事实与解释、6.5 区分采访小传也都是手段） |
| 3 | SKILL.md | 「**多数**角色只需两层」 | 6.2 说「**有的**角色最终只需要基础信息与人物性情」，并列举三种情况，未量化 |
| 4 | SKILL.md | 「**教程里最有说服力的**是正面对照」 | 教程从没评价自己哪部分最有说服力。3.6 反而明确限定「不能证明某种写法在所有模型和所有场景中必然获胜」 |
| 5 | SKILL.md | 「那个弟弟就是**在没有这道标记的情况下**发生的」 | 3.7 记了弟弟这事，但**从没把它归因于缺少来源标记**——教程压根没讨论标记机制。因果链是主 agent 造的 |
| 6 | SKILL.md | 「摘眼镜、摸车票，**根源都是**提示词给过这个镜头」 | 3.7 只说这些小动作"继续存在"；紧跟的因果是「**若生活结构把每个爱好和物件都解释成意义**，这些内容反而更容易再次出现」——是过度解释物件，不是给过镜头 |
| 7 | SKILL.md | 硬约束写「模型**都会**照着学」 | 2.1 写「都**可能**」。2.2 还说「提供更多内容不必然等于理解得更好」 |
| 8 | layer-1 §8.2 | 引「教程 1.0」 | 教程没这个编号，内容在第一部分开头无编号导言段。**其余 8 处章节号全部正确**（4.15/5.9/1.4/1.3/1.5/4.9/1.2/1.6） |
| 9 | layer-1 §8.1 | 「静态混入动态」放在「有实测证据」档 | 文件自己注明「教程未做 A/B 对照输出、实测对比暂缺」——按它自己的标准该在 §8.2 |
| 10 | layer-1 §8.1 | 「程雾摘眼镜在多模型输出中反复复读」 | 教程的论点是「小动作不会因为加了更深层内容就自动消失」，不是"跨模型反复复读"。定性比教程强 |

**驳回了一类 agent 结论**：有 agent 把「三轮」「200 字」「八成」「三次」「不许连续两段讲原理」全判成"凭空发明"。**不采纳**——教程压根不管 AI 怎么跟用户说话（它假设作者自己深挖，不存在把用户追跑的问题），这些数字必须自己定，且 SKILL.md 没有假借教程之名说它们。真正的错只是上面 1–7 那些**冒充教程说过**的。

### 7.1 必改：内容缺失与减重过度

| # | 缺什么 | 依据 | 后果 |
|---|---|---|---|
| 1 | **夏燃案例整个没有** | 教程 3.7 + 5.9。这是教程唯一系统性证明「不写比写更好」的案例 | 「可以跳过」没有任何实证支撑，AI 会"保险起见还是写一点"。这条是 §5.10[未定1] 那个结构问题的解药 |
| 2 | **四个角色的正面案例全丢，只用了负面** | 贺明珠生活结构最成功的对照（从"合同是大家一起拼下来的"到"这一杯先敬自己"，教程特别强调"她没有被改成只顾个人功劳的人"）· 唐酥场景表达的正面对照（从内心独白到"退票去周边小城""只定今晚的去处"）· 程雾场景表达的正面对照（5.9） | AI 知道"不要做什么"，不知道"应该做什么" |
| 3 | **教程 3.7 后三条结论** | 「以稳定事实为主少写心理解释」「更适合重组压缩已有资料而不是继续堆叠」「**写得过多、过满或过度解释时也会增加模型复读、补写和忽略其他信息的机会**」 | 缺了最后一条，AI 会认为生活结构多多益善 |
| 4 | **教程 2.1 的三作用框架** | 确定事实 / 提供理解方向 / 影响表达倾向。这三条正好一一对应四层分工（事实=基础信息、理解方向=性情、表达倾向=场景表达）。§5.5 只压了第三条的一角 | AI 只知道"要分四层"，不知道每层在模型生成中扮演什么角色，容易机械填表。补起来很便宜，三行字 |
| 5 | **多情境测试从三维压成一维** | 教程 3.5 是「是否改变人物**注意到什么、能够做什么或不能丢下什么**」+ 一条排除标准「只能贡献一个漂亮物件或一种固定动作的，上下文价值通常很低」。SKILL.md 只剩"她现在有事可做吗" | 单维度会让 AI 把"她每天浇花"当有效生活结构 |
| 6 | 教程 4.2「同一个标签落在不同人身上」的认知 | 教程用"温柔""骄傲"各展开四种含义。**不需要教用户**（用户不必懂方法），但执行 skill 的 AI 必须内化——AI 自己不理解"温柔有四种"，用户说"她很温柔"时它就会停止追问 | 写 layer-3 时必须补 |

### 7.2 必改：缺定义、路由不同步

| # | 问题 | 详情 |
|---|---|---|
| 1 | 档案模板文件名三个版本 | handoff §4.2 用 `character-archive-template.md`，SKILL.md 路由表用 `character-file-template.md`。以 handoff 为准，改 SKILL.md |
| 2 | 路由表未同步文件合并 | SKILL.md 仍分列 `card-template.md` 和 `correct-samples.md`，§5.7 已定合并成 `samples-and-skeleton.md` |
| 3 | layer-1 §6.2 措辞与 §5.8 矛盾 | 写「风险提示式优先于正面讲原理」，应为「各占一半，不定主导形式」。（本轮审查独立确认：教程最有说服力的部分恰恰是正面对照，见 7.1-2） |
| 4 | 说话深浅规则两处不一致 | SKILL.md 有两条 layer-1 没有的（用户说"别解释了直接写"降到纯引导 / 连问三次以上可问要不要系统讲）。AI 只读一边行为会不一致 |
| 5 | §九 禁读列表不全 | SKILL.md 末尾的禁读列表少了 `knot-cli` |
| 6 | 「动态记录」等四词零定义、三种说法 | 见 §5.10[未定3] |
| 7 | 「演绎指令」无边界定义 | 见 §5.10[未定4] |

### 7.3 可压缩：SKILL.md 承重比例约 43%

用教程 6.4 那五条反过来审 skill 自己（约 210 行，约 90 行真正承重）：

- **真承重**：三条硬约束（12 行）· 四状态来源标注系统及使用规则（20 行，被评为「skill 对教程最关键的工程化贡献」）· 全程要盯的两种错（25 行）· 代笔三条硬约束（15 行）· 「先接住，再整理」（5 行，短但决定了 AI 第一次接触用户时的全部行为）· 终止条件「能在陌生场景继续生活」（5 行，没它 AI 会无限追问）
- **删了几乎不影响 AI 行为**：「说话的三档深浅」表格及详细说明（约 30 行，从"用户不需要知道在填哪一层"和"讲完原理立刻回到角色"两条原则就能推出深度）·「讲道理不如讲后果」的多个示例（约 15 行，原则一句话就说清）· 「写给你的 why 和透给用户的 why 不是一回事」整节（约 8 行，layer-1 §6.3 有更详细展开，这版是重复）

### 7.4 已预测的失败场景（写剩余文件时防着）

1. **AI 替用户编造角色生活**——面对用户说"验尸写报告"，无法判断是"够了"还是"碎片"（§5.10[未定2]），按自己标准判为"不够"，然后补上"经手过 200 多具尸体、专攻溺水案、市级鉴定中心、严厉的导师"。缺一个"停手"信号。
2. **用户被追问到放弃**——layer-1 事实域映射太详细（必要域 7 项 + 条件域 7 类 + 关系域 4 类），虽说了"不是填空表"但没给「单次最多问几个域」的上限，AI 面对清单本能会逐项覆盖。
3. **角色卡变成 AI 写的**——逐段确认只降低了单次字数，没改变"用户对着漂亮文字天然点头"这个根本问题。没要求用户用自己的话重说一遍，也没要求每段至少给一处修改。
4. **产物里漏进演绎指令**——因为"演绎指令"没有定义边界（§5.10[未定4]），AI 只能凭感觉，而感觉偏向保留。
5. **关系写成"除了 {{user}} 她没有生活"**——禁止为 {{user}} 虚构关系是对的，但没有安全阀：当关系域只有 {{user}} 时应主动提醒风险并追问一个不依赖 {{user}} 的关系或生活线索。

### 7.5 还没测的（需文件写全才能做）

- **产物纯净度**：转出来的卡里有没有演绎指令、工程术语、栏目说明、过程痕迹。等 `to-final-text.md` + 档案模板写完。
- **实际效果（唯一真正说明问题的检验，也最贵）**：抄教程自己的方法——拿它冻结的四份人物资料（程雾/夏燃/贺明珠/唐酥），用 skill 走一遍，跟教程里作者自己整理的结果比，看 skill 带出来的东西够不够接近懂方法的人自己整理的水平。教程 3.6 记录了它自己就是这么做的（四份冻结资料、AI 直出 vs 人物生境、同场景同来话、74 份有效对照）。
- **用户会不会跑掉**：追问轮数、代笔占最终卡的比例、用户"行吧就这样"的次数。这三个数字直接对应 7.4 的失败场景 2 和 3。
- **触发准不准**：该触发时触发、不该触发时不触发。description 那一行的质量。

### 7.6 其他文件

- `to-final-text.md`：未写。写时照 §5.3（首指令列确认状态 + 6.4 五条自查 + 禁入清单）。
- `character-archive-template.md`：未建。照 §5.4（四层骨架 + 四状态 + 待定区）。
- `samples-and-skeleton.md`：未写。示例刻意不等长、不含过程痕迹；调子见 §5.8。**这里是补 7.1-1、7.1-2 那些案例的好位置**（夏燃 + 三个角色的正面对照）。
- `checklist.md`：未写。主体教程 6.4 五条 + 产物痕迹排查。
- layer-2/3/4：未写。照 §6.1 参考 + §五决策 + §6.2 教程映射。

### 7.7 layer-1 与 §五决策的力度偏差（非错误，待用户裁）

不默认 layer-1 错，也不默认 handoff 对，回教程判断：

| # | handoff 决策 | layer-1 实际 | 性质 |
|---|---|---|---|
| 1 | 每层四部分（§5.1） | 实际九节，四部分都在但膨胀约一倍 | 力度不同。可视为方法说明的展开，若期望严格四部分需裁切 |
| 2 | 四状态标注（§5.4） | 全文未提 | 未实现。核心决策，layer-2~4 须含 |
| 3 | 「本块待定/全局待定」（§5.2） | 未提 | 未实现 |
| 4 | 代笔三条硬约束（§2.2） | 未提 | 未实现 |
| 5 | 追问三轮停止（§5.6） | 未提 | 未实现。跨层，更适合 SKILL.md，但克制纪律里该提 |
| 6 | 文件合并（§5.7） | §九 仍引用独立的 `correct-samples.md` | 位置不对 |
| 7 | 精选范例调子（§5.8：一句嵌追问、口语化、有画面感） | §七 提问示例偏模板化、清单感，缺"像人聊天"的口语质感 | 力度不同，方向对 |

layer-1 的高质量部分（§一~§六 核心方法、判断边界、透明度策略）可作 layer-2 的参考——但只参考，见 §6.1 的「照做 / 只参考可质疑」两份清单。

### 7.8 待核对：今天另一会话的动作（未对照 round-1-review）

2026-08-17 有一个**独立会话**在不知道本 handoff 新版 + 第一轮审查存在的情况下做了一批动作。它开始时读的是 handoff 旧版（294 行）。**改任何 layer-1 / 档案模板前，先核对这些动作和 round-1-review 是否重复或冲突。**

1. **layer-1 忠实度核对（重复劳动）**：该会话派 knot + 自核各一份，核对 layer-1 对教程第一部分的还原度。和第一轮审查（§7）角度重叠（它额外核了「作者写给作者的话」是否该转译），但结论大部分被 §7 覆盖。
2. **layer-1 被该会话改了 6 处**（基于它自己的核对，未对照 round-1-review）：
   - ① 开头补「除非因果确实属于人物经历」例外
   - ② §二 明示「没有绝对固定表格、不保留空项」
   - ③ §3.2 补「故事开始时是普通朋友」例
   - ④ §五 关系补「不必为独立故意削弱」+ 竞争对手
   - ⑤ **§九 指向改成 `character-archive-template` + `correct-samples.md`——和 §5.7 冲突**（§5.7 已定合并成 `samples-and-skeleton.md`，该会话把 §九 指向了应被合并掉的旧文件名，需回退按 §5.7 改）
   - ⑥ 开头职责说明同步
   - 其余 5 处是补缺，但没和审查对齐，明天需逐条和 round-1-review 核对是否重复/冲突。
   - 另：该会话还做了 skill-creator 自审小修 4 条（硬约束转 why、克制补 why、imperative、示例指向），混在这 6 处里。
3. **建了 `assets/character-archive-template.md`——不符合 §5.4**：该会话建的版本是简陋四层+备注，**没有四状态标注、没有待定区机制**。§7.6 说这个文件「未建，照 §5.4」——该会话建的版本需按 §5.4 重做或弃用。
4. **dot-skill 研究 + knot 提问方法论（今天唯一真正新增工作）**：结论已进 §5.10 [未定 7]，不是待核对问题。

**明天处理顺序建议**：先读 round-1-review 核对今天的改动（尤其 layer-1 六处和档案模板），决定保留/回退/重做，再走 §八的两条路选择。

---

## 八、待办（按依赖链）

> **下一步该做什么**：用户睡前的状态是——审查做完了、问题清楚了、**一条都还没改**。有两条路可走，等用户定：
> - **路 A：先修再往下写。** 先做 §7.0 那 10 条纯改错（不依赖任何决定，改完 skill 至少不再假借教程之名说错话），再让用户过 §5.10 那六条未决，然后继续写 layer-2/3/4。好处：layer-2/3/4 不会沿着已知的错误往下长。
> - **路 B：先写完再统一修。** 按原计划先写 layer-2/3/4，最后统一修。好处：不打断写作节奏。风险：§5.10[未定1] 那个结构问题（减法 vs 加法）会被复制到另外三个层文件里，改的时候要动四个文件而不是一个。
>
> **主 agent 倾向路 A**，但只做 §7.0（纯改错），因为 §7.1/§5.10 都要用户拍板。

1. **§7.0 十条纯改错**——不依赖任何决定，可立刻做。改 SKILL.md 七处 + layer-1 三处。
2. **§7.2 六条路由/措辞同步**——文件名统一、路由表合并、layer-1 §6.2 措辞、禁读列表补 knot-cli 等。也不依赖决定。
3. **用户过 §5.10 六条未决**——尤其 [未定1]（删减检查下沉，影响所有层文件的写法）和 [未定2]（十一条判据模糊，量最大，建议单独一轮）。
4. **layer-2 / layer-3 / layer-4**——照 §6.1 + §6.2 + §五决策。三件可并行。**写之前先看 §5.10[未定1] 定了没**——它决定每层文件要不要带"本层收尾删减检查"这一节。layer-3 必须补 §7.1-6（标签的四种含义）。
5. **`character-archive-template.md`**——照 §5.4。可与 layer 文件并行。
6. **`to-final-text.md`**——依赖档案模板（需知道档案里有什么标注）。
7. **`samples-and-skeleton.md`**——依赖 layer 文件定稿。**§7.1-1、§7.1-2 那些案例（夏燃 + 三个角色的正面对照）适合放这里**。
8. **`checklist.md`**——依赖 to-final-text。
9. layer 文件定稿后回填 SKILL.md 操作细节。
10. **测试**——按 §7.5 的四项。其中"实际效果"那项（拿教程冻结的四份人物资料走一遍再和作者的版本比）是唯一真正说明问题的检验。

---

## 九、禁忌

- **禁止读仓库里已有的相关 skill**：`.claude/skills/char-creator/`、`性格调色盘/`、`.claude/skills/knot-cli/` 等。用户明确说那些都做失败了，找来重做就是因为它们失败。不要参考、不要被污染。只读教程原文 + skill-creator。SKILL.md 末尾也有这条，但那里的禁读列表少了 knot-cli，需统一。
- 不要造词，说人话。
- 不要替用户定架构决策，讲清楚让用户定。

---

## 十、当前进度

- [x] 对齐宗旨与原则
- [x] 敲定文件架构（按层切、每层一文件、含角色档案+转世界书）
- [x] §五各项设计决策已定（转正文放哪 / 角色档案组织 / 教程第二部分归宿 / 追问停止 / 文件合并 / 透明度各占一半）
- [x] **第一轮独立审查已做**（2026-08-17，五个 agent 拿教程原文核对 SKILL.md + layer-1）。报告：`.claude/skills/char-habitat/review/round-1-review.md`
- [~] SKILL.md 初稿——已写，**已审出问题，一条未修**（§7.0 七处教程依据错误 + §7.1 内容缺失 + §7.2 路由不同步 + §7.3 可压缩约 53 行）
- [~] layer-1 基础信息——经用户审改 + 第一轮审查出问题。**今天（2026-08-17）另一会话又改了 6 处，未对照 round-1-review，其中 §九 指向和 §5.7 冲突——见 §7.8，需明天核对**
- [ ] §5.10 六条未决事项待用户拍板（[未定1] 删减检查下沉最重要，影响所有层文件写法）
- [ ] §7.0 + §7.2 纯改错（不依赖任何决定，可立刻做）
- [ ] §7.8 核对今天另一会话改的 layer-1 六处 + 建的档案模板（和 round-1-review 比对，决定保留/回退/重做，再走 §八两条路）
- [ ] layer-2 / layer-3 / layer-4
- [ ] character-archive-template.md
- [ ] to-final-text.md
- [ ] samples-and-skeleton.md / checklist.md
- [ ] 回填 SKILL.md 操作细节
- [ ] 测试（§7.5 四项，其中"拿教程四份冻结资料走一遍再和作者版本比"是唯一真正说明问题的）

> `[x]` = 已定稿 · `[~]` = 已写出但未审查/未定稿 · `[ ]` = 未做
